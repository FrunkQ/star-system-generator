# Observations inbox

Raw observations land here as they are noticed, get triaged, and are handed to a worker session as a
grounded prompt. This file is the source of truth between sessions — an item that is only in a chat
transcript is an item that will be lost.

**How to use it.** Add anything to CAPTURED, however rough, without stopping to diagnose. Triage moves an
item into a bucket with a verdict. When a bucket is worth a session, a prompt is written from it and the
items are marked with the version that fixed them.

**Status vocabulary:** `captured` (raw) · `triaged` (understood, not started) · `assigned` (a session has
it) · `fixed vX.Y.Z` · `wont-fix` (with the reason).

Last triage: 2026-07-31, at v2.1.313-beta. Bucket B worked 2026-08-01, at v2.1.331-336-beta.

**Signed off at v2.1.313-beta.** Bucket F is CLOSED (F11 untouched -- it is a feature). The GM starmap
chrome batch is CLOSED: A8, A11, A12, A13, A14, A15, A16, A18 all done, on top of A1, A3, A4 and A9
earlier in the same run. Two items landed that were never inbox observations, recorded so they are not
lost: the Field Guide and Projector rail entries now sit behind the same `PLAYER_VIEWS_ENABLED` flag as
Player Views, so beta shows one door and a production cut restores the old pair (v2.1.287-beta); and the
info-block body picture now fills its space, where flat discs had a 220 px ceiling and the 3D globe was
framed at the map style's 50% (v2.1.289-beta).

**Bucket B (the physics engine) had its session, 2026-08-01, at v2.1.331-336-beta.** Four of the five
items are closed and the fifth was deliberately NOT built. **Read this before picking any of it up:**
- **B9(a), B7, B8, B6 are FIXED.** TWO OF THE FOUR HAD THE WRONG ROOT CAUSE RECORDED (B9a and B8), B8's
  own suggested test refuted its own hypothesis, and B6's headline evidence turned out not to be
  load-bearing. Read the entries rather than the original triage.
- **[[B5]] IS STILL OPEN AND WAS STOPPED AT ITS GATE, on purpose.** The clean ordering split does not
  exist — geology depends on the thermal solve through FOUR inputs, not the one the triage names, and
  three of them are water-phase products the solve produces. The entry records what was tried, what
  joining the fixed point would cost, why the obvious frost shortcut is the bistability trap, and why
  the bare-rock half alone would make Mars worse. It also names a safe first step for the next
  session. An unfinished B5 is the intended outcome, not a failure.
- **[[B9]](b), the stellar rotation/magnetism model, was NOT built either** — scoped in the entry for
  the HR / stellar-generation phase. A star's field is pack data for now and its rotation is honestly
  absent.
- **B15, B14 and B12 were then done as tails** (v2.1.336-337-beta). B15 and B14 are fixed; **[[B12]] is a WONT-FIX on the constant with the anchor table to prove it** — Venus and Mercury both clamp to the same rotation factor and have opposite outcomes, so no measured body's result would change wherever the threshold moved. Its STRUCTURE is separately falsified by Mercury and Ganymede, and a geology-driven rebuild is scoped in the entry — but do NOT sell that rebuild as restoring the ~40 lost dynamos, because 24 of them are Venus-like episodic-lid worlds a better model would also suppress.
- **A second sanity-check pass then closed [[B19]] and [[B18]]** (v2.1.338-339-beta). Both had the wrong cause
  in the entry and both were confirmed by measurement before anything changed: B19's belt hypothesis was
  refuted (belts are already excluded, with a test) and its 5.5 threshold never fired — the pair is Mars
  against a 16 km asteroid at delta 9.28, and the fault was applying one pair's fate to both members. B18
  was a category error, not a weighting artefact: a gas giant has no surface, and the surface axis was
  giving it full marks on the three factors measured at its notional 1-bar level.
- **[[B17]] is CLOSED** (v2.1.340-beta): the belt term. The engine summed luminous sources only, so a
  moon deep in a giant's magnetosphere had no representation of its dominant hazard. Io and Europa now
  differ by 8.9x where they used to agree to four significant figures. Calibrated on Io and Callisto,
  with Europa (0.76x) and Ganymede (1.54x) as predictions it was not fitted to. **[[B20]] depends on
  this and is the natural next one.**
- **[[B22]] is CLOSED** (v2.1.341-beta): a planet was never inside its own belt, because the belt term
  resolves its host as the parent and a planet's parent is its star. Fixed with an INNER EDGE derived
  from atmospheric scale height, without which the same law puts 2.31 Sv/day on Earth's ground. Radiation
  is now TWO named figures on every body — surface / 1-bar, and the environment above the atmosphere —
  which is the shape **[[B20]] should extend rather than replace.** Derived diff was pure addition.
- **New from that session: [[B10]] [[B11]] [[B13]] [[B16]] [[B24]] [[B25]] [[B26]], plus `docs/dev/generation-duplication-map.md`.** **[[B13]] is the one to
  read** — `process()` is not idempotent, surface radiation drifts up to 18% on a second pass, it is
  pre-existing, and it is the SAME ordering fault as B5 in a much cheaper place. Fixing it first would
  establish the pattern B5 needs.
- **Before touching GENERATION at all, read `docs/dev/generation-duplication-map.md`.** Written in the
  same session after hitting the wall twice: two live system generators, three body-creation routes and
  five places that invent a magnetic field, most of which the processor then discards — but NOT safely
  deletable until [[B13]] is fixed, because pass 2 reads them before pass 3 overwrites them. The tidy-up
  itself is scoped for **V2.3+** and is deliberately not started.

**What is open, and what to pick up first.**
- **A17 is the one correctness bug on the board.** The measure tool has never counted depth, so a GM is
  told roughly half the distance the routes and journey durations use for the same pair. Small fix, found
  and diagnosed but NOT shipped. Do this before anything cosmetic.
- **The whole guide-document line is DONE** (v2.1.323-331-beta): A5, A6, A2, then D3, A25, A26, A27, A28, A29, plus two requested features (side-by-side navigation buttons, and rainbow buttons matching their marker on the chart). **Left behind by the closing batch, and the first is the one to read:** [[D4]] — the starmap build kit NO LONGER REPRODUCES the shipped maps and running it reverts twelve hand-applied fixes including C3's `frame: 'ecliptic'`; do not `node scripts/starmap-build/build-starmaps.mjs` and keep the output until that is resolved. Then [[A30]] (what a construct should look like in the info block, now that the wrong picture is gone) and [[A31]] (a construct has no RATED Δv, only its current one, so the capacity view shows none).
- **A5, A6, A2 are DONE** (v2.1.323-325-beta): the guide-document batch. The stat block's temperature range
  is the surface range and a frozen surface is no longer called liquid; a star's page lists its planets, so
  the page you land on finally has a way down; and a construct has its own block instead of a planet's.
  All three verified on BOTH `DocPanel` and `FilteredDocumentView`. **They left six items behind, and the
  first two are the ones worth reading:** [[D3]] (two nodes share one ID in two bundled systems, so one of
  each pair cannot be selected at all — a DATA bug in the build kit) and [[A27]] (`cargoDescription`
  reaches players with no hide flag of its own; needs a decision, not a fix). Then [[B8]], [[A25]],
  [[A26]], [[A28]]. A5 also strengthened the cross-references on [[B6]] and [[B7]]: both are the same
  "two rows, two pipeline stages" fault, and both are VISIBLE in the stat block rather than internal.
- **A19, A22, A23 and A24 are DONE** (v2.1.319-321-beta): the holo scene has a floating origin, barycentre
  members have orbit rings about their barycentre, orbit rings re-sample about the focus so they stay smooth
  at any zoom, and constructs draw their orbits. All four confirmed on screen except the last.
- **B5, B6, B7** all want a physics session holding that context, and each needs a before/after diff
  across the bundled maps. B6 alone moves ~14 classification fingerprints.
- **C3** (moon orbits ignore the parent's axial tilt) and **C4** (on-planet constructs render at the body
  centre) are both "a declared fact is being re-derived from geometry that may not be there" -- worth
  scoping together.
- A10, C2, D1, D2 are parked and low value. E1 is not ours. F11 is a feature.

---

## A — UI bugs (self-contained, no physics)

| # | Observation | Status |
|---|---|---|
| A1 | **Guide-tip header/footer banner does not reflow on resize — it is a BITMAP being stretched.** Scoped 2026-07-30. The banner is a canvas built by `drawHud()` (`lib/catalogue/infoCard.ts`) and drawn onto a full-frame quad, so once built the wrap points are baked in at that width and any size change magnifies the picture. The font sizing is innocent: `fontPx = max(12, min(viewW*0.011, viewH*0.026, 20))`, clamped 12-20px — nothing computes a giant font, a small canvas is being upscaled. **Scope is narrow: three call sites, two at risk.** (1) Player STARMAP, 2D and 3D, via `Starmap3DView.applyTips()` — ALREADY GUARDED, it re-measures the container on every build and carries a comment about this exact failure. (2) Player SYSTEM 3D via `catalogue/+page.svelte:600`, a reactive `hudCanvas = drawHud({...})` feeding `HoloView` — **prime suspect**, check whether its viewW/viewH refresh on resize. (3) DOCUMENT headers/footers (`document/blocks.ts`, `documentStyles.ts`) are a SEPARATE path doing real layout, not a bitmap — expected unaffected, confirm only. **Note the puzzle:** the reported screenshot is the STARMAP, which is the guarded surface — so either the player starmap does not go through `applyTips`, or there is a second stale-size source. Identify which surface actually drew it before fixing. Best fix moves the re-measure INTO `drawHud` so a third caller cannot reintroduce it. **Session notes (2026-07-31, v2.1.304):** could not reproduce headless (the automated pane stops compositing, which suspends the ResizeObservers under test). Reviewed all three sites: (2) the catalogue hudCanvas IS reactive on hudW/hudH and `setHud` re-sizes its quad on scene resize, so it should track a visible-window resize — same for the starmap scene. One real mechanism found and closed: RO notifications deliver BEFORE PAINT, so a resize landing while the window is not painting (minimised, background tab, other desktop) leaves a canvas at the stale size until something forces a render — the DOCUMENT view (path 3) now also re-measures on window resize + visibilitychange. If the screenshot came from a resize-while-hidden, that may be the whole story for path 3 but paths 1-2 would still bitmap-stretch until their next prop change. **ROOT CAUSE FOUND (live repro by Alex, v2.1.305): the rebuild always ran and never reached the screen.** Both scene setHud implementations swapped the rebuilt canvas into a live texture via `map.image = ...` -- and WebGL2 texture storage is immutable once allocated (texStorage2D), so uploading a DIFFERENT-SIZED canvas fails silently and the quad keeps stretching the stale bitmap. Every re-measure guard upstream was therefore working and irrelevant. Fixed by recreating the texture whenever the canvas dimensions change (system holo + starmap scenes), and the same trap closed in the label sprites (drawLabel resizes its canvas under a live texture on font/text change). The document view was never affected -- its setSource already recreates per frame. Followed by a fine-tuning pass centring the text within the band (v2.1.306). | fixed v2.1.304-306-beta |
| A2 | Player-view construct panel is anaemic — Blip-A shows Type / Orbit distance / Atmosphere, i.e. BODY fields, while the data holds crew, engines, fuel tanks, cargo and delta-v. Constructs need their own block in the document / DocPanel builder. **FIXED v2.1.325-beta.** **Root cause: there was no construct branch at all.** `bodyFacts` had exactly one construct-aware line — the Type label at `:70` — and a construct then fell through the whole body block, matching only the rows a ship happens to share with a world. Blip-A's three rows were Type, Orbit distance and Atmosphere, and the last one said "None". **Everything in the new block is READ from `calculateFullConstructSpecs`**, the existing derivation behind the GM's Construct Derived Specs panel, the transit planner, the autopilot adapter and the interstellar modal — nothing is recomputed, so the player block and the GM panel cannot print different numbers for one ship. Confirmed at the formatter too: the GM uses `fmt.speedMs`, which is `formatSpeedKmS(ms / 1000, …)`, character-for-character what the block now calls for Δv. Twelve rows for Blip-A: class, location, crew, supplies, dimensions, dry mass, cargo against capacity, total mass, power surplus, fuel by name, max acceleration, Δv. **The rule pack had to be threaded in, and that is the one structural change:** engine and fuel DEFINITIONS live in the pack, not on the node, and without them a tank has no density and an engine no Isp. `GuideDocOpts` and both consumers (`DocPanel`, `FilteredDocumentView`) take an OPTIONAL `rulePack`, passed at all six mount sites; the host is resolved inside the builder the same way the parent-nav row resolves it, so the two cannot name different parents. **The honest-degradation rule was applied twice after testing the no-pack path and finding it QUIETLY WRONG rather than merely short** — the same fault class as A5. A tank whose fuel is not in the pack contributes zero mass, so "Total mass" read 1,200 t instead of 2,300 t; and an unresolved engine has no power draw, so what was labelled "Power surplus" was really the plant's output (100 MW against a true 60). Total mass is now suppressed unless every tank resolved, and the power row is relabelled "Power output" unless every engine did. **Redaction: nothing new is exposed.** The boundary is node-level — `computePlayerSnapshot` drops hidden subtrees and `gmNotes`, and optionally `description`; every field this block reads (class, placement, crew, `physical_parameters`, `systems.life_support`, `power_plants`, `fuel_tanks`, `engines`, `current_cargo_tonnes`) already crossed to players. **One field deliberately LEFT OUT because it is not clearly player-safe: `cargoDescription`** — see [[A27]]. **Verified on all 26 constructs in the campaign** (the block reads sensibly for a 6-million-crew colony drum, a 1,000 km ring gate and two derelicts alike) and live on BOTH consumers with identical rows: `FilteredDocumentView`, reached by drilling Tau Ceti → Adrian → Blip-A, and `DocPanel`. **Not exercised on live data, said plainly: the `Status` row and a construct's `Tags` row** — no construct in the bundled campaign carries a `flight_state` (it is set at runtime by the transit planner) or a single tag. **One knock-on fixed in the same commit:** the panel subtitle appended the singular `class`, which only constructs carry, so with the class now properly in the Type row it read "Ship · Ship/Interstellar/Eridian". Suppressed for constructs; bodies are untouched. Also raised: [[A28]]. | fixed v2.1.325-beta |

| A4 | **GM starmap labels grow with zoom (system names, depth cues, placement ghost).** System-name font should stay a constant screen size as you zoom, not scale with the map transform. `Starmap.svelte` — the labels are inside the world transform, so they scale with it. (Compare: the 3D starmap uses sprites with an explicit `setLabelSize`, which is why it does not have this.) Each label font size and its offset from the marker now divide by the zoom, cancelling the one inherited factor. **Not covered — see A8:** route labels, the measure read-out and the multi-star `+` indicator still scale. | fixed v2.1.295-beta |

| A5 | **Stat block pairs two different temperatures in adjacent rows.** Pandora: "Surface temp 45 °C" against "Temp range -28 °C to -23 °C" — the mean sits 70 °C outside its own range. 45 °C is 318 K, the range is 245–250 K, and the ~71 K gap is the GREENHOUSE: the block shows the composed surface temperature on one row and the EQUILIBRIUM min/max on the next. A proper surface range already exists — `SurfaceTempProfile.totalMinK` / `.totalMaxK`, which is what the GM body panel decomposes by cause. Use that. Check every other paired figure in the same block while there. **FIXED v2.1.323-beta, exactly as scoped — and the SWEEP found a second row of the same class, which is the more valuable half.** Root cause confirmed at `bodyFacts.ts:92-93`: one row printed `temperatureK` (the composed SURFACE temperature) and the next printed `equilibriumTempMinK/MaxK`, which omit the greenhouse and every other heat term. Now `temperatureRangeK`, which `surfaceTempProfile` builds as `mean ± rss(swings)` and therefore brackets `temperatureK` **by construction**, not by luck. **Verified numerically over the whole bundled Sol map rather than on the reported body: 34 of 34 means sit inside the surface range; 11 of 34 sat outside the equilibrium range that was being shown** — Venus (754 K against "229–231"), Earth (287 against "252–256"), all four giants, Io, Europa, Titan, Mimas. **Degradation needed no fallback and this was checked rather than assumed:** `processEnvironment` returns early for stars (`SystemProcessor.ts:356`) so a star has NEITHER field, and `importFixup`'s `DERIVED_FIELDS` strips and re-derives both together — so there is no state where one exists without the other, and the row simply drops. Confirmed live: Sol prints "Surface temp 5,778 K" and no range row. **THE SWEEP, item by item, so the next session need not redo it.** (a) **"Surface liquid" was the second bug, same class, and is fixed in the same commit.** The row printed raw `hydrosphere.coverage` and hard-coded the word "liquid", while the PHASE is separately derived and published as a `hydrosphere/*` tag — so the block contradicted its own Tags row two lines below. Europa read "Surface liquid 100% water" beside "Frozen surface: water", and Europa's liquid is famously UNDER the ice; **five of Sol's seven hydrosphere bodies are frozen** (Europa, Ganymede, Callisto, Enceladus, Triton). The label now follows the tag — surface liquid / surface ice / surface volatile, marked boiled off or supercritical — reading a published derived value, so no physics moved. (b) **Radiation band vs range: a real but INVISIBLE inconsistency, recorded as B8, not fixed.** The mean is composed from the per-star spectral components while the endpoints are rebuilt from a flat photon/particle fraction times the range total, so the mean can land marginally outside its own range — measured on Sol, 3 of 34 bodies (Neptune, Triton, Neptune's Rings), worst excess ~0.4%, which rounds away at the block's 1 d.p. Physics, and out of scope here. (c) **Day length vs "tidally locked" is the same class and is already B7** — an authored spin against a derived lock. Cross-referenced there. (d) **Type vs Surface temp is the same class and is already B6** — the classifier keys on `Teq_K` for 26 fingerprints. (e) **Ruled out, with the reason:** Orbit distance / Orbital period (the period is derived FROM `a_AU`, or for a binary member from the `n_rad_per_s` both members share, which comes from the same separation); Mass / Radius / Gravity / Density / Ascent Δv (one source pair, `massKg` + `radiusKm`); Atmosphere pressure / Air mix (one `body.atmosphere`, and atmospheric escape rewrites both together); Habitability (reads `planet.temperatureK`, the surface figure, and scores its solvent off the phase-aware fluid-layer model, so it agrees with both corrected rows). **Blast radius, and it is wider than the batch brief assumed: `bodyFacts` has FOUR consumers, not two** — the shared `buildGuideDocument` (so `DocPanel` AND `FilteredDocumentView`), the legacy `CatalogueBrowser` Field Guide, the holo HUD info card (`catalogue/+page.svelte:611`) and a direct fact list (`:893`). One line reaches all of them. **Verified on both document consumers, per F7/F9's lesson:** `FilteredDocumentView` (Venus 480 °C / 463–499 °C; Jupiter −114 / −129 to −98; Europa "Surface ice 100% water") and `DocPanel` (Europa identical on all three rows). **One judgement call worth knowing:** on a tidally-heated body the surface range is WIDE, because the profile's upper bound is the tidal-hotspot term — Pandora now reads 13 °C to 1,197 °C and Europa −250 °C to 401 °C. That is honest, it is inside the physics, and it is character-for-character what the GM's own body panel already prints, which is the point of a shared figure. | fixed v2.1.323-beta |

| A6 | **In the DOCUMENT view of a system, the star's page should list its natural satellites — the planets — exactly as a planet lists its moons.** It currently does not, so the only way down into the system from the star is the picker at the top, and that is sometimes ineffective. Every other body in the document gets a satellite list; the star is the one node missing it, and it is the node you land on first (E1 preselects the primary star on entry), so the omission is on the FIRST page a player sees. Treat it as the same list the planets already render — `buildGuideDocument` / `systemTopology` — not a new bespoke block. Fixing this also gives the document a working navigation path that does not depend on the picker at all. **FIXED v2.1.324-beta.** **Root cause: the helper existed and the drill-in section simply never called it.** `guideDocument.ts` built its navigator from `membersOf` / `moonsOf` / `constructsOf` only; `listBodiesOf` — the direct-orbiters-in-orbit-order query the BodyPicker and the legacy `CatalogueBrowser` chip row both already use — was never imported. So this was a missing call, not missing topology. Same block shape as the moons, same `drillItems`, and using `listBodiesOf` rather than `planetsOf` deliberately, so BELTS are listed too instead of being pickable only by tapping the chart. Heading is chosen by content: `Planets`, `Belts`, or `Planets & belts`. **Keyed on `selected.id`, NOT `subject.id`**, because `subject` has already been resolved from a barycentre to its dominant member, and a circumbinary planet hangs off the barycentre. **The de-duplication filter is load-bearing twice, and both cases are real rather than defensive:** (1) `orbiters()` excludes only moons, and **Pluto is `roleHint: 'planet'` parented to the Pluto-Charon barycentre**, so that page would have offered Pluto as its own satellite; (2) `membersOf` matches on `n.parentId === bary.id` as well as `memberIds`, so a circumbinary planet is ALREADY claimed as a barycentre "member" and would have been listed twice. **Verified numerically over the whole campaign, not on the reported body: all 60 stars in 42 systems, every list length compared against the ground truth read straight from the data — 33 stars list 92 bodies between them, 0 mismatches, and the 27 stars with no orbiters correctly render no block.** Also swept all 210 body pages for a page offering ITSELF as a drill target: none. **Verified live** on `FilteredDocumentView`: Sol lists Mercury→Kuiper Belt in orbit order with belts carrying the `⋯` glyph, and Sol → Jupiter → Europa drills all the way down without touching the picker. In the Alpha Centauri triple: Rigil Kentaurus → Polyphemus, Proxima → its two planets, Toliman correctly empty. **Honestly NOT verified on live data: the circumbinary (P-type) path, because the bundled campaign contains no such body** — 20 barycentres, none with a non-member planet child. Proved on a synthetic fixture instead, where both circumbinary planets appear exactly once. **Three things found while doing it, all recorded rather than fixed:** duplicate node IDs in two bundled systems ([[D3]]), the "Moons" heading being used for companion STARS on a barycentre page ([[A25]]), and a ringed planet's ring appearing in no drill list at all ([[A26]]). | fixed v2.1.324-beta |

## F — Player Views: preset editor (one batch, 2026-07-30)

All from a single pass over the preset editor. Several may share a root — F4/F5 are both "the preview
re-frames when it should not", F2/F3 are both "true-scale mode breaks proportion".

| # | Observation | Status |
|---|---|---|
| F1 | **Body graphics were being drawn on the SYSTEM MAP itself. They must NEVER be.** Body graphics (photo / disc / sphere) are for the INFO BLOCK only; the map always uses the 2D or 3D render. This was stated once before during the D5/E2 work and fixed for the 3D list — so this is either a regression or the fix never covered 2D. **Highest priority in this bucket: it is a rule, not a preference.** Cause of the recurrence: the earlier fix (f947826) forced sphere for `holo3d` only and deliberately LEFT the control on the 2D map, because a flat body sprite was then considered a legitimate 2D-map look. Fixed at the source instead — `bodyGfx` is gone from `HoloStyle` and from the scene entirely (type, setter, render branch, `buildBodyDisc`), so no map style can carry one. | fixed v2.1.286-beta |
| F2 | At TRUE scale, the "glow" points are far too large and do not stay in proportion to the orbit lines — an oversaturated field of giant blobs. One root with F3: everything the scene draws at a FIXED size (minimum body radius, wireframe glow vertices, belt rubble, ring particles) was sized for the READABLE end of the dial and then applied at every setting. `markerScale()` now shrinks all of them with the dial, floored at 2% of readable. 2D and 3D are the same engine, so both. | fixed v2.1.288-beta |
| F3 | Same at true scale for RINGS and BELTS — they lose proportion when zoomed to actual sizes rather than readable sizes. The diagnosis was right: same root as F2, same fix. **Residual, see A9:** a body can be VISIBLE at true scale or in PROPORTION at true scale, not both — Earth's true radius is about 0.05 px at whole-system framing. The floor survives at 2%, so bodies are still over-sized against their orbits at the extreme end; the belt no longer buries them. | fixed v2.1.288-beta |
| F4 | Changing a general setting (e.g. belt type) re-zooms the preview to the star. Appearance settings should never move the camera. `setFraming` was the one setter in the scene that did not bail when handed the values it already held — it re-armed its 48-frame ease on every call, and `applyStyle` calls it for every style object, which the editor rebuilds on every keystroke. | fixed v2.1.290-beta |
| F5 | "Frame whole system (never zoom to a body)" did not show the whole system in the preview. The distance was a fixed `GRID_RADIUS * 1.5` with no reference to the lens: at fov 45 that fits a half-extent of about 7.5 of the 12 units a system occupies, so the outer third was always off the edge. Now the same fov/aspect maths `frameDistance` already used for a single body — then fitted to the BOUNDING SPHERE rather than a flat extent, so a tilted shot does not clip either (v2.1.298). | fixed v2.1.291-beta, v2.1.298-beta |
| F6 | 2D low-poly / wireframe render styles apply to the STAR but not to the bodies. Same root as F1: those styles only exist on the sphere path, and the 2D map's bodies were flat sprites. The star is never a sprite, which is why it alone obeyed. | fixed v2.1.286-beta |
| F7 | Body Graphics → photo: nothing appears in the preview until you leave the tab and come back. The loader was keyed on the selected body alone, and the line that clears the image for every non-photo mode stamped that same key — so switching TO photo without changing body looked, to the loader, like nothing had happened. Keyed on subject AND mode now, in `DocPanel` and `FilteredDocumentView` alike (both carried the same pair of lines). | fixed v2.1.292-beta |
| F8 | **VERIFIED, no re-unification needed.** One info-block component: the catalogue renders a single `inspectorAside` snippet for the holo AND the 2D-map tiers, containing exactly one `<DocPanel>`. `DocPanel` and `FilteredDocumentView` import the same builder (`buildGuideDocument`), the same theme resolver (`makeDocTheme`) and the same renderer (`renderDocument`). The settings DID reach both — but two arrived wrong, which is what the 2D-only testing hid: F7 and F9 were both found by this check, and both were faults in the SHARED layer rather than in one surface. One cosmetic gap left, not worth changing: `DocPanel` does not pass `navStyle` to `makeDocTheme`, harmless because panel mode builds no navigator block. | verified v2.1.296-beta |
| F9 | The selected font COLOUR should carry through to the headings in the info box, **including Rainbow**. Two faults in one line of `makeDocTheme`. (1) It flattened the `rainbow` sentinel to a hex before passing the theme on, and the rainbow-heading path in `renderDocument` tests for exactly that sentinel — so those headings had never rendered in ANY surface since v2.1.266, not merely failed to reach `DocPanel`. (2) The preset accent was not part of the colour seeding at all, so choosing a colour moved the chrome and left headings on the document style default. The accent now outranks the style seed for the accent + heading slots; an explicitly tweaked slot still outranks both. A follow-up found the live player view passing the pre-flattened accent to the engine while the editor passed the raw one — the exact preview-vs-live drift the shared resolver exists to prevent. | fixed v2.1.293-beta, v2.1.296-beta |
| F10 | The info-panel WIDTH slider should show its effect live as you drag, and should read as a percentage of the screen rather than a raw pixel count. Now a stored FRACTION of the viewport (`inspectorWidthPct`), converted to the pixels of whatever display it lands on when a preset is applied — a proportion travels from the GM's screen to a player's, a pixel count does not. The preview drew the raw pixel figure capped at 340, so the top half of the slider's travel moved nothing; it is a percentage of the stage now. | fixed v2.1.294-beta |
| F11 | **FEATURE, not a bug.** Pin the info panel to top / bottom / left / right to change the look and feel, with the view sliding to re-frame around it the way it already does when the panel appears. | captured, feature |

## G — Features (deliberately not bugs — do not hand these to a bug batch)

| # | Observation | Status |
|---|---|---|
| G2 | **Document navigation links flow side by side and wrap, and that is now the DEFAULT.** Requested directly 2026-08-01 while the A25/A26 work was being verified: "sometimes it is many rows — side by side buttons is the default". The drill-in lists drew one full-width row per item, so Sol's thirteen planets turned the end of a page into a column of links; A26 had just added a Rings row on top. **Built as a third `NavStyle`, `chips`**, alongside the existing `plain` and `boxed` — laid out inside `drawList` rather than as a new block kind, because it is the same list with a different arrangement, so every drill-in list (planets, moons, rings, companions, constructs, the parent row) got it at once without any of them knowing. **The one real risk was hit-testing**, since side-by-side chips share a y band where rows never did: each chip pushes an X-BOUNDED region, which the document's hit test already prefers when present (the schematic's boxes work the same way). Verified by clicking Jupiter out of thirteen chips on one line and landing on Jupiter. An over-long name ellipsises to a full-width chip rather than running off the edge — the one way a flowing layout can break where rows cannot. **Default changed to `chips` for new presets**, per the request; `boxed` and `plain` remain selectable and are relabelled "Buttons — one per row" and "Plain text". A preset already stored keeps whatever it has. **FOLLOW-UP v2.1.331: in rainbow mode each button carries the hue of that body's MARKER on the schematic above.** Asked for as "can each of those buttons be a different colour?", then refined to "make those colours match the planet colour on the diagram above". **Built by SHARING the index rather than re-deriving it:** `systemSchematic` now exports `rainbowHue` and `rainbowHueIndex`, and its own drawer consumes the exported builder instead of an inline copy — so there is one map, and a chip and its dot cannot drift apart by construction. The builder attaches `ListItem.color`; the renderer only paints what it is handed, which keeps the look in the data layer rather than in the drawer. **Off-chart items are handled honestly:** moons and constructs are not drawn on the schematic and have no index, so they fall back to their position in their own list — varied, but not pretending to match a marker that is not there. Monochrome ignores the colour entirely, the same exemption the rainbow headings take. **The `hsl()` colour forced one change:** `hexA` only parses `#rrggbb` and silently returns its input, which would have painted a fully opaque chip — the tint goes through `globalAlpha` instead, which works whatever the format. **Verified across BOTH bundled maps: 490 navigator buttons that point at a charted body, ZERO whose colour differs from `rainbowHue(rainbowHueIndex(system).get(id))` — the exact expression the schematic's `markerCol` paints the dot with.** A pixel check of the drawn markers agreed on 7 of 12 and was INCONCLUSIVE on the rest, which is worth recording rather than hiding: each disagreement sampled a NEIGHBOUR's hue (Earth read Venus's, Pluto and the Kuiper Belt read Neptune's) because the schematic's hit boxes overlap on a crowded row, so it is a limitation of the sampler and not a colour fault. The parent-nav row was found uncoloured by that sweep and fixed in the same pass. | shipped v2.1.330-331-beta |

| # | Observation | Status |
|---|---|---|
| G1 | **More ARRANGEMENTS for the starmap textual view — a form, pickable boxes, star/planet icons.** Requested 2026-07-31. Today `buildStarmapDocument` (`catalogue/document/starmapDocument.ts:30`) emits exactly ONE shape: heading, optional italic description, `rule`, and a single `list` block of one row per system with a "1 star · 8 planets · 12 moons" sub-line. **The engine is far more capable than that view uses** — `blocks.ts` defines twelve block kinds (`heading, text, keyValue, tags, list, image, bodyDisc, rule, spacer, columnStart, columnEnd, schematic`) and `ListStyle` alone already offers six (`plain, illustrated-bullets, numbered-dossier, terminal-log, ledger, manifest`). The starmap document uses four blocks, no list style and no `keyValue` at all. So most of this is arranging capability that already exists. **Four candidates, cheapest first.** (1) DOSSIER/FORM — per system a level-2 heading then a stack of `keyValue` rows (Primary / Companions / Planets / Moons / Depth / Distance) then a `rule`. Literally a form, ZERO new block kinds, and the most different from what is there. Build this one first. (2) STAR-GLYPH CATALOGUE — a row of real coloured discs per system, primary large, companions smaller, planets a trailing run. `bodyDisc` already draws "from the body's true colour" and `systemVisualStars` already returns the star list, so the colours are DERIVED. **Hard rule: do not add a class-to-colour lookup table for this** — that is precisely the renderer shortcut that was reverted before. Needs one small addition: a glyph ROW of several small discs inline, rather than the current one-disc-per-block. (3) PICKABLE BOXES — **note the trap:** `columnStart`/`columnEnd` is NOT a general grid, it is image-specific (a left image strip with text flowing right), so a true 2-3 column card grid needs new layout capability in the renderer and is a real job. The cheap 80% is a new `ListStyle: 'cards'` — each item a bordered full-width box that highlights when selected. One enum value, no layout engine. Ship that and see whether the grid is still wanted. (4) SCHEMATIC PER SYSTEM — `SchematicBlock` already exists and takes a whole `System`, and `systemSchematic.ts` already draws them; a small schematic per system would be the standout look and reuses machinery already paid for. Most work of the four. **THE DESIGN STEER THAT MATTERS MORE THAN ANY OF THEM:** make this ONE new `layout` option on `StarmapDocOpts` and the preset, composing WITH the existing `documentStyle`, `listStyle` and colouration — a matrix (layout x style), not four bespoke views. That is a lot of distinct looks for very little code and it preserves the one-builder-one-renderer property F8 verified and that caught F7 and F9. **Follow-on, not part of this:** the SYSTEM-level document (`guideDocument`) has the same single-shape limitation, so whatever lands here should extend to it later. **RAINBOW — an open design question per arrangement, to be answered while building rather than up front.** When the preset accent is `rainbow`, what does the rainbow colour here? Either each ROW or BOX takes a different part of the spectrum walking down the list — the system document already does something like this, giving each drill-in item a hue (`guideDocument.ts:64-71`, `rainbowHue()`) — or it applies to the LABELS and headings only, leaving rows in the document's normal colours. It may well differ by arrangement: a stack of cards can carry a hue each without shouting, where a dense form probably wants it on the field labels alone. Set the colour in the BUILDER, not the renderer (`blocks.ts:105`). **One landmine:** never flatten the `'rainbow'` sentinel before `renderDocument` sees it — doing so is what broke the Guide's rainbow headings from v2.1.266 until F9 found it (`documentStyles.ts:85-86`). Check every arrangement in Rainbow as well as the plain styles. | captured, feature |

## B — Physics engine (needs someone holding that context)

| # | Observation | Status |
|---|---|---|
| B1 | `albedo.ts` derives its OWN clouds (`CONDENSE_BOIL`, `teqK < boil * 1.6`) while `cloudDecks.ts` is documented as "THE single evaluation". They disagree on Adrian: albedo declares a CO2 deck, the column physics reports none. The crude model is UPSTREAM of everything. **Circular** — albedo → Teq → profile → decks → albedo. | fixed v2.1.282-beta |
| B2 | Greenhouse looks strong: Adrian +337 K from 8 bar CO2, vs Venus +505 K from 92 bar. Checked while fixing B1: **not downstream of it.** Adrian's greenhouse is +336.8 K before AND after — B1 moved the Teq underneath it (246 → 304 K), not the greenhouse term. The ratio is the model's own shape: forcing goes as `log(1 + sqrt(100·pp))`, then the response is `log` again, so partial pressure enters twice-logarithmically and saturates hard (Adrian pp 7.3 → 3.33, Venus pp 88.8 → 4.56, a ratio of 0.73 on 8.7% of the pressure). `broadening = min(1, sqrt(P))` is also pinned at 1 for anything over 1 bar, so it distinguishes nothing above that. Then CHECKED against every anchor the Solar System offers, not just Venus: Mars 0.006 bar +5.8 K (measured 6), Earth 1.013 bar +32.8 (33), Titan 1.45 bar +14.1 (12), Venus 92 bar +523.6 (505). It hits all four across four orders of magnitude of pressure. There is no measurement anywhere near 8 bar, so the apparent anomaly is an intuition with nothing to check it against, and re-shaping the curve to satisfy it would put the four anchors it does hit at risk. **Recommend no change** unless a real anchor turns up. | wont-fix — model matches every anchor there is |
| B9 | **A newly created system has rotation period 0 and magnetic field 0 — on stars especially, though it is not only stars.** Reported 2026-07-31: "I thought we generated magnetic field... magnetars do... perhaps we dropped that ball". **TWO SEPARATE FAULTS WEARING ONE COAT. Do not fix them as one.** **(a) The cheap one: a placeholder zero is a CLAIM, not an absence.** `core/BodyFactory.ts` initialises every manually created body with `axial_tilt_deg: 0`, `rotation_period_hours: 0` (`:20-21`) and `magneticField: { strengthGauss: 0 }` (`:36-38`), under the comment "Default Physical Properties (Avoid NaNs)". Nothing fills them in afterwards. This makes the two reported symptoms ONE root, not two: `SystemProcessor.ts:775` overwrites `magneticField` from `deriveMagnetism`, and that model derives strength from ROTATION plus composition plus core size — so a zero rotation produces a zero field by construction. It hits manually created PLANETS as well as stars. Whether the fix is to leave the fields absent (letting a derivation see "unknown" rather than "zero") or to derive a plausible primordial spin at creation is the design call; note D2a already argued rotation is the obvious next inference, and that "inferred must be distinguishable from measured" or the map starts asserting invented facts. **(b) The real one: there is no stellar rotation or magnetism model at all.** A procedurally generated star gets a field only if its rule-pack template happens to carry `mag_gauss` (`generation/star.ts:32-33`) — otherwise `starMagneticField` stays undefined — and nothing anywhere derives a star's rotation. `deriveMagnetism` is a PLANETARY DYNAMO model (rotation + core + composition); pointing it at a star would be wrong physics, not a shortcut. Magnetars are precisely the case that exposes the gap: their 1e14-1e15 gauss comes from flux and angular-momentum conservation through collapse, i.e. it is derivable from mass and end state rather than authored. **This belongs with the banked HR-diagram / stellar-generation phase** ([[project_sse_v2_hr_stellar]]), which already planned to put stellar age, lifespan and TIME into the stellar model — rotation is the same family, since a star spins down with age and its field follows. Do NOT build a bespoke stellar field formula in isolation ahead of that work. Related: [[D2]], [[D2a]], [[B7]]. **FIXED (a) v2.1.332-beta; (b) DELIBERATELY NOT BUILT — see the last paragraph.** **Root cause of (a) confirmed, and one HALF of the triage above is wrong, which matters because it is the half that looked like the whole bug.** The placeholder zeros are real: `BodyFactory` stamps `axial_tilt_deg: 0`, `rotation_period_hours: 0` and `magneticField: { strengthGauss: 0 }` on every body it makes. But **"a zero rotation produces a zero field by construction" is NOT true** — `rotationFactor()` (`magnetism.ts:45-49`) has an explicit `if (!h) return 0.6; // unknown -> middling` branch, and 0 is falsy, so it takes it. Reproduced: a manually-created Earth-mass planet processes to **0.3 G**, not zero. **And `deriveMagnetism` never runs on a star at all** — it lives in `processClassification`, which returns at `:539` for anything that is not a planet or a moon. So the reported symptom had a different cause entirely. **The real cause of the zero FIELD: there are two star-creation paths and only one of them read the pack.** `generation/star.ts` drew `strengthGauss` from the class's `mag_gauss` band; `generateFromConfig.ts`'s `starSeedToBody` — **the path the generation wizard uses**, which is exactly "create a new system" — set nothing, so BodyFactory's zero survived to the screen. Both now go through one `starFieldFromPack`, and it looks up the EVOLVED class, so a 13 Gyr G-star draws the white-dwarf band. Measured: wizard stars went 0 G to **7.86 G (G, band 1-10), 433 G (M, band 100-1000), 4.6e5 G (WD, band 1e4-1e6)**. It takes its own rng stream keyed on the star id so no existing seed's planets move — verified byte-identical output for three legacy-random seeds. **This is NOT (b), and the distinction is the point.** The pack already carries every class from `star/BH` at [0,0] to `star/magnetar` at [1e11, 1e15], so the magnetar case the report asked about is answered by DATA, which is where the architecture rule says it belongs. Reading a band is not a dynamo model. **The zeros themselves are gone.** All three fields are simply absent now; every reader already guarded with `?? 0` / `|| 0`, `planet.ts:537` already set `magneticField = undefined`, and `generateFromConfig.ts:52` already worked around the tilt placeholder with a falsy test and a comment about it. The star editor shows rotation and tilt EMPTY with a "not set" note rather than a confident 0, and clearing either box deletes the field instead of writing zero — otherwise the honest state is unreachable once you leave it. **The derived diff is EMPTY, and that is correct rather than suspicious:** the Sol baseline is loaded from JSON and never passes through `BodyFactory`. Verified on generated systems instead. **(b) THE STELLAR ROTATION/MAGNETISM MODEL IS NOT BUILT, deliberately, and stays open.** Nothing derives a star's spin; `deriveMagnetism` is a planetary dynamo and pointing it at a star would be wrong physics rather than a shortcut. Scoped for the HR / stellar-generation phase, where age already belongs: a star spins down with age (Skumanich, roughly t^-1/2) and its field follows, so ROTATION is the derived quantity and the field falls out of it — which is also what makes a magnetar derivable (flux and angular-momentum conservation through collapse) rather than authored. Until then a star's field is a pack band and its rotation is honestly absent. **Two things found while doing this, recorded separately: [[B10]] and [[B11]].** | fixed v2.1.332-beta (a) — (b) scoped, NOT built |
| G3 | **FEATURE, DESIGN INVESTIGATION ONLY: ship appearance — lightweight 3D models for constructs.** Raised 2026-08-01 as a future requirement, explicitly an idle thought to be investigated rather than built. The want: a construct should be able to carry a simple 3D model the way a body can carry a photo, as an ALTERNATIVE OR ADDITIONAL upload beside the ship image — "like planets they can have photos or 3D viz". Lots of PRE-EXISTING models rather than bespoke art, kept lightweight, with procedurally applied texture/colour options so a handful of models covers many ships. STL was floated on the grounds that 3D-printing models are abundant; that wants testing rather than assuming, since printing meshes carry no materials or UVs and tend to be very high-poly. **This ANSWERS A30, which is already open and deliberately unanswered** — A30 removed the wrong picture (a ship illustrated with a planet's sphere) and did not choose a right one, so a construct currently has a blank where every body has a graphic. Any outcome here should close A30 or explicitly leave it to a cheaper interim. **Three things already established, so the investigation does not have to find them.** (1) The upload path EXISTS: `image?: ImageRef` on the node type (`types.ts:308`), with `ImageRef.custom` meaning a GM-uploaded picture the processor will not overwrite. (2) three.js is ALREADY a dependency and `three/examples/jsm/*` is already imported (`holo/filteredCanvas.ts`, `holo/galleryScene.ts`), so a `GLTFLoader` is an import rather than a new dependency. (3) **The load-bearing assumption a model BREAKS:** the scene treats a construct as "a fixed-screen-size glyph with no rendered globe, so it contributes no radius" (`holo/scene.ts:1722`). Real geometry has extent, which touches framing, the A9 true-scale floor and the bounding sphere F5 frames the whole system by. **The design tension to address head-on rather than trip over:** body graphics (photo/disc/sphere) are INFO-BLOCK ONLY and must never appear on the system map — a rule that has come back twice and was closed at source in v2.1.286. Whether a 3D ship model on the 3D map is a violation of that rule or its opposite (the honest render of a thing that really is that shape) is a genuine question, and the answer decides whether this is an info-block feature or a scene feature. Do not assume either. Related: [[A30]], [[F1]]. **INVESTIGATED 2026-08-01 — full design at `docs/dev/ship-appearance-design.md`; read that before building any of it. THE RECOMMENDATION IN FOUR SENTENCES.** (1) **Format: GLB, meshopt- or Draco-compressed; STL and OBJ are accepted at upload and CONVERTED, never stored or bundled** — measured on identical ~30k-triangle geometry, Draco GLB is 111 KB where binary STL is 1,461 KB with no materials/UVs/units, and printing STLs average ~126k triangles across NASA's own repo; every loader and decoder needed already ships inside the existing `three@0.169` dependency, so zero new packages. (2) **Sourcing: bundled models must be CC0/public domain and enough exist** — NASA 3D Resources (public domain, 257 GLBs, real craft mostly under 500 KB; a measured spectrum from fully textured PBR (Hubble, InSight) to per-part flat-colour materials (Voyager, Juno) — none of it bare geometry, and textured craft fit the budget after a 512 px texture downscale: Hubble 1,655→128 KB), Kenney Space Kit (CC0, 150 models) and Quaternius Ultimate Space Kit (CC0, 92) are verified; which hull a ship uses is EXPLICITLY ASSIGNED, never inferred from the free-form class string. (3) **The map-versus-info-block question: BOTH, DIFFERENTLY, and the scene half is NOT an F1 violation** — the info block gets a lit turntable viewer (the `bodyDisc` sphere is the exact precedent), and the 3D scene may draw the model ONLY AS THE MARKER (sprite slot, same focus rule, glyph below a legibility threshold) because unlike a pasted photo the model IS the honest render of the hull's shape; the 2D plan view and starmap keep the glyph, a GM photo stays info-block-only, and the model must keep contributing ZERO radius to ring clearance and F5 framing. (4) **The binding constraint is the broadcast, not storage** — `sendIfChanged` re-stringifies and re-sends the whole snapshot on any change, so custom models must live in a hash-addressed side store sent once per session, not as inline data URLs. Dressing (tint from `icon_color`, decal from `icon_type`, status from tags once constructs have them) keeps the tags-drive-the-image rule; five owner decisions are flagged at the end of the doc, the first being whether the scene marker (Phase 2, severable) is wanted at all. **STEER 2026-08-01: the primary use case is USER-UPLOADED SF ships; NASA/bundled is rarely used.** Phasing reordered accordingly — Phase 1 is the full import pipeline (GLB, STL and OBJ with convert-at-import, `meshopt_simplifier` for high-poly printing STLs, orientation/up-axis preview, `icon_color` tint for material-less meshes), and the bundled starter set drops to optional Phase 4. The `meshoptimizer` dependency (MIT) moves from flagged-optional to recommended-accept: without it the front door rejects most fan models. | design done 2026-08-01 — awaiting owner decisions, not started |
| B7 | **A derived tidal lock does not reconcile an authored rotation period, so the block contradicts itself.** Pandora is shown "Rotation: tidally locked" AND "Day length 41.8 h" on a 3-day orbit. `rotation_period_hours: 41.8` is AUTHORED in the map; `tidallyLocked` is absent from the file and DERIVED at load — correctly, a moon at 0.0038 AU from a gas giant despins fast. The two are never reconciled. For a locked body the sidereal day EQUALS the orbital period (here 72 h), and the solar day with respect to its primary is infinite. Decide which wins and make it one number: physics-drives says the derived lock should set the rotation period (and say it did), the alternative is that an authored rotation suppresses the lock — but showing both is not an option. Applies to every locked body with an authored spin, not just Pandora. **Re-confirmed from the display side by the A5 sweep (v2.1.323):** these two rows are the same class of fault as A5 — two figures that look like one quantity, arriving from different stages — but unlike A5 the display cannot pick a winner honestly, because both values are genuinely present on the body and only physics knows which is real. Left here deliberately rather than papered over in `bodyFacts`. Shares its shape with [[A5]] and [[B6]]. **FIXED v2.1.333-beta.** **Root cause: two stages answering one question, and the temperature model had already picked a side.** `surfaceTemperature.ts:83` reads `effRotHours = tidallyLocked ? (orbitalPeriodHours || ...) : rotationHours` — so the thermal model has ALWAYS used the orbital period for a locked body and ignored the stored spin. The stat block, `rotationFactor()` in the dynamo and `rotationalDeform()` all read the stored number. The lock now sets it, which unifies them rather than adding a third opinion. **MERCURY IS THE ANCHOR AND IT NEARLY BROKE.** A blanket 1:1 rule would have overwritten Mercury's **measured 1407.6 h day** with its 2110.9 h year. Mercury is despun but NOT synchronous: at e = 0.206 it is captured in a 3:2 spin-orbit resonance (Goldreich & Peale 1966). So `lockedSpin()` keeps a spin sitting on a half-integer ratio of the orbit when the orbit is eccentric enough to hold it (e >= 0.1, ratio >= 1.5, within 2% of p:2, order <= 4:1), publishes `orbit/spin-orbit-resonance` = "3:2", and the stat block prints the resonance instead of "tidally locked". Everything else takes its orbital period. Pinned in the baseline as a value that must NOT move. **What the thresholds RULE OUT, checked body by body rather than assumed:** Chazon (ratio 1.523, within 1.5% of 3:2) is excluded by e = 0.029 — too circular to hold it; Conquestor (ratio 0.508, e = 0.126) by the sub-synchronous floor, since despinning passes THROUGH 1:2 toward 1:1 and does not stop there; Fames (e = 0.29), Adonis, Bellum and Fuller by not being near any half-integer. Across all 561 bundled bodies the exception fires on exactly one body — Mercury, four times, because Sol appears in four maps. **The numbers, measured against a worktree at the parent commit.** 23 locked bodies had a spin contradicting their own orbit; **23 -> 4, and all 4 are Mercury**. 117 locked bodies had NO spin at all and now have one. In Sol 20 rotation periods moved and **every one by under 1.2%** — their authored day already WAS their orbital period, so the shift is only our Kepler solution against the real one: Pluto/Charon 1.12%, Deimos/Ariel 0.72%, Luna 655.7 -> 659.03 h (0.51%), Io 0.02%. Pluto's retrograde sign survives. In the fictional maps the moves are large because the numbers were flavour: **Pandora 41.8 -> 74.76 h** (the reported body), LV-426 50 -> 80.6, Adonis 15 -> 2630.8, Bellum 9.05 -> 2152.3. **No temperature, no habitability and NO CLASSIFICATION moved anywhere** — expected, since the thermal model was already using the orbital period. **The one consequence worth arguing about, stated plainly: 53 bodies lost `magnetic/dynamo`** (41 to unshielded, 12 to tenuous), including Proxima b, Ross 128 b, Luyten b and the outer TRAPPIST-1 worlds. **40 of them had no rotation at all**, so `rotationFactor`'s "unknown -> 0.6" default had been handing every rotation-less planet an Earth-like field — a magnetosphere derived from not knowing, the same fault class as [[B9]]. Given their real 9-49 day spin the model says what it has always said about a slow rotator. **The threshold was NOT retuned to soften this** — see [[B12]]: it has no anchor between Venus and Earth and is now load-bearing for ~40 bundled planets. One body also lost `shape/oblate`, correctly: a world forced to a 200 h day is not flattened. Also found while measuring: [[B13]]. | fixed v2.1.333-beta |
| B5 | Mars's surface albedo is 0.154 from rock + metal makeup alone; the measured Bond albedo is 0.25, because Mars is bright with ferric dust and polar caps. Exposed by B1: the bogus CO2 deck had been carrying Mars to 0.236, right for the wrong reason. Costs Mars its real 210 ppm water-ice wisp (it survives to Teq ≈ 214.5 K; Mars now sits at 216.7). Same class: Io 0.153 vs measured 0.63 (SO2 frost), Luna 0.15 vs 0.11, Mercury 0.169 vs 0.088. `deriveOxidation()` already grades the rust but reads `geoActivity.surfaceAgeGyr`, derived AFTER the thermal solve — so this needs the geology derivation moved too, or it reintroduces the one-pass lag v2.1.282 removed. **Plan when it gets a session:** it is a surface-albedo model, not a Mars patch — the measurements point three different ways at once. Bare rock wants to be DARKER than the current flat 0.15 (Mercury 0.088, Luna 0.11 — the model is too bright for both), while bodies with surface DEPOSITS want to be brighter (Mars 0.25 from ferric dust, Io 0.63 from SO2 frost). So: lower the bare-rock constant, then add brightening from the deposits the engine already derives — `surface/oxidised` for rust, frost/cap coverage for the rest. The blocker is ordering: everything that grades those deposits (`deriveOxidation`, `deriveGeoActivity`) runs after the thermal solve, and `deriveGeoActivity` itself takes `teqK`, so it cannot simply move — either the non-thermal part of geology splits out ahead of the solve, or surface deposits join the fixed point. Expect real classification movement; needs the same before/after diff B1 used. **A CONCRETE, QUANTIFIED ACCEPTANCE TEST, added 2026-08-01 after "Mars once had CO2 wisps of cloud and they are not visible now".** Confirmed in `tests/output/solar-system-derived.json`: Mars carries `teqK = 216.7116` and **no `structure/cloud-deck` tag at all**, while Venus (sulfuric-acid veil), Earth (water overcast) and Titan (methane overcast) each carry one. The threshold recorded above is Teq 214.5 K, so **the deck is missing by 2.2 K** — and the missing deck is the WATER-ice wisp at 210 ppm (`H2O: 0.00021` in the campaign data), not a CO2 one; Mars has real CO2 clouds but they were never what the engine modelled. Since `Teq` goes as `(1 - A)^(1/4)`, the arithmetic is direct: at the model's A = 0.154 Mars sits at 216.7 K, and at the measured Bond albedo A = 0.25 it would sit at **210.2 K**, i.e. 4.3 K BELOW the survival threshold, so the deck returns with margin rather than marginally. **That is the acceptance test for this item: fix the albedo model and Mars regains its wisp WITHOUT a single line of cloud code or any Mars-specific branch.** If the wisp does not come back, the albedo change has not moved far enough; if it comes back on a body that should not have one, the deposit brightening is too generous. **Note the history, because it explains why this looked like a regression:** the wisp used to appear, propped up by the bogus CO2 deck B1 deleted, which had been carrying Mars to A = 0.236 — very close to the real 0.25, but right for the wrong reason. Nothing broke; the fault was always there and B1 removed what was concealing it. **NOT BUILT. STOPPED AT THE HARD GATE, deliberately, with the evidence below. B5 STAYS OPEN.** **The clean split does not exist, and the triage above undercounts the problem by three.** It says `deriveGeoActivity` "itself takes `teqK`, so it cannot simply move". True, but `teqK` is one of **four** inputs that are downstream of the thermal solve, and it is the least of them: 1. `teqK` — direct, but used in exactly ONE branch (`geoActivity.ts:141`, the Triton solar-seasonal geyser, `teqK < 60`). On its own this one WOULD be carvable. 2. `hasSurfaceWater` — from `deriveFluidLayers` at `SystemProcessor.ts:657`, pass THREE. The solve is at `:416-433`, pass two. 3. `hasSubsurfaceOcean` — same `fluidLayers` call, same pass. 4. `icyShell` at `:684` — from `surfacePhase` and `makeupIceIsSolid`, and **[[B4]] added that temperature gate deliberately at v2.1.283** ("ice-rich interior and icy shell are different claims"). So carving out the single `teqK` branch would not free geology: the water-PHASE inputs would have to come with it, and those are the thermal solve's downstream products by design. Undoing that to make albedo work would undo B4. **What "deposits join the fixed point" would actually mean, since that is the remaining route.** Every iteration of `solveThermalState` would have to run fluid layers -> icy shell -> subsurface ocean -> `deriveGeoActivity` -> `deriveSurfaceAgeGyr` -> `deriveOxidation` -> surface albedo. The solve is deliberately NON-MUTATING — evaluated against a probe so the answer never depends on what a previous `process()` left on the body — and all seven of those derivations mutate the body today. That is a rewrite of most of pass 3, not an ordering tweak, and getting it wrong reintroduces exactly the one-pass lag v2.1.282 removed. Which is not hypothetical: [[B13]] is that lag, alive right now, in surface radiation. **THE OBVIOUS SHORTCUT IS THE TRAP, and it is worth writing down before someone reaches for it.** "Do the frost half and leave the rust" looks free — frost and cap coverage need no geology. It is the single most dangerous version: **a BRIGHT condensate makes the solve BISTABLE**, and frost is the canonical case (ice-albedo feedback is genuinely bistable in nature — that is what snowball Earth IS). The rule from the albedo session applies at full strength. **And the bare-rock half ALONE would make Mars worse.** Current anchors, read from the derived output: Mercury 0.169 (measured 0.088), Luna 0.150 (0.11), **Mars 0.154 (0.25)**, Io 0.153 (0.63). Lowering the bare-rock constant toward Luna and Mercury takes Mars to about 0.11 — further from its measurement, warmer, with knock-on into its cloud decks and every rocky body in every map. Fixing two anchors by breaking a third is what [[B2]]'s rule exists to prevent. The two halves have to land together, exactly as the plan says. **Two things established that make the next attempt cheaper.** (a) **Mars ALREADY carries `surface/oxidised=moderate`** — the rust signal exists and is graded; nothing needs deriving, it is purely a question of when. Venus, TRAPPIST-1 c/d, Adrian, Chazon and 11 others carry it too: **17 bodies of 561**, and `climate/polar-ice` covers 10. Io carries no frost tag at all, so its 0.63 needs a NEW SO2-frost derivation rather than a wire-up — the Io case is not "read the tag". (b) **`SURF_ALBEDO = { metal: 0.18, rock: 0.15, carbon: 0.05, ice: 0.62, gas: 0.30 }` is hardcoded at `albedo.ts:22`**, and B5's own plan requires those in rule-pack DATA per the architecture rule. **RECOMMENDED FIRST STEP for whoever picks this up, and it is safe on its own:** migrate `SURF_ALBEDO` and `OCEAN_ALBEDO` into the rule pack as a pure no-op (same numbers, read from data, empty derived diff). That is independent of the ordering problem, satisfies the constants-in-data rule, and leaves B5 with only the ordering question. NOT done here because the session that builds the model should choose the schema — it will need per-deposit brightening coefficients too, not just the three base numbers, and guessing that shape now means redoing it. **Also weigh the sequencing:** the whole rust half moves 17 bodies, and the price is restructuring the processor's central solve. [[B13]] is the same ordering fault in a cheaper place and is probably the right thing to fix first — it would establish the pattern for moving a derivation across the solve boundary, with an idempotence test, on a subsystem where getting it wrong costs far less. | NOT BUILT — stopped at the gate, evidence below, STILL OPEN |
| B3 | Classification keys on EQUILIBRIUM temperature, not surface — a 309 °C runaway greenhouse can be labelled "cold eyeball". The eyeball family is fixed: all three fingerprints describe the GROUND ("molten/dry dayside", "icy except the substellar point", "temperate oasis") and now match `SurfaceTemp_K`. Pinned by a test; no bundled body changed, because every star-locked world in the shipped maps is airless and its two temperatures agree. | fixed v2.1.283-beta |
| B6 | The REST of B3. 26 more fingerprints key on `Teq_K`, and they split cleanly in two. Types whose note describes the surface should follow the eyeballs to `SurfaceTemp_K`: desert, ice ("Frozen surface"), lava ("Molten surface"), ocean, methane, ammonia-planet, hycean, subsurface-ocean, earth-analogue, earth-like, superhabitable, forest, jungle, swamp. Types about the RADIATION ENVIRONMENT or a giant's cloud-top chemistry are right as they are: hot-jupiter, ultra-hot-jupiter, hot-neptune, ultra-hot-neptune, chthonian, ice-giant, ultra-cool-dwarf, and all five gas-giant cloud classes. Evidence the bands were authored as surface temperatures in the first place: earth-analogue wants Teq 255–300 and Earth's Teq is 254.1, just outside — its SURFACE is 287, mid-band. Not done here because it is ~14 fingerprints and would move real classifications; wants its own pass with a before/after diff. **Noted by the A5 sweep (v2.1.323) as VISIBLE, not just internal:** the stat block prints Type and Surface temp two rows apart, so a fingerprint keyed on `Teq_K` puts a contradiction on the page — the same class of fault as A5, and the reason this is worth more than a classifier tidy-up. **FIXED v2.1.335-beta, exactly as scoped — 14 moved, 12 left, no argument with the split.** **FIVE bodies change class across all 561 in every bundled system, and all five are in `Testion-System.json`, the one-body-per-class fixture. Judged individually, as asked:** - **ammonia planet -> desert. BETTER.** Teq 187 K but SURFACE 346 K. Ammonia boils at ~250 K at its authored 1.5 bar, so the body had no ammonia hydrosphere to be named after. Note its fingerprint even carries a `range` of Teq 120-220 which it satisfied — the surface was 126 K outside. - **earth like -> terrestrial. BETTER.** Surface 345.8 K, i.e. 73 °C, labelled "Habitable Earth-like world". Its authored air is 1% CO2, 25x Earth's, which is where the +92 K comes from — the body really is a hothouse and was only ever earth-like by reading the wrong temperature. - **superhabitable -> super-earth. BETTER.** Surface 371.4 K, 98 °C, labelled "even better for life than Earth", with 80% water coverage that would be at boiling. Correctly drops `science/rare-world-type` with it. - **swamp -> jungle. NEUTRAL, and say so rather than claim a win.** Surface 298.1 K is inside BOTH bands (swamp 278-300, jungle 290-318), both weigh 1.5, and the margin is 0.04 points. A tie-break flip, not an accuracy change. - **coreless -> desert. LATERAL, arguably a small loss, and the cause is NOT the temperature key.** A dry 268 K rock: "desert" is true of its surface and "coreless" is true of its interior, but both are `kind: base` and bases are mutually exclusive, so only one survives. B6 merely tipped a scoring contest that base-exclusivity had already set up. **Why the blast radius is small, measured rather than assumed:** only **131 of 366** bundled planets/moons opt into `autoClassify` at all, and of those the ones with a real greenhouse mostly carry AUTHORED classes that the processor must not overwrite. 88 of 366 bodies have a greenhouse over 10 K and 59 over 50 K, but most of that population never reaches the classifier. **THE ITEM'S HEADLINE EVIDENCE IS TRUE BUT WAS NOT LOAD-BEARING, and the next person should know.** Earth's Teq 254.1 does sit just outside earth-analogue's 255-300 — but `bandFit` decays LINEARLY over a 15% relative margin instead of cutting off, so 254.1 scored 0.977 and Earth classified anyway. What actually changed is the strength of the fit: Earth's best-scoring type now scores a clean 1.0 on every band (1.8 -> 2.1). That is still the right evidence for the split, just not for the reason recorded. **ONE REGRESSION FOUND AND FIXED IN THE SAME COMMIT, caught by the existing tests.** `viableTypesAt` — the generator's "what could exist at this orbit" menu — read `fp.match['Teq_K']` only, and treats a type with no such band as having NO temperature constraint. So every moved type became viable everywhere and lava was offered in the Goldilocks zone. It reads either key now. **The same regression has been live for the three EYEBALL classes since v2.1.283, when [[B3]] moved them, with nothing covering it** — fixed here and now pinned. The menu picks a type for a body that does not exist yet and so cannot have a surface temperature; `GREENHOUSE_COLD_SLACK_K` already exists to bridge exactly that gap, which is independent evidence the bands were authored as surface bands all along. **Sol: no body changes class.** Seven classification EXPLANATIONS change, which is the score improving. Also raised: [[B14]], [[B15]]. | fixed v2.1.335-beta |
| B4 | The ice-shell rule (ices float → visible crust) has no temperature gate; it will paint an ice crust on a 582 K world. The makeup branch (`mk.ice > 0.3`) now also requires water to be SOLID at the surface T and P. Ice-rich interior and icy shell are different claims. Three bundled bodies lost a false `structure/icy-shell` (Lacaille 9352 c at 303 K, GJ 674 b at 488 K, GJ 876 d at 523 K); nothing else moved. | fixed v2.1.283-beta |
| B8 | **The surface-radiation mean can sit marginally OUTSIDE its own min/max, because the three figures are composed two different ways.** Found by the A5 sweep (v2.1.323) while checking every paired figure in the stat block for A5's fault; recorded rather than fixed, because it is physics and because it is currently invisible. `calculateSurfaceRadiation` (`physics/radiation.ts`) builds `surfaceRadiation` from the PER-STAR spectral components (`components.photon` / `components.particle`, each already shielded), while `fluxToDose` at `:198-202` rebuilds the endpoints from a single flat `rawPhotonFrac`/`rawParticleFrac` applied to `calculateTotalStellarRadiationRange`'s totals. The comment at `:194` says the endpoints are kept "consistent with the same component model used above" — they are consistent in SHAPE but not identical in composition, so the two can disagree in the last fraction of a percent. **Measured on the bundled Sol map: 3 of 34 bodies land outside** — Neptune (2.28 vs a 2.26–2.28 range), Triton (2.57 vs 2.54–2.56), Neptune's Rings (0.57 vs 0.54–0.56). **Worst excess ~0.4%, and the stat block prints the range at one decimal place, so nothing contradictory reaches a reader today** — which is exactly why it is here and not fixed: the display is honest, the model is very slightly not. **Why it is worth a line anyway:** the same fault at a larger amplitude is what A5 was, and a multi-star system (where the spectral mix genuinely differs between the near and far ends of an orbit) is where this would grow rather than stay at 0.4%. Whoever takes it should check a binary before deciding it is negligible. Related: [[A5]]. **FIXED v2.1.334-beta — and BOTH halves of the triage above were wrong, which is the finding.** **The recorded cause is impossible.** The mean is `(photonFlux + particleFlux) * DOSE + bg`, and expanding it gives **exactly** `fluxToDose(components.total)` — the shielded mix cancels algebraically, because `rawPhotonFrac` is defined as `components.photon / total`. `fluxToDose` is linear and monotonic in flux. So the spectral composition CANNOT push the mean outside its own endpoints; the excursion had to be in the flux, one level down. **Real root cause: a missing TERM, not a rounding difference.** `calculateStellarRadiationComponents` adds an episodic flare dose to the particle channel (`star.flareActivity * FLARE_PARTICLE_WEIGHT`); `calculateTotalStellarRadiationRange` was a SECOND sum of the same quantity with no flare term in it at all. The mean therefore carried a dose the endpoints did not, by exactly `(1 + flare/2)` — which is why the excess is systematically ABOVE the maximum and never below the minimum. **The measurement, over all 420 ranged bodies in every bundled system rather than Sol alone: 105 land outside, not 3, and the worst is 19.75% of the mean, not 0.4%.** Wolf 359's Graveyard reads 4.46 against a 3.30-3.58 range — Wolf 359 being a famously active flare star, which is the entire mechanism visible in one row. Epsilon Eridani's belts 14.5%, Lacaille 9352 e 5.9%. Sol was simply the benign case: a quiet G star has little flare activity, so its worst body was 0.4%. **THE BINARY TEST THE ITEM ASKED FOR REFUTES ITS OWN HYPOTHESIS.** Multi-star systems are the MILDER case — 34 offenders, worst 5.71% — and single-star systems the worse, 71 offenders, worst 19.75%. What drives this is stellar ACTIVITY, not the spectral mix differing between the near and far ends of an orbit. Had it only been checked on a binary it would have been closed as negligible on the wrong evidence. **Fix: delete the second sum.** `calculateStellarRadiationComponents` takes a distance selector, and mean / min / max are all it, evaluated at the current, far and near distances. Every star's split and flare weight are constants of the star and flux falls monotonically with distance, so **min <= mean <= max now holds by construction**, not by luck. 105 -> 0. Pinned by a test whose subject is a flaring star on an eccentric orbit. **Derived diff touches exactly four fields on 68 bodies** — `stellarRadiationMin/Max`, `surfaceRadiationMin/Max`. No mean, no temperature, no habitability, no class moved, which is the expected shape: the mean was always right and the endpoints were short. Sol's three named offenders now bracket — Neptune 2.277 in 2.271-2.284, Triton 2.566 in 2.553-2.579, Neptune's Rings 0.566 in 0.553-0.579. | fixed v2.1.334-beta |

## C — Rendering / appearance

| # | Observation | Status |
|---|---|---|
| C1 | `condensateTint` (cloudDecks.ts ~165) pulls every deck to within 60/255 of white. Right for scattering droplets, wrong for a pigmented suspension — the ceiling is pastel for ANY deck, whatever the data says. Now `LiquidDef.cloudTintDistance`, rule-pack data with the old 60 as the default, so nothing shipped changes look unless its data says so. The physics is that a clean SCATTERER goes white however dark the liquid (water) while an ABSORBING suspension keeps its colour however finely divided (Jupiter's brown hydrosulphide, martian dust). Set on the five genuinely pigmented condensates. **No visible change on the bundled maps** — the only bodies carrying a pigmented deck are giants, and giants take their look from gas chemistry rather than the deck path (E6). It bites the moment someone authors one, or when giants join the deck stack. | fixed v2.1.283-beta |
| C5 | **Which plane does a moon actually orbit in? The Laplace plane, and we approximate it with a flag.** Raised by the C3 audit. C3 rotates a satellite's orbit into its parent's equator, which is right for regular satellites and wrong for distant ones: beyond roughly fifty host radii solar perturbation wins over the parent's oblateness and the orbit follows the SYSTEM plane instead. The real answer is the Laplace plane, which transitions smoothly between the two with distance. C3 approximates it with a per-orbit `frame: 'ecliptic'` declaration, set on Luna (60 Earth radii) and Phoebe (222 Saturn radii). **The case that shows the limit is Iapetus:** at 61 Saturn radii it is squarely in the transition, its orbit genuinely precesses about the Laplace plane, and its quoted 15.47 deg is conventionally referenced to Saturn's equator — so it is currently treated as equatorial and drawn 15.47 deg off the ring plane, which is defensible but not exactly right. **What a proper fix looks like:** compute the Laplace plane from the host's J2, mass ratio and the moon's semi-major axis, and orient the orbit to it — a physics change, in the propagator or a derivation feeding it, needing a before/after across the bundled maps. Until then the flag is the honest lever, and PROC GEN should set it for anything it generates beyond the transition. Related: C3 (c), also open. | captured |
| C3 | **[SCOPE WIDENED on request: also AUDIT every moon in both bundled maps, and make PROC GEN incline moons and rings with their host by default.]** **Moon orbits ignore the parent's axial tilt; rings do not — so coplanar bodies are drawn ~27° apart.** Noticed as "Saturn's moons don't sit in the ring plane". The DATA is correct and real: rings `i_deg = 0`, Mimas 1.57, Enceladus 0.009, Dione 0.019, Rhea 0.345, Titan 0.348, Iapetus 15.47 — all quoted the conventional way for regular satellites, i.e. **relative to the parent's equator**. Both ring builders in `holo/scene.ts` (~2584 particle ring, ~2670 band) explicitly rotate into the equatorial plane — "Ring plane = planet equator" — by `parent.axial_tilt_deg`. Moon ORBITS do not get that rotation, so their `i_deg` is applied in the system plane and they stay flat while the rings tilt 26.73°. Real Saturn has its inner moons IN the ring plane, so the picture is misleading; Iapetus should be the only visibly inclined one and is currently wrong the other way. **Fix:** a satellite's orbital reference frame should be the parent's equator, the same rotation the rings already use. **Scope:** every moon system — Jupiter's Galileans, Uranus (98° tilt, so the error is dramatic there), Neptune/Triton. **Confirm the convention first, do not assume:** satellite inclinations are parent-equatorial but PLANET inclinations are ecliptic-relative, so this must apply to satellites only, not to every orbit.  **FIXED (a)+(b) v2.1.316-beta; (c) NOT done.** **The convention held, with two real exceptions — and they are physics, not data errors.** Audited all 31 distinct moons across both bundled maps, 19 of them with a tilted host. Every regular satellite is quoted parent-equatorially as assumed: Saturn's inner six, Jupiter's Galileans, Mars's pair, Uranus's three, Triton. **The exceptions are the far-out ones, and there is a reason:** beyond roughly fifty host radii the LAPLACE PLANE hands over from the parent's equator to the system plane, so a distant moon's orbit follows the ecliptic instead. Luna sits at 60 Earth radii and its 5.145 deg is the ECLIPTIC value (to Earth's equator it is 18.3-28.6 deg and time-varying, so there is no single number to author); Phoebe at 222 Saturn radii is the same story. Both are now declared `orbit.frame: 'ecliptic'` and are left in their own plane — a declaration, in the spirit of this batch, rather than a threshold invented in code. **Iapetus is genuinely ambiguous and was left alone:** 61 Saturn radii puts it squarely in the transition, and its 15.47 deg is usually quoted to Saturn's equator, so it is treated as equatorial. See C5. **Verified numerically rather than by eye, which for angles is the stronger check** — the angle between each moon's orbit normal and the ring-plane normal, before and after. Saturn: every inner moon 26.73 deg off the ring plane before, now at its own inclination (Enceladus 0.01, Dione 0.02, Rhea 0.34, Titan 0.35, Tethys 1.12, Mimas 1.57), with Iapetus 30.60 -> 15.47 as the one odd moon out. Uranus 97.77 -> 0.06-0.34. Jupiter 3.13 -> 0.05-0.47. Triton 144.30 -> 157.30, its true retrograde figure. Luna unchanged at 23.97 deg from Earth's equatorial plane, which is inside the expected 18.3-28.6 band — an independent confirmation that the ecliptic flag does the right thing. **Applied to SATELLITES ONLY**, gated on the scene's `satellite` flag: barycentre members share a parent but are system-level and must not be rotated, and a planet's inclination is ecliptic-relative and is never touched. Both sites moved together — the moon's body position and its orbit ring — so the moon rides its own ring. **(c) proc gen is NOT done:** new moons and rings are still generated without inheriting their host's tilt. It needs the generator, not the renderer, and wants its own pass. | fixed v2.1.316-beta (a,b); (c) open |
| C4 | **ON-PLANET constructs sit at the planet's CENTRE instead of on its surface, and so do not turn with it.** Noticed on the starmaps generated for the rebuild. A surface construct should be glued to a fixed point on the surface and inherit the body's rotation period; instead it renders at the centre and stays still while the planet turns. **Partly verified — two different definitions of "surface construct" exist and they may not agree.** (1) DATA: `constructsOf` (`document/systemTopology.ts:66-70`) splits surface from orbiting on an authored field, `c.placement === 'surface'` — an explicit declaration. (2) GEOMETRY: the holo scene does not read that field at all; it DETECTS surface-lock by comparing the construct's distance against `physRadiusAu`, "the true physical radius in AU (for detecting surface-locked constructs)" (`holo/scene.ts:141-142`), then glues the construct to a fixed surface point and re-glues it every frame so it turns with the body (~1794, ~1930). So the scene infers what the data already states. **Most likely cause, NOT yet verified — check this first:** if `physRadiusAu` is absent or zero on the generated bodies the detection cannot fire, and a construct authored at distance 0 then renders exactly where it was put, which is the centre. That would also explain why it does not rotate: the re-glue at ~1930 only runs for constructs the lock engaged on. Relate to D2 — the rebuilt systems are data-poor by design, so a mechanism that depends on a derived field is exactly what breaks on them first. **The fix is probably not a scene fix.** An authored `placement: 'surface'` is a stronger statement than a geometric guess and should WIN: the scene should honour the declaration and place the construct on the surface, rather than re-deriving surface-ness from a radius that may not be there. Confirm which way round it actually is before building — **FIXED v2.1.315-beta. The hypothesis above was WRONG, and it IS a scene fix — the data is fine.** `physRadiusAu` was never absent: it has a 3000 km fallback (`scene.ts:1743`) and every host in question has a real radius. What actually excluded them was the guard `off > 1e-12` on the surface-lock branch. Read the shipped data and it is unambiguous — all seven surface constructs across both bundled maps (Ceres, Eros, Ganymede, Phoebe, Bellerophon, Hadley's Hope, The Derelict) declare `placement: "Surface"` and carry NO `orbit` at all, because that is the honest way to author a ground station. So `off` is 0, the lock branch skipped them, and the orbital branch below is gated on the SAME `off > 1e-12` — so nothing positioned them and they kept `tmpParent`, the parent's centre, and were never re-glued, hence motionless. The one case that most plainly means "on the ground" was the one case both branches refused. **Fix:** the scene now reads the declaration, using the same test as `constructsOf` (`String(placement).toLowerCase() === 'surface'`) so the document builder and the scene cannot disagree about which constructs are on the ground; a declared construct locks whatever its offset. Where there is no offset to say WHERE, the landing site is derived from the construct's id — deterministic across reloads, and spread by latitude as well as longitude so two stations on one moon do not stack. Verified numerically: all seven directions unit length, and the LV-426 pair 33.8 degrees apart. **NEW FINDING, and it is the other half of "does not turn":** a host with no `rotation_period_hours` has no `spinPeriodSec`, so its quaternion never changes and a correctly surface-locked construct still will not rotate. Altair IV has no rotation period; LV-426 has 50 h. On the rebuilt data-poor maps this half persists after this fix and is a DATA gap — see D2, which already lists `rotation_period_hours` as one of the fields those systems lack. **What I could NOT confirm:** a clean visual of a construct sitting on the limb. The construct is a fixed-screen-size sprite and the planet mesh wins the raycast across the whole disc, so the pick oracle cannot see it either before or after (confirmed by A/B-ing the fix off and sweeping both ways); at a large framing the icon is swallowed by the globe and at whole-system framing the offset is sub-pixel. Altair IV, the one host I could reach in the automated sweep, also has no spin, so nothing there could turn regardless. The causal chain is verified at both ends — data read from the shipped JSON, both branches read in the code — but the eyeball check on a spinning host (LV-426 or the Sol moons) is still worth doing. Original hypothesis text follows: check a generated map for what a surface construct carries, and whether `physRadiusAu` reaches the scene for those bodies. If the generator is simply not writing `placement`, this is a DATA item and belongs in bucket D. | fixed v2.1.315-beta |
| C6 | **The GM 2D orrery's body disc never spins — surface features hold a fixed orientation while the lighting tracks the star correctly.** Reported 2026-08-01 on Mercury: "it does not rotate at all, so the right face is not facing the star... the cratered side always faces the same direction". The player 3D view is fine, because a real sphere spinning is free there. **Low priority, said so explicitly ("no biggie"), but recorded because the cause is not what it looks like.** **Verified:** `catalogue/PlanetDisc.svelte` takes `body, ringed, ringDensity, size, suppress, lightAngle, showStamp` — **there is no rotation or spin-phase input of any kind.** What it does have is `lightAngle`, which the orrery passes as the true angle toward the star, so the TERMINATOR is already correct and tracks the body round its orbit; only the surface stays put. Feature positions come from a seeded PRNG per body id (`seeded(9001)` craters, `seeded(9002)`, and so on) under the stated intent that "every feature seeds its own positions so the look is stable" — stability was the goal, and staticness is the same thing seen from another angle. **THE TRAP, and the reason this is not a one-liner:** rotating the disc's features about its centre is not a spinning sphere, it is a spinning record. A real spin carries features ACROSS the face and compresses them into the limbs, so an honest fix needs feature positions treated as sphere longitude/latitude and projected per frame, or at minimum a horizontal traverse with limb foreshortening. A naive `rotate()` will look worse than the current stillness and should not be accepted as a fix. Inputs needed: `rotation_period_hours` and the current time — both already available, and both now trustworthy after [[B7]] and [[B9]]. **Note also this only shows when zoomed in:** `DISC_OVERLAY_MIN_R = 11` px gates the overlay, so nothing is promoted at whole-system framing (see [[A10]]). Related: [[A10]], [[B7]]. | captured |
| C2 | `planetAppearance.ts` resolves deck liquids via `liquidDef(species)` with NO rulepack argument, so a campaign's own custom liquids never reach the 3D deck renderer. Worked around by putting `taumoeba-bloom` / `iron-oxide-dust` in the global list. If custom liquids are a real authoring surface, this needs fixing. | triaged |

## D — Content / data

| # | Observation | Status |
|---|---|---|
| D3 | **TWO NODES SHARE ONE ID in two bundled systems, so one of each pair is unreachable.** Found by the A6 sweep (v2.1.324) while checking the drill-in lists for duplicates — the duplicate was in the DATA, not the builder. `Struve 2398`: the planet "Struve 2398 A b" and the star "Struve 2398 B (GJ 725 B)" are BOTH `struve2398-b`. `Groombridge 34`: the planet "Groombridge 34 A b" and the star "Groombridge 34 B (GQ Andromedae)" are BOTH `groombridge34-b`. Same mechanism both times — the slug for "<system> A b" drops the component letter and collides with "<system> B". Swept all 42 systems; these two are the only collisions. **Consequences, and they are not cosmetic.** (a) `nodeById` is a `.find()`, so the FIRST match wins and the second node cannot be selected by id at all — in both systems the planet is ordered first, so tapping the companion STAR opens the planet. (b) `membersOf` tests `ids.includes(n.id)` against the barycentre's `memberIds`, so the PLANET matches the star's id and is listed as a barycentre member: the Struve 2398 barycentre page shows "Moons: ★ Struve 2398 B | ● Struve 2398 A b" — two rows, one id. (c) Anything else keyed by node id — selection, focus, the player broadcast, routes, constructs — is ambiguous in those two systems. **Where the fix belongs: the starmap build kit (`scripts/starmap-build/`), not the app.** Note the constraint before touching it — stable ids are deliberate and are referenced from elsewhere, so renaming one is a data migration, not an edit; the planet is the safer one to re-slug (`struve2398-ab` / `struve2398-a-b`) since the star's id follows the system's own naming. Related: [[A6]]. **FIXED v2.1.326-beta.** **Mechanism, one line in the generator:** `build-starmaps.mjs:174` forms a planet id as `` `${planetIdPrefix ?? slug}-${letter}` ``, and Struve 2398 A's prefix is the bare system slug — so its planet "b" became `struve2398-b`, which the roster authors two lines below as the companion star's `starId`. The planet's NAME already carries the component letter ("Struve 2398 A b"); its id did not. **Which one moved, decided on evidence rather than the guess above:** counted every occurrence of each id in both maps — 3 each, being the two nodes' own ids plus ONE external reference, the barycentre's `memberIds`, and that reference means the STAR. The planet's id is referenced by nothing. So the planet moved to `struve2398-a-b` / `groombridge34-a-b` and the star kept the id everything points at. **`groombridge34-c` was deliberately left alone** — it is a working stable id on the second planet, and renaming it for symmetry would break the very rule this fix exists to protect ("Never renumber", `scripts/starmap-build/README.md`). The two planets of that star now have differently-shaped ids; that is a cosmetic wart preferred to a real broken reference. **Fixed in the build kit AND in the shipped JSON, because the kit can no longer be run** — see [[D4]]: regenerating would revert twelve hand-applied changes including C3's `frame: 'ecliptic'`. The kit uses `planetOverrides: { '<archive name>': { id } }`, the same mechanism `epseri-aegir` already uses, so the two now agree on these ids; the shipped maps were patched by anchored text surgery (2 lines each, verified as exactly 2) rather than re-serialised, since a JSON round-trip is not byte-identical and would have buried the fix in a 20,000-line reformat. **A duplicate-id assertion now runs on every build and THROWS** — naming the system and both colliding nodes — rather than de-duplicating, because a generator that quietly renames a clash hides the next one exactly as well as silence did. Proven both ways: it passes with the fix, and removing one override reproduces the original failure verbatim. **Verified as an A/B, which is what makes it more than a rename:** on the pre-fix file, asking for the id of "Struve 2398 B (GJ 725 B)" returns "Struve 2398 A b" — both systems, both maps; on the fixed file every node in both systems resolves to itself, and the barycentre page now reads "Companion star: Struve 2398 B" instead of listing the planet as a member. Swept both maps again afterwards: 414 nodes, zero collisions. **What this does NOT fix, and it matters:** an EXISTING campaign built from the old map still carries the old ids — the bundled files are a template, not a live link — so a GM already playing Struve 2398 keeps the collision until they rebase. Related: [[A6]], [[D4]]. | fixed v2.1.326-beta |
| D4d | **DECIDED 2026-08-01: fold the twelve fixes IN and keep the kit authoritative.** The kit stays the source of truth its README claims to be. Each hand-applied fix moves into the kit's generator input, roster or rules — whichever is the honest home for that particular correction — so that a rebuild reproduces the shipped maps EXACTLY. Then pin it: a test that regenerates and asserts equality against the shipped JSON, so the next drift fails loudly at the moment it is introduced rather than being discovered a month later by someone who stopped to check. The alternatives were rejected deliberately: demoting the kit to a one-time bootstrap leaves it to rot and makes every future roster change manual, and a diff-only gate makes drift visible without making regeneration usable. **Sequencing note: do this BEFORE any further starmap data work**, including the C3 moon audit and anything arising from B9 — every one of those wants to change generator input, and doing so while the kit cannot reproduce its own output is how the twelve become twenty. Each fix should be folded in as its own commit, naming which fix it is, so a wrong home for one can be rewound alone. | decided, ready to build |
| D4 | **THE STARMAP BUILD KIT NO LONGER REPRODUCES THE SHIPPED MAPS — running it reverts twelve hand-applied fixes. Read this before touching `scripts/starmap-build/`.** Found by D3 (v2.1.326), which had to establish whether regenerating was safe before changing generator input; it is not. `README.md` presents the kit as the source of truth ("regenerate the three output files"), and it is offline and deterministic, so the natural move is to edit the roster and rebuild. **Do not.** Diffed shipped against regenerated node by node, ignoring formatting: same 42 systems and same node counts in both maps, no nodes added or lost, but **12 nodes differ in content** and every one of them is a fix applied to the JSON and never back-ported to the kit. The real map: `solar-system-luna :: orbit` — that is **C3's `frame: 'ecliptic'` (v2.1.316)**, and the string "ecliptic" appears nowhere in `scripts/starmap-build/`. The SciFi map: Luna and `sol-phoebe` again (C3's other body), plus `tauceti-adrian :: radiusKm` and `:: atmosphere`, `tauceti-hail-mary :: engines` and `:: fuel_tanks`, a second construct's engines/fuel_tanks, a `gmNotes`, a `description`, and a **`parentId`** — a re-parenting that a rebuild would silently undo. There is also a whole-file formatting difference: the SciFi map ships at 2-space indent and the generator writes 1-space, so a rebuild produces a 20,000-line diff that would bury any real change inside it. **Why this is worth its own entry rather than a note on D3:** the kit is not merely stale, it is stale in a way that LOOKS safe — deterministic, committed caches, a README inviting you to re-run it — and the damage would be a silent revert of shipped physics fixes rather than a build failure. D3 was fixed on both sides for exactly this reason. **Two ways out, and it is a real decision:** (a) back-port the twelve deltas into `systems-real.mjs` / `systems-fiction.mjs` so the kit is authoritative again, then regenerate once and accept the reformat as a single mechanical commit; or (b) accept that the shipped JSON is now the source of truth, and demote the kit in its README to "how these maps were first built" with a loud warning. (a) is the better end state and is a genuine piece of work — the twelve deltas have to be understood, not just copied, because some (C3's frame flags) encode physics decisions. **Cheap guard worth adding either way:** a test that regenerates to a temp dir and asserts content-equality with the shipped files, so the two can never drift silently again. Related: [[D3]], [[C3]]. | captured |
| D1 | Adrian's green: the physics currently says nothing in its air condenses. Needs the Taumoeba fraction and the bloom's boil point tuned until a deck forms — DATA, not code. Blocked visually by C1 even once it does. | triaged |

## E — Known-not-ours

| # | Observation | Status |
|---|---|---|
| E1 | `src/routes/page.spec.ts` — 4 failures (jsdom cannot fetch a relative URL). Verified failing at 255768a via a detached worktree, so NOT from the v2.1.277 work despite being attributed there. Likely origin `371f649`. | triaged |

---

## Captured, not yet triaged

_(new observations go here — rough is fine)_

| # | Observation | Status |
|---|---|---|
| B19 | **Mars is tagged "Stability · Fated: flung out". It is not going anywhere.** Sanity check 2026-08-01. Mars's orbit is stable on any timescale a game cares about; the inner Solar System shows only mild chaos over billions of years. A prediction of ejection points at the test, not at Mars. **Where it comes from:** `physics/stability.ts:146` — `if (delta < 5.5 && !isResonanceProtected(inner) && !isResonanceProtected(outer)) out.fate = 'eject'` — a MUTUAL HILL SEPARATION test, with other eject paths at `:126` (mass-ratio dependent) and `:362`. The 5.5 threshold is itself defensible: the literature puts long-term instability below roughly ten mutual Hill radii for comparable masses, so 5.5 is conservative rather than aggressive, and the constant is probably not the fault. **HYPOTHESIS, NOT VERIFIED — CHECK IT FIRST:** Mars against Earth is about 26 mutual Hill radii and could not trip this. But Mars's outward NEIGHBOUR IN ORBIT ORDER IS THE ASTEROID BELT, and [[B11]] has just established that belts return early from the third derivation pass. If a belt node carries a mass and is included in the neighbour pairing, then the mutual Hill radius is being computed between a planet and a diffuse belt, which is not a two-body problem and will produce nonsense. Confirm what Mars is actually being paired with before touching any constant — print the pair and the delta. **If that is it, the fix is about which nodes are ELIGIBLE for a pairwise stability test, not about the threshold**, and it will affect every system with a belt between planets rather than Mars alone. Do not "fix" this by raising 5.5 until the pairing is proven sound. Related: [[B11]]. **FIXED v2.1.338-beta. THE HYPOTHESIS IS REFUTED AND THE THRESHOLD IS INNOCENT — both checked before anything changed.** **Belts were already excluded.** `annotateGravitationalStability` filters `roleHint` 'belt' and 'ring' out of `orbitalNodes` before any pairing, with a comment naming this exact hazard ("their massKg is a debris-density proxy, not a point mass"), and `stability.spec.ts` already had a test for it. No planet is being paired against a diffuse belt. **The 5.5 threshold never fires here.** Printed the pair and the delta first, as the entry demanded: Mars's inward neighbour in the starmap Sol is **433 EROS**, a 16 km near-Earth asteroid carried as `roleHint: 'planet'` at a = 1.458, and the pair sits at **DELTA = 9.28** — well outside 5.5. The constant is untouched. **The real cause is the ORBIT-OVERLAP branch (`stability.ts:95-126`), and it is one line below where anyone was looking.** Eros's eccentric orbit genuinely does cross Mars's, so the crossing test fires correctly; with a mass ratio of ~1e-8 it correctly returns `eject` rather than `collision`. Then `mergeAssessment` copied that fate onto **BOTH** members of the pair. **The code's own comments already stated the intent** — "a lightweight crosser is scattered out", "packed systems shed their lighter member" — so the model always knew which body it meant and simply never recorded it. **Fix: `StabilityAssessment` gains `fateNodeId`**, set to the lighter body on both eject paths (crossing and Hill spacing) and left unset for a `collision`, which between comparable masses genuinely IS mutual. Severity and reasons still merge onto both, because both bodies really are in a risky pair — only the FATE is directional. **Measured over every bundled system: 29 bodies tagged `fate/eject` -> 22, and all seven that dropped it were the heavier half of their pair.** Mars keeps its metastable severity and loses the verdict; Eros keeps it, which is right. GJ 876 is the clean demonstration — b at 4.32e27 kg drops it while c at 1.36e27 and e at 8.72e25 keep it. **`tests/output/solar-system-derived.json` is UNCHANGED, correctly:** the baseline is built from `static/examples/Sol_2030-System.json`, which contains no Eros — the reported body lives in the starmap Sol. The sweep above is the measurement. Pinned by three tests, one of which asserts the delta is outside 5.5 so nobody re-blames the threshold. **Two things noticed and NOT fixed:** several bodies carry a reason reading "...so it stays stable" beside "Predicted outcome: flung out", because the fate came from a different driver than the reason that is printed next to it ([[B24]]); and for two nearly equal masses on the Hill path "lighter" is close to arbitrary, though naming one is still more honest than naming both. | fixed v2.1.338-beta |
| B18 | **Uranus scores 50% habitability — above Mars at 8% and Enceladus at 35%.** Sanity check 2026-08-01. Uranus is an ice giant with no solid surface, crushing pressures at depth, violent winds and extreme cold at the cloud decks. Even on the most generous atmospheric-envelope reading it should not outrank a rocky world with a real surface or a moon with a subsurface ocean. The RANKING is the evidence here, more than the absolute number: whatever the score means, Mars and Enceladus should sit above Uranus on it, and they do not. **Suspect a weighting artefact rather than a wrong input** — something over-rewarding atmospheric mass, volatile presence or air mix, which a gas giant trivially maximises while a thin-aired rocky world scores near zero on all three. **Where to start:** `SystemProcessor.ts:1170` clamps `habitabilityScore` to 0-130, with a subsurface-niche floor at `:1179-1180` (`SUBSURFACE_NICHE_SCORE`) and an alien-habitable gate at `:1188` requiring score > 40 and `factors.solvent > 0`. The factor weights feeding that score are the thing to audit — I have not read them, so treat the artefact diagnosis as a starting hypothesis rather than a finding. **Check whether a body with NO SOLID SURFACE should be scoring on this axis at all**: the subsurface-niche floor shows the model already accepts that habitability can mean something other than "stand on it", so the question is whether a giant's envelope is a niche the model means to score, or an accident of factors designed for surfaces. Answer that before adjusting weights, or the weights will be tuned to hide a category error. Sanity anchors to hold the result against once changed: Earth top, Mars low but non-zero, Enceladus and Europa meaningful via the subsurface niche, Venus low, the giants below all of them. **FIXED v2.1.339-beta. IT IS A CATEGORY ERROR, NOT A WEIGHTING ARTEFACT — the category question is answered first, and answering it made the weights irrelevant.** **The axis is a SURFACE model and says so throughout:** the running total is literally named `surfaceScore`, every factor is a surface condition, and `isEarthLike` / `isHumanHabitable` all test what it is like to stand there. **The proof that the model MEANS surface is the subsurface niche itself** — non-surface habitability is handled by ADDING an explicitly named niche with its own floor and its own tier, precisely because these factors cannot express it. So the model had already answered the question: a niche that is not a surface gets named and scored separately, rather than being left to surface factors. A giant's envelope is therefore outside what this axis measures. **Which factor does a giant trivially maximise? All three that survive, and the arithmetic is exact.** Uranus scores **temp 0, solvent 0** — the two worth 50 of the 100 points, and the two that actually decide the question — then **pressure 18 + radiation 17 + gravity 15 = 50**. Neptune is identical, Saturn 49.9, Jupiter 30 (it loses gravity at 2.64 g). **And all three are evaluated at a level that is not a surface:** a giant's `atmosphere.pressure_bar` is pinned to the 1-bar cloud-top reference (a deliberate simplification, ARCHITECTURE.md), and its "surface" radiation and "surface" gravity are computed at that same notional radius. The three factors are not over-weighted — they are being asked about a place that does not exist. Tuning them would have buried that. **Gated on `makeupFractions(planet).gas <= 0.5`, the same test the geology model already uses**, so the two agree on what a surface is. A caught body scores 0 and its Bio tab reads "No solid surface — surface habitability does not apply" instead of a blank panel or a silent 50. **Every anchor the entry set now holds:** Earth 100 (earth-like) > Titan 47 (alien — it has a real surface and real liquid methane) > Europa 35 = Enceladus 35 (subsurface niche) > Mars 8 > Venus 7 > **all four giants 0**. **Swept all 366 bundled planets and moons: 59 caught, every one a giant / neptune / puffy class, and ZERO of them have a surface liquid** — which is the check that would have exposed a false positive. `planet/hycean` is NOT caught, so an ocean under a thick H2 envelope keeps its score. 52 of the 59 were previously scoring over 10 from the surfaceless factors alone. **Derived diff is four bodies and only the giants: 30/50/50/50 -> 0. Their TIER does not change** — it was already `habitability/none`, because `isAlienHabitable` requires `factors.solvent > 0` and they never had any. **Only the NUMBER ever lied**, which is why this survived so long: the tag was right the whole time. **Deliberately NOT done:** whether a giant's ENVELOPE deserves an aerial niche of its own, the way the subsurface ocean has one. That is a design decision rather than a bug fix, and inventing one unasked would repeat the same mistake in the other direction. Also raised: [[B25]]. | fixed v2.1.339-beta |
| B17 | **Io's radiation is far too low — nothing models radiation from a NON-STELLAR source.** Sanity check 2026-08-01. Io shows `moderate (19.2-2... mSv/y)` and tags `Irradiation: low`. In reality Io sits deep inside Jupiter's inner magnetosphere and its energetic particle belt: unshielded surface dose is of order 36 Sv per DAY, around 13,000 Sv/year, making it one of the most violently irradiated surfaces in the Solar System. The engine is out by something like six orders of magnitude, and it is out because the source does not exist in the model rather than because a number is mistuned. **The engine computes radiation from STARS only** — `calculateSurfaceRadiation` and `calculateTotalStellarRadiationRange` are stellar by construction (see [[B8]], which fixed a missing flare term in that same stellar path). **The interesting part, and the reason this is not simply "a missing feature":** the engine ALREADY KNOWS a body can sit inside a giant's magnetosphere. `SystemProcessor.ts:765-768` computes `insideHostMagnetosphere` from the host's mass, gas fraction and field strength, and feeds it to `deriveMagnetism` — but it is used only to influence the MOON'S OWN magnetism, never as a radiation source. So the concept is present and pointed the wrong way: being inside a magnetosphere is currently treated purely as context for a dynamo, when for a close-in moon of a strong-field giant it is the dominant HAZARD. That existing flag is the natural hook. **Scope honestly before building:** a real magnetospheric dose model needs the host's field strength, the moon's orbital distance in host radii, and some belt profile — and it should degrade gracefully to zero for the many hosts with no meaningful field. It will change Io, Europa, Ganymede and Callisto (in that steep order) and essentially nothing else in the bundled maps, which makes it easy to validate and hard to get accidentally wrong elsewhere. Anchors exist for all four Galileans, so this is a model that can be CALIBRATED rather than guessed — the same standard [[B2]] was held to. **THE INPUTS ALREADY EXIST AND ARE ALREADY ACCURATE — checked in `tests/output/solar-system-derived.json`, so this is far cheaper than "a new physics model" sounds.** A magnetosphere is powered by FIELD x SPIN, and the engine already derives both for every giant: Jupiter `strengthGauss` **4.32** against a measured ~4.3 and rotation 9.925 h; Earth 0.5014 against ~0.5; Saturn 0.2396 (~0.21) at 10.656 h; Uranus 0.2088 (~0.23) at **-17.24 h, retrograde with the correct sign**; Neptune 0.2235 (~0.14) at 16.11 h. Nothing new needs authoring or placeholding — `deriveMagnetism` is already producing the numbers and the radiation path simply never asks for them. **The clean diagnostic, worth using as the before/after test:** Io's `surfaceRadiation` is 20.9397 and Europa's is 20.9276 — near-identical, because solar distance is the only term in the model, while in reality Io takes roughly SEVEN TIMES Europa's dose. Two moons agreeing to four significant figures is the fingerprint of the missing source, and separating them is the headline result. Io's own `strengthGauss` is 0, correctly — it has no dynamo, so all of its dose is external. **Shape of the model:** dose as a function of (host field, host spin) x a steep falloff in orbital distance measured in HOST RADII — the Galilean anchors imply something like an inverse sixth-to-eighth power between 5.9 and 26 R_J. Saturn is the second system and the real test of the field term, since its structure is the same and its field is roughly eighteen times weaker, so Enceladus and Titan must come out far lower than the Galileans rather than merely proportionally lower. Must degrade to zero for hosts with no meaningful field, which is most of them. **DO NOT BUILD THIS AS "A PLANET EMITS RADIATION" — that was proposed 2026-08-01 and it is the wrong mechanism, for a reason that is decisive rather than stylistic.** Three things are easily conflated here and only one is missing. **(a) Self-luminous bodies are ALREADY DONE.** `physics/substellar.ts` models them from mass, system age and radius, exposes `luminositySolar` commented "for irradiating moons", and `isLuminousSource` (`:51-52`) is `roleHint === 'star' || isSelfLuminous` — so a substellar object already joins the same `radiationOutput / d^2` sum as the stars in `calculateTotalStellarRadiation`. The flag is set by the processor's substellar pass (`SystemProcessor.ts:336/347`) and the model's table starts at **13 M_jup**, the deuterium-burning limit. Brown dwarfs are covered; the sum was always a predicate over emitters rather than a hardcoded star check. **(b) Jupiter is correctly EXCLUDED and must stay so.** At ~1 M_jup it is an order of magnitude under that floor, and its real excess output (about 1.67x absorbed sunlight, from Kelvin-Helmholtz contraction) is INFRARED and non-ionising. It is a heat source, not a dose source. **(c) THE ARITHMETIC THAT SETTLES IT.** An emitter-plus-distance model is inverse-square, `r^-2`. The Galilean anchors demand a falloff between **`r^-4`** (Io to Europa: 1.59x the distance, 6.7x less dose) and **`r^-8.6`** (Io to Callisto: 4.4x the distance, 360,000x less dose). No inverse-square source can fit those: calibrate at Io and Callisto is wrong by orders of magnitude, calibrate at Callisto and Io is. That is because the dose is not radiation FROM Jupiter but charged particles TRAPPED BY its field and accelerated by its rotation — a peaked belt profile in planetary radii, which is a different law and needs its own TERM rather than a new entry in the luminous-source sum. **CORRECTED AND SHARPENED 2026-08-01 — "surely we can work out its likely radiation output, magnetic field driven, all procedural and reusable" is RIGHT, and the objection above applies only to the DISTANCE LAW, not to the idea of a derived planetary source.** The intended chain — rotation and composition give a field, the field drives a source strength, that source is summed at the receiving body — is exactly correct and is what this item should build. **And the sum it needs ALREADY EXISTS.** `calculateStellarRadiationComponents` (`radiation.ts:55-82`) splits every source into a PHOTON and a PARTICLE channel, and the particle channel is already attenuated by the receiver's magnetosphere and atmosphere: "Goes in the particle channel so a magnetosphere + atmosphere shield against it (an unshielded close world bears the brunt)." Trapped belt radiation is a PURE particle-channel source with no photon component, so it drops straight into existing, already-shielded machinery. `insideHostMagnetosphere` (`SystemProcessor.ts:765-768`) is already computed and is the natural gate. **So the whole delta is ONE TERM: a belt strength derived from host field x spin, delivered into the particle channel with a falloff in HOST RADII rather than `1/d^2`.** Nothing about the architecture needs changing and no body needs a special case. **CALIBRATION HAZARD, and it is the same shape as the Mars albedo trap in [[B5]]:** Ganymede is the only moon in the Solar System with its own dynamo (~0.0072 G measured) and that shield is part of why its dose sits where it does — but the engine currently gives it `strengthGauss: 0`. Calibrate the belt profile against Ganymede's measured dose without modelling its shield and the shielding gets baked into the belt law, giving a fit that is right for the wrong reason. **Calibrate on Io and Callisto, neither of which has a field, then treat Ganymede's shield as a PREDICTION to check.** Related: [[B8]], [[B12]], [[B5]]. **FIXED v2.1.340-beta.** **Root cause in a sentence: the engine only ever summed LUMINOUS sources, so the dominant hazard for a close-in moon of a strong-field giant had no representation at all** — not a mistuned number but an absent mechanism, and the fingerprint was Io 20.9397 against Europa 20.9276, two moons agreeing to four significant figures because solar distance was the only term either had. **THE SUBSTELLAR PATH WAS VERIFIED END TO END FIRST, as the brief demanded, rather than trusted from the predicate.** Measured on a synthetic host: at 1 M_jup the moon sees 2.01 mSv/yr; at 13 M_jup `isSelfLuminous` flips and it sees **104.4**, then 351 at 20 M_jup and 2626 at 40. The path works, Jupiter at ~1 M_jup is correctly excluded, and the new term sits beside it without touching it. **THE LAW, and its shape is forced by the anchors rather than chosen:** `dose(r) = D0 · (B/B_ref)² · (Ω/Ω_ref) · exp(−r/λ)` with `λ = λ_ref · (B/B_ref)^(1/3)`, r in HOST RADII, delivered into the particle channel. An emitter at a distance is inverse-square; the Galileans demand r^-4 from Io to Europa and r^-8.6 from Io to Callisto, and no power law fits both — but a belt is not a point source, it is a population confined by a field, and **one exponential fits the entire family**. **The exponents are REASONED, NOT FITTED** — there is only one calibrated system, so fitting them would be overfitting: B² is the magnetic energy density available to trap, Ω is the corotation drive that energises the particles, and a dipole's magnetopause standoff goes as B^(1/3), so a weaker host holds a TIGHTER belt as well as a fainter one. Constants live in `generation_parameters` as rule-pack data. No body is special-cased. **CALIBRATED ON IO AND CALLISTO ONLY** — the two Galileans with no field of their own, so no self-shielding is baked into the law. Two anchors, two free parameters, using the ENGINE's radii rather than textbook ones: **λ = 1.6324 R_J, D0 = 1451.1 Sv/day**. | body | r (R_J) | predicted | measured | ratio | | |---|---|---|---|---|---| | Io | 6.03 | 3.600e+1 | 3.600e+1 | 1.00 | fitted | | Europa | 9.59 | 4.086e+0 | 5.400e+0 | **0.76** | PREDICTED | | Ganymede | 15.30 | 1.234e-1 | 8.000e-2 | **1.54** | PREDICTED, held out | | Callisto | 26.92 | 1.000e-4 | 1.000e-4 | 1.00 | fitted | **GANYMEDE, the deliberate hold-out, came in 1.54x HIGH — and high is the right direction**, because it is the only moon in the Solar System with its own dynamo (~0.0072 G) and the law does not model that shield. Avoiding it as an anchor is what kept its shielding out of the belt profile; had it been fitted, the law would have been right for the wrong reason. **THE DERIVED DIFF: 18 bodies, all moons and rings of the four giants, and only radiation fields** plus one habitability knock-on (radiation is a habitability factor — only Ariel moves, 2 -> 0). No class and no temperature moved anywhere. `Io 20.940 -> 1.27501e+7 mSv/yr` (34.9 Sv/day against a 36 anchor) and `Europa 20.928 -> 1.43732e+6` (3.94 against 5.4). **Io/Europa is now 8.9x where it was 1.0006x** — that separation is the headline result. Jupiter's Rings top the list at 352 Sv/day, which is correct rather than alarming: the main ring sits near 1.8 R_J, inside Io and close to the belt peak. **SATURN, the field term's real test: Enceladus 0.0034 Sv/day and Titan effectively nothing, against Io's 34.9.** Enceladus lands **~10,000x below Io where the field ratio alone is only 18x**, because the B² intensity and the tighter B^(1/3) scale length compound. Far lower rather than merely proportionally lower, as required. **Degrades to zero where it should:** Luna sits 60 Earth radii out and gets nothing; Earth, Triton, Titania and Oberon are unchanged to five significant figures. It returns 0 for an unknown host SPIN too — an absent rotation is not a claim of a stationary host, and inventing a hazard from a missing input is worse than omitting one ([[B9]]). **[[B8]]'s invariant survives**: 0 of 34 bodies outside their own min/max, because the belt goes through the same three-way component call. **NOT DONE, said plainly.** (a) **Ganymede's authored 0.007 G is overwritten by `deriveMagnetism`'s 0, yet `radiationShieldingMag` reads 0.0768** — pass 2 shielded it with the authored value before pass 3 zeroed it. That is **[[B13]] visible in a single body**, and it means the engine's Ganymede figure (0.1086 Sv/day) is partly shielded by a field the same output reports as absent; the pure law gives 0.1234. Left for B13. (b) Whether a RING should carry a "surface" dose at all — it already did before this change, so it is not new, but it is odd. (c) [[B20]]'s radiation decomposition, which depends on this landing first. | fixed v2.1.340-beta |
| B20 | **Radiation should be decomposed BY SOURCE AND BY CAUSE, with a range — exactly as temperature already is.** Requested 2026-08-01: "radiation data blocks that detail the summing and sources. Perhaps even ranges if that is more accurate — like temp (tidally locked stars may have low radiation on non-star facing side)". **This is a MIRROR of an existing pattern, not a new subsystem, and it should be built as one.** `types.ts:36-38` already defines `TempSource = 'latitude' | 'seasonal' | 'diurnal' | 'locked-day' | 'locked-night' | 'tidal-hotspot'`, `TempComponent { source, label, lowK, highK, note? }` and `SurfaceTempProfile { meanK, totalMinK, totalMaxK, components[] }`, with the header comment stating the case for it: each component is the swing that source ALONE would produce around the mean, and the totals are the combined worst-case extremes. `BodyTemperatureTab.svelte:222` renders it with `{#each p.components as c}`. A `SurfaceRadiationProfile` should take the same shape, share the same cause vocabulary where it overlaps (`locked-day`/`locked-night`/`diurnal` all apply), and REUSE that renderer generalised rather than copied. **Two halves, and they are very different sizes.** **(1) Mostly restructuring:** the per-SOURCE itemisation. `calculateStellarRadiationComponents` (`radiation.ts:55-82`) already loops every luminous source and already splits photon/particle per source — it just ACCUMULATES into two running totals. Keeping the per-source rows instead of collapsing them gives "Alpha Centauri A contributes X, B contributes Y" for free, which is genuinely useful in a binary and currently impossible to see. The orbital-distance RANGE also already exists: the `where: 'current' | 'near' | 'far'` parameter computes periapsis and apoapsis flux, which is what [[B8]] rebuilt the endpoints from. **(2) Genuinely new physics:** the day/night and locked-face variation. **AND THE PHYSICS CAUTION THAT MAKES THE WHOLE BREAKDOWN WORTH HAVING:** "low on the far side" is TRUE for the PHOTON channel — the body's own bulk geometrically shadows the night side from starlight — and MUCH LESS TRUE for the PARTICLE channel, because charged particles spiral along magnetic field lines and arrive from all directions. So a locked world has a genuinely safe night side under a quiet star, and effectively none inside a giant's belt ([[B17]]). A single min/max cannot express that; a by-cause decomposition can, and it is the argument for doing this properly rather than just widening a range. **Do this AFTER [[B17]]**, so the belt term exists as a source to decompose rather than needing retrofitting into the block afterwards. **THE DEEPER POINT, AND IT CHANGES THE BUILD — "heat is just radiation much of the time".** These two profiles are not merely similar in SHAPE, they share INPUTS: the same emitters, the same `1/d^2`, the same periapsis/apoapsis range, the same locked-face and day/night geometry. So the correct structure is ONE flux-and-geometry layer feeding TWO derived profiles, not two parallel systems that happen to resemble each other. Building them separately is how they drift, and that exact failure has now happened twice in this engine: [[B8]] was literally two sums of the same quantity disagreeing, and [[B1]] was `albedo.ts` running a rival cloud model against the documented single evaluation. **THE ONE PLACE THEY MUST STAY DISTINCT — do not let the shared layer collapse them.** Temperature is driven by ABSORBED BOLOMETRIC flux, weighted by albedo and then REDISTRIBUTED by atmosphere and rotation. Dose is driven by the IONISING slice (UV/X-ray plus charged particles), a small spectrally distinct fraction, and it is ATTENUATED by atmosphere and magnetosphere rather than absorbed and spread around. The clean illustration: **Venus's night side is as hot as its day side, because a thick atmosphere transports the heat — while its night-side UV dose is zero.** Same body, same star, same geometry, profiles that diverge completely. Share the geometry, never the quantity. Related: [[B8]], [[B1]], [[B17]], [[A5]]. | captured |
| B21 | **Draw RADIATION ZONES around dangerously emitting planets, the way stellar zones are already drawn, and make routes passing through them say so.** Requested 2026-08-01, immediately after [[B17]] shipped. Three surfaces, and all three already have the machinery — this is an extension, not a new subsystem. **(1) THE PHYSICS IS FREE.** `physics/zones.ts` already computes stellar zones — `calculateKillZone(star)` (`:31`), `calculateGoldilocksZone` (`:103`), frost/soot/silicate/CO2/CO ice lines, and `calculateAllStellarZones` (`:285`). B17's belt law is `dose(r) = D0 x (B/B_ref)^2 x (Omega/Omega_ref) x exp(-r/lambda)` with `lambda = lambda_ref x (B/B_ref)^(1/3)`, calibrated to `lambda = 1.6324 R_J`, `D0 = 1451.1 Sv/day`. That INVERTS ANALYTICALLY: solve for a dose threshold and the zone radius is `r = lambda x ln(...)`. So a planetary hazard radius is one line of algebra against a law that already exists — no new model, no new fitting, and it automatically degrades to nothing for the many hosts with no meaningful field, exactly as the dose does. **Pick the THRESHOLD as rule-pack DATA, not a code constant**, and consider more than one band the way the stellar zones are banded rather than a single on/off radius — "lethal in hours" and "lethal in weeks" are different tactical facts for a GM. **(2) DRAWING IT.** The stellar zones already render; a planetary zone is the same idea centred on a planet rather than a star, so the question is reuse rather than invention. Watch the scale problem: a belt is a few host radii across, which at whole-system framing is sub-pixel, so it will need the same screen-space treatment the true-scale work uses ([[A9]], [[A19]]) or it will be invisible exactly when a GM is looking at the system as a whole. **(3) ROUTES.** `transit/telemetry.ts` already carries `HazardEvent[]` (`:14`) and already pushes hazards for rings and local dangers (`:85-142`), so a leg crossing a belt raises one through existing plumbing. **Do better than a binary crossing flag if it is cheap:** dose is an integral over TIME SPENT INSIDE, so a fast flyby and a slow spiral through the same zone are very different, and the telemetry already knows the leg's timing. An accumulated dose for the transit is far more useful at the table than "route passes through radiation". **Sequencing:** wants [[B20]] at least designed first, since the decomposition defines how a belt contribution is named and surfaced, and a route hazard should speak the same language as the body's own radiation block. **THE HOST ITSELF TAKES NO BELT DOSE, AND THAT IS THE CASE THIS ITEM HAS TO ANSWER.** Asked directly 2026-08-01 — "are we forgetting to apply it to itself?" — and the answer is yes, structurally: `beltParticleFlux` (`radiation.ts:71`) resolves its host as `body.parentId` (`:84`), so a PLANET'S host is its STAR and a giant is never inside its own belt. Jupiter consequently reads 11.5 mSv/yr, lower than Mars. For the quoted 1-bar "surface" that is roughly the right answer anyway, since the overlying atmosphere would absorb the particles — but it is right by EXCLUSION rather than by shielding, and the distinction matters here. **A ship skimming Jupiter's cloud tops is inside the belt and currently reads as safe, and Jupiter carries a `frontier/gas-skimming` tag**, so this is a real route-planning case rather than a theoretical one. Whatever zone model this item builds must treat the host's own upper atmosphere as INSIDE the hazard, not outside it — the zone starts at the cloud tops, not at the host's orbit. Related: [[B17]], [[B20]], [[A9]]. | captured, feature |
| B22 | **A GIANT IS NEVER INSIDE ITS OWN BELT: Jupiter reads 11.5 mSv/yr, less than Mars, at the centre of the most intense radiation environment in the Solar System.** Reported 2026-08-01 as "radiation is super high on Io but lowish on Jupiter — are we forgetting to apply it to itself?" Yes, and structurally rather than by a tuning error. **VERIFIED:** `beltParticleFlux` (`radiation.ts:71`) resolves its host as `body.parentId` (`:84`), so a PLANET'S host is its STAR. Jupiter's belt term is therefore evaluated against Sol — 1610 solar radii away, where the exponential is zero — and Jupiter's own 4.32 G field never irradiates anything but its moons. **The shipped law already gives the right answer; it is simply never asked.** Run it at Jupiter's own cloud tops: r = 1 R_J against `lambda = 1.6324 R_J` gives `exp(-0.613) x 1451.1 = about 786 Sv/day`, roughly twenty times Io's 36 Sv/day — which is correct, because Io orbits at 5.9 R_J, well outside the belt peak. Nothing needs calibrating; the host case needs including. **THE SECOND, DEEPER FAULT, and it should be settled before the first is patched:** quoting a SURFACE radiation for a gas giant is a category error, and this engine has already ruled on exactly that reasoning — [[B18]] shipped at v2.1.339 on the grounds that "a gas giant has no surface, so it does not score on a surface axis". The same argument applies here. The figure a GM needs for Jupiter is not a surface dose but the environment where things actually GO: the cloud tops and orbital space. So the fix is not merely "evaluate the belt at r = 1 host radius" — it is deciding what radiation figure a surfaceless body should report at all, and at what reference altitude, then making the belt available there. **Ties directly to [[B21]]:** a ship skimming Jupiter's cloud tops is deep inside the belt and currently reads as safe, and Jupiter carries a `frontier/gas-skimming` tag, so this is a live route-planning case rather than a curiosity. **Do this with or before [[B20]]** — a decomposition that cannot express "this body has no surface, here is the dose at the 1-bar level and here is the dose in orbit" will need reworking as soon as this lands. **DECIDED 2026-08-01: report BOTH, LABELLED.** A surfaceless body quotes two named figures — the 1-bar reference level AND the cloud-top / orbital environment — because the ambiguity is real and hiding it behind one number is what produced this report. **Build it as the general two-figure shape that [[B20]] will extend, NOT as a giant-specific special case**, or B20 rewrites it immediately. **STOP: THE OBVIOUS IMPLEMENTATION MAKES EARTH LETHAL. Read this before writing any code.** If the fix is "let a body evaluate the belt against its OWN field at r = 1 host radius", run the shipped law on Earth's own numbers: `B/B_ref = 0.5014/4.32 = 0.116` so `(B/B_ref)^2 = 0.0135`; spin ratio `9.925/23.934 = 0.415`; `lambda = 1.6324 x 0.116^(1/3) = 0.797 R_E`; `exp(-1/0.797) = 0.285`. With `D0 = 1451.1 Sv/day` that is **2.31 Sv/day at Earth's surface**, and even after atmospheric attenuation (`exp(-7.09) = 8.3e-4`) it is about 700 mSv/yr against a real background of 2.4 mSv/yr — nearly 300x too high, on the single best-calibrated body in the engine. **The physics that the law is missing is an INNER EDGE.** Real radiation belts do not reach the surface: the atmosphere absorbs trapped particles into the loss cone, which is why Earth's inner belt begins around 1.2 R_E rather than at the ground. A bare `exp(-r/lambda)` has no lower boundary and will happily report the belt peak at the centre of the planet. So B22 is NOT "evaluate at r = 1" — it needs a belt inner boundary, below which the trapped population is absorbed rather than merely attenuated. That boundary is also what makes the cloud-top figure meaningful rather than arbitrary. **Earth at ~2.4 mSv/yr and Mars at ~213 mSv/yr are the regression tests; neither may move.** Related: [[B17]], [[B18]], [[B20]], [[B21]], [[A33]]. **FIXED v2.1.341-beta.** **Root cause in a sentence: `beltParticleFlux` resolves its host as `body.parentId`, so a PLANET's host is its STAR** — Jupiter's belt was evaluated against Sol at 1610 solar radii where the exponential is zero, and its own 4.32 G field irradiated nothing but its moons. The law was already right; it was simply never asked. **THE INNER EDGE IS THE PART THAT MATTERS, and the warning in this entry was correct — the obvious implementation does make Earth lethal.** A bare `exp(-r/λ)` has no lower boundary, so asked about a body's own field it reports the belt PEAK at the centre of the planet: **2.31 Sv/day at Earth's ground, ~300x the real background**, on the best-calibrated body in the engine. **What it derives from, and it is not a picked constant:** real belts stop well above the surface because the ATMOSPHERE absorbs trapped particles into the loss cone — a particle whose mirror point lies in dense air is gone within one bounce rather than merely attenuated. So the boundary is a property of the atmosphere, and the engine already derives the only quantity it needs: **`atmosphere.scaleHeightKm` (H = RT/gM)**. The edge sits a fixed number of SCALE HEIGHTS above the reference level, which makes it scale with the AIR rather than with the planet — a puffy hot atmosphere holds its belt further out, a thin one lets it closer, and an **airless body has no absorber at all so its belt reaches the ground** (which is why Ganymede's poles are scoured by precipitating particles). The scale-height count is rule-pack DATA, calibrated on the one inner edge that is well measured — **Earth's, near 1.2 R_E, giving 150 H**. Jupiter is then a CHECK rather than a fit: 1.048 R_J, 764 Sv/day, about 21x Io, which is the region this entry independently predicted. **TWO NAMED FIGURES, built general rather than giant-specific so [[B20]] can extend it.** `surfaceRadiation` is the ground, or for a surfaceless body the 1-bar reference level; `orbitalRadiation` is the environment above the atmosphere, where a magnetised body sits inside its own belt. **Every body gets both** — it is just as true of Earth, whose orbital space contains the Van Allen belts and whose surface does not. Labelled in the stat block: "Radiation (surface)" against "Radiation (at 1 bar)", with the orbital row printed only where it genuinely differs. | body | 1-bar / surface | in orbit | inner edge | |---|---|---|---| | Jupiter | 11.4655 mSv/y | **747.2 Sv/day** | 1.0499 R_J | | Saturn | 5.0929 mSv/y | 0.4859 Sv/day | 1.1219 R_S | | Uranus | 2.6641 mSv/y | 0.3500 Sv/day | 1.1740 R_U | | Neptune | 2.2773 mSv/y | 0.1083 Sv/day | 1.1240 R_N | | Earth | 2.2990 mSv/y | 1.789 Sv/day | 1.1982 R_E | **Jupiter/Saturn at the cloud tops is 1538x where the field ratio alone is 18x** — far lower rather than proportionally lower, because the B² intensity and the tighter B^(1/3) scale length compound, exactly as [[B17]] predicted they would. **EVERY REGRESSION ANCHOR IS UNMOVED TO THE LAST DIGIT: Earth 2.29899, Mars 213.692, Io 1.27501e+7 (34.93 Sv/day), Europa 1.43732e+6, Ganymede 39657.2, Callisto 54.9044.** And **the derived diff is PURE ADDITION — 68 insertions, 0 deletions**, only `orbitalRadiation` and `beltInnerEdgeRadii` on all 34 bodies. Not one existing value moved, because the inner edge absorbs the self-belt at r = 1 for every body that has an atmosphere. That is the strongest form the "must not move" test could have taken. **One bug caught by MEASUREMENT rather than review, worth recording because it would have looked plausible:** `components.particle` already carries the belt terms from B17, so the first orbital figure double-counted the host belt and every Galilean read **exactly 2.000x** its surface dose. An exact factor of two is the giveaway. The stellar particle component is now separated out before deflection, and the belt terms are deliberately NOT magnetospherically deflected in the orbital figure — a body's own field does not shield it from the belt that field is holding in place. **NOT DONE, said plainly.** (a) [[B20]]'s full by-source decomposition, which this was shaped to accept rather than pre-empt. (b) **Saturn's real inner edge is set by RING absorption, not by its atmosphere** — its rings sweep up trapped particles, which is why its belts genuinely start near 2.3 R_S rather than the 1.12 R_S this model gives. The consequence is that Saturn's cloud-top figure is an over-estimate; it is still ~1500x below Jupiter's, so no conclusion here rests on it, but a ring-absorption term is the obvious next refinement and it ties to [[B26]]. (c) The old B22 (a ring carrying a surface dose) collided with this entry's number and is renumbered [[B26]]. | fixed v2.1.341-beta |
| B23 | **CHECKED, NO CHANGE RECOMMENDED: should stellar radiation be derived from stellar spin and field now that planets have both?** Asked 2026-08-01 — "we abstracted it to 1, but now we have spin and magnetic fields we may be able to do that more accurately, or happy to stay as is". Recorded so nobody re-opens it without new information. **The premise does not hold yet: planets have DERIVED spin and field, stars do NOT.** [[B9]](b) established there is no stellar rotation model anywhere, and a star's `mag_gauss` is a rule-pack RANGE rolled at generation rather than derived from anything. The ingredients for a spin-driven stellar model are exactly what the banked HR-diagram phase would provide. **And the current model is considerably better than "abstracted to 1" suggests.** `radiationOutput` is rule-pack DATA per class, not a constant — `static/rulepacks/starter-sf/stars.json` carries G `[0.8, 1.2]`, F `[1.5, 3]`, K `[0.5, 1.5]`, M `[0.8, 1500]`; Sol reads 1 because it is the reference. `photonParticleSplit` (`radiation.ts:28-43`) already varies by spectral class, O/B 95:5 through M 78:22, which is the right shape since cool dwarfs are wind- and flare-dominated. `flareActivity` is already DERIVED from class and system age (`SystemProcessor.ts:79`, `physics/stellar-evolution.ts`), already feeds the particle channel via `FLARE_PARTICLE_WEIGHT` (`radiation.ts:144`), and already raises `hazard/flaring` above 0.4. **THE ARGUMENT FOR LEAVING IT, which is the part worth keeping:** real stellar activity IS rotation-driven, but stellar rotation is itself a function of class and age — stars spin down by magnetic braking, which is precisely WHY the age-activity relation exists (gyrochronology runs the same inference backwards). So `flareActivity(class, age)` is already that causal chain with the intermediate elided. Inserting spin would derive spin from class and age, then derive activity from spin, and arrive at the same number: more mechanism, no more accuracy. **Where it WOULD genuinely gain, and the trigger for revisiting:** close binaries tidally spun UP and therefore hyperactive far beyond their age (RS CVn systems run orders of magnitude above the age prediction), and young systems before rotation converges. Both are real and both are currently invisible — and both need the HR-diagram work regardless. **Revisit with the HR phase, not before**, at which point stellar spin becomes a derived property worth showing a GM in its own right. Related: [[B9]], [[project_sse_v2_hr_stellar]]. | checked — no change, revisit with the HR phase |
| B27 | **`orbitalRadiation` names a place it does not describe: Earth's reads 653 Sv/yr, which would tell a GM that low orbit is instantly fatal.** Noticed 2026-08-01 while verifying [[B22]], which is otherwise clean. The figure is `652,964.7` mSv/yr for Earth. That is defensible AT THE BELT INNER EDGE — the inner proton belt genuinely is a lethal place to linger — but "orbital radiation" reads as "the dose in orbit", and the ISS at 400 km takes about **150 mSv/yr**, roughly four thousand times less, because it flies BELOW the inner belt except over the South Atlantic Anomaly. A GM planning a station or a parking orbit is being told the wrong thing by four orders of magnitude. **Same fault class as [[A33]]**, which was the same week: a number correct for a reference point that its label does not name. It is not a physics error and [[B22]]'s numbers should not be re-derived — Earth's inner edge at 1.1982 R_E matches the measured ~1.2 R_E, and Jupiter's 747 Sv/day at the cloud tops is right. **It is a question of WHERE the second figure is quoted, and it needs answering per body type rather than globally.** For an airless body the orbital and surface environments genuinely coincide (Io's two figures agree, correctly). For a giant, the cloud-top figure is the useful one and is what was asked for. For a MAGNETISED body with an atmosphere, the interesting number is not the belt peak at all — it is the dose in a LOW orbit beneath the belt, which is where anything actually parks. Options: name the reference altitude in the label; quote a below-the-belt figure for magnetised bodies; or give the belt peak its own clearly hazardous name and keep "orbital" for somewhere a ship would sit. **Whatever is chosen must survive [[B20]]**, which will decompose these figures by source and cause and needs each one to mean a definite place. Related: [[B22]], [[A33]], [[B20]], [[B21]]. | captured |
| B28 | **Io is tagged `Surface · Irradiation: low` while taking 12,750 Sv/yr — the tag is bound to a space-weathering proxy, not to the hazard.** Reported 2026-08-01 after [[B17]]/[[B22]] landed: "the tag generation system hasn't caught up — are your tag rules bound to the base solar radiation values rather than the newly implemented [belt]?" Yes. **VERIFIED:** `deriveIrradiationDose(teqK, magShield, surfaceAgeGyr)` (`radiation.ts:17`) takes exactly those three arguments and never sees `surfaceRadiation`, so it cannot know a belt exists; `SystemProcessor.ts:862-868` buckets its output into `surface/irradiation` at thresholds 0.05 / 0.2. **BUT THE QUANTITY IS NOT WRONG AND MUST NOT BE "FIXED" BY FEEDING IT THE BELT.** Its own comment states its purpose: it is the driver of THOLIN darkening (`docs/dev/geo-foundations.md` Foundation 4) — cumulative space weathering, stellar UV plus a galactic-cosmic-ray floor, times the unshielded fraction, times how long the surface has been exposed. Io's value is 0 because `ageFactor = surfaceAgeGyr / 4.5` and Io is resurfaced continuously by volcanism, so its surface has accumulated no weathering at all. For what it measures, zero is CORRECT. **The fault is that an APPEARANCE DRIVER is being published as a HAZARD READING.** A GM reading "Irradiation: low" on the most irradiated surface in the Solar System is being actively misled, and a player standing there dies. The user-facing tag should be bucketed off `surfaceRadiation` — which now includes the belt — while `irradiationDose` keeps driving tholins under a name that says what it is. Check Europa too: it reads 0.002 and is tagged low while taking 1,437 Sv/yr. **A SEPARATE AND LEGITIMATE PHYSICS QUESTION, worth its own consideration rather than being bundled into the tag fix:** should the belt ALSO feed the weathering model? Europa's real surface discolouration IS radiolytic — sulphur from Io's torus, processed by exactly the particles B17 now models — so there is a genuine case that a magnetospheric term belongs in the tholin/weathering chain as well. That is a physics change with visual consequences and it needs its own before/after diff; the tag fix does not and should not wait for it. **THIRD INSTANCE OF ONE FAMILY THIS WEEK** — see [[A33]] (a relative ratio printed beside an absolute dose) and [[B27]] (the belt peak labelled as "orbital"). The radiation model has grown several distinct quantities and the presentation layer treats them as interchangeable. Whoever takes [[B20]] should settle the VOCABULARY once — what each quantity measures, where, and in what units — rather than fixing the three labels one at a time. Related: [[B17]], [[B22]], [[A33]], [[B27]], [[B20]]. | captured |
| B29 | **AUDIT: does every tag report the quantity its name claims? Sweep all 41 tag-emission sites.** Raised 2026-08-01 after [[B28]] — "audit and validate this won't happen more, or improve instructions?" Both; this is the audit half, and the instruction half is already in the standing rules at the foot of this file. **The surface is BOUNDED and countable, so this is one session rather than an open-ended sweep:** 41 `tags.push` sites — 32 in `core/SystemProcessor.ts`, then `physics/stability.ts` (2), `physics/surfaceTemperature.ts` (2), `physics/accrete-adapter.ts` (2), `physics/cloudDecks.ts` (1), `physics/resonance.ts` (1), `physics/reasonsToVisit.ts` (1). **ONE QUESTION PER SITE:** what quantity is this tag bucketed off, and is that the quantity a reader would think the tag describes? Record the answer for every site even where it is fine — a table of "tag / source quantity / what a reader assumes / verdict" is the deliverable, because the value is as much in the sites that are CLEAN as in the ones that are not. The next person should never have to re-derive this. **Precedent for expecting real finds:** the equivalent sweep on paired figures during [[A5]] turned up [[B8]], a genuine physics fault putting 105 of 420 bodies outside their own stated range. Three of this family are already known — [[A33]], [[B27]], [[B28]] — and they were each found by accident rather than by looking. **THE DURABLE HALF, and it is the point of doing this at all:** for every mismatch found, add a CONSISTENCY TEST rather than only fixing the label, because a fixed label drifts again the moment the underlying physics moves. The tests are cheap and concrete — for instance, no body with `surfaceRadiation` above a lethal threshold may carry `surface/irradiation: low`. That form of assertion, one per pair the audit finds, is what stops the whole family recurring. **Do NOT fix physics here.** If a tag is wrong because the quantity behind it is wrong, that is a new B item — write it up unnumbered and carry on. This audit changes labels, buckets and tests, not models. Related: [[B28]], [[A33]], [[B27]], [[A5]], [[B8]]. | captured |
| B10 | **The legacy random generator gives no body an axial tilt at all, so a whole generated system has no seasons.** Found while doing [[B9]]. `generateSystem` (the Generate button) produces planets with a rotation period but `axial_tilt_deg` undefined on every single body — measured across three seeds, 0 of 40 bodies had one. The WIZARD path does set a stellar tilt, but only inside `applyKnobBias`, which `generateSystemFromConfig` calls only `if (config.knobs && !config.emptyPlanets)` — so a wizard run with no knobs also produces none, and its PLANETS never get one on any path. The manual add-a-body route in `SystemView.svelte:478-505` is the only place that sets planetary tilt, by die-roll. This is not cosmetic: tilt drives the seasonal term in `surfaceTempProfile`, the `climate/monsoon` flavour tag in the cloud-decks design, and the disc/holo renderers' spin axis. Note the shape of the fix is already argued in `applyKnobBias`'s own comment — a star and its planets condense from one disc, so they start ALIGNED and misalignment is evidence of a violent past rather than a die-roll to make worlds look varied. Planetary obliquity is a different question again (giant impacts, Uranus), so it wants its own thinking rather than reusing the stellar spread. | captured |
| B11 | **Belts and rings get no derived physics at all, because `processClassification` returns early for anything that is not a planet or a moon.** Found while doing [[B9]]: the early return at `SystemProcessor.ts:539` gates the whole third pass, which is where magnetism, rotational deformation, geology, surface age and the reasons-to-visit hooks live. For a belt or a ring that is arguably correct and certainly harmless. For a STAR it is the reason `deriveMagnetism` never runs on one, which is worth knowing before anyone tries to point a stellar model at that call site — it is not wired up. Recorded so the next person does not re-derive it; probably a wont-fix as it stands, but the star half becomes live the moment [[B9]](b) is built. | captured |
| B12 | **The dynamo's slow-rotator cut-off has no anchor between Venus and Earth, and [[B7]] just made it load-bearing for ~40 bundled planets.** `magnetism.ts:122` kills an ordered dynamo below `rot < 0.12`, i.e. a day longer than about 200 h, calibrated on Venus (5832 h, effectively no field) with Earth (24 h, 0.5 G) at the other end. There is no measured body anywhere between, and the whole habitable-zone population of an M dwarf lands in the gap: tidally locked worlds rotating once every 9 to 50 days sit at 0.02 to 0.11, just under the cut. Before [[B7]] they had no rotation at all and took the `unknown -> 0.6` default, so the threshold never fired on them; now 41 of them read `magnetic/unshielded` and 12 `magnetic/tenuous`. **Deliberately NOT retuned** — there is no measurement to retune it against, and re-shaping a curve to satisfy an intuition is what [[B2]] warns about. But the real literature question is that dynamo strength scales with the ROSSBY number (rotation against the convective overturn time), not the period alone, so a slowly-rotating world with a vigorously convecting core can still sustain a field. If an anchor or a Rossby formulation is wanted, this is the item. Consequence to weigh first: magnetic shielding feeds atmospheric retention and the particle dose, so moving this moves habitability across a whole class of world. **NO CHANGE TO THE CONSTANT, and the anchor hunt is why. This is a B2-style wont-fix, not an unfinished item.** **Every Solar System body with a measured field and a known spin, against the gate (`rot < 0.12`, i.e. a day longer than ~200 h):** | body | period h | rotFactor | clamped | vs gate | measured | ours | |---|---|---|---|---|---|---| | Venus | 5832 | 0.0041 | **0.040** | suppressed | ~0 (<3e-5 G) | 0 | | Mercury | 1408 | 0.0170 | **0.040** | suppressed | **0.003 G — a real, dipolar, axially-aligned dynamo** | 0.003 | | Ganymede | 171.5 | 0.1399 | 0.140 | ok | **0.007 G — a real dynamo** | **0** | | Earth | 24 | 1.0 | 1.000 | ok | 0.5 | 0.5014 | | Mars | 24.6 | 0.976 | 0.976 | ok | 0 | 0 | | Jupiter / Saturn / Uranus / Neptune | 9.9-17.2 | — | 1.4-1.6 | ok | 4.3 / 0.2 / 0.23 / 0.14 | 4.32 / 0.24 / 0.21 / 0.22 | **TUNING 0.12 IS POINTLESS AND THAT IS THE ANSWER.** `rotationFactor` clamps to a floor of 0.04, and **BOTH bodies below the gate land ON that floor** — Venus and Mercury are IDENTICAL on rotation in this model and have opposite outcomes. What separates them is already `bigIronCore = mk.metal > 0.5`, i.e. COMPOSITION. So there is no measured body whose result would change if the threshold moved anywhere in (0.04, 1.0). **No anchor pins the constant, and moving it could not be validated against anything.** Rule 4 applied: left alone. **But the STRUCTURE is falsified by measurement, which is a different claim from "the number looks wrong".** Mercury turns once per 58.6 days — a raw rotation factor of 0.017, three times below the gate — and sustains a genuine self-sustained core dynamo. "Slow rotation prevents a dynamo" is contradicted by a Solar System body we can measure. Venus, the sole body the gate was ever calibrated on, is explained in the literature by a lack of CORE CONVECTION (stagnant lid -> poor mantle cooling -> no inner-core nucleation), not by its spin. And the field-strength scaling in `nominalGauss = 0.5 * rot * sizeF` makes strength LINEAR in rotation, where the standard numerical-dynamo result (Christensen & Aubert 2006) is that strength scales with buoyancy flux and is essentially INDEPENDENT of rotation rate. Rotation properly sets GEOMETRY — dipolar at low Rossby number, multipolar at high — which the model already half-knows, since it flips `geometry` to multipolar in this very branch and then also zeroes the strength. **The engine already derives what a better model needs, and the ordering is easy** — unlike [[B5]]. `deriveMagnetism` is at `SystemProcessor.ts:788` and `deriveGeoActivity` at `:833`: same pass, 45 lines apart, no thermal-solve boundary between them, and geology reads nothing from magnetism (grep: 0 hits). `geoActivity.vigor` is the buoyancy-flux proxy and `geoActivity.regime` is the stagnant-vs-mobile-lid distinction that is the actual Venus story. **DO NOT SELL THAT REBUILD AS "GIVE THE 41 WORLDS THEIR FIELDS BACK" — measured, it probably would not.** Of the 43 bodies the model currently calls `suppressed`, **41 have a molten-iron core in our own fluid-layer model** and their geology is vigorous (24 episodic, 12 plutonic, 1 plate-tectonics, only 6 inactive; vigor up to 2.43). But **24 of them are `episodic`-lid, exactly like Venus** — so a regime-based gate would suppress most of them too, for a better reason. The honest expected outcome is a similar answer with a defensible mechanism, not a mass restoration. **One concrete anchored MISS, separately actionable and NOT about rotation: Ganymede.** Measured 0.007 G, ours 0. It sits ABOVE the gate at 0.140, so rotation is not the cause — it fails the molten-iron branch's `massMe > 0.3` test (Ganymede is 0.025 Me) and our fluid-layer model gives it no conductive interior layer at all. That is a CORE model defect and it has a real measurement behind it, which makes it the best available starting anchor for any rework. | wont-fix (the constant) — structure falsified by Mercury + Ganymede, rebuild scoped |
| B13 | **`process()` is not idempotent: surface radiation changes on a second pass, by up to 18%.** Measured on the Local Neighbourhood map, 156 bodies, comparing one pass against two: **37 bodies drift on `surfaceRadiation` (worst 17.5%) and 4 on `habitabilityScore` (worst 19.7%)**. Pre-existing, NOT caused by [[B7]] — verified against a detached worktree at the parent commit, where it is worse (55 bodies, 18.5%). **Root cause: `calculateSurfaceRadiation` reads `body.magneticField` in pass 2 (`processEnvironment`), and `deriveMagnetism` WRITES `body.magneticField` in pass 3 (`processClassification:774`).** So the first pass shields with whatever field was authored or left over, and every later pass shields with the derived one. Since `process()` runs on load AND after every edit, the dose a GM reads depends on how many times the system has been through — a freshly imported system and the same system after one nudge print different radiation. This is exactly the one-pass lag the v2.1.282 thermal unification removed, in a different quantity, and the standing rule that anything the solve reads must be committed before it runs applies here too. Likely fix is to derive magnetism BEFORE the environment pass (it needs makeup, mass and the fluid layers, plus `equilibriumTempK` for the giant helium-rain factor — so the same ordering question [[B5]] has, and possibly the same answer). An idempotence test belongs with the fix. | captured |
| B14 | **The `Testion-System.json` per-class fixture no longer demonstrates four of its classes, because their authored atmospheres put their surfaces far outside the band they are named for.** Fallout from [[B6]], and the fixture is the messenger rather than the victim. `t-21-ammonia-planet` has a 346 K surface (ammonia boils at ~250 K at its 1.5 bar); `t-23-earth-like` 346 K, i.e. 73 °C, from 1% CO2 — twenty-five times Earth's — against a body labelled habitable; `t-24-superhabitable` 371 K, 98 °C, with 80% ocean; `t-5-coreless` loses to `desert` on base-class exclusivity. These were only ever their named class because the fingerprints read equilibrium temperature and their equilibrium temperatures ARE in band. **The fix is to re-author the four bodies' atmospheres so their SURFACES land where intended — not to widen the bands**, which would put the contradiction back. Worth doing because a one-body-per-class fixture that silently stopped covering four classes is a test that has quietly stopped testing. Check the rest of the file at the same time: only these four were caught by a class CHANGE, and a body could be sitting mid-band for the wrong reason without moving. **FIXED v2.1.337-beta. In four of the five the AUTHORED DATA is wrong and the band is right; no band was touched.** - **ammonia planet — DATA WRONG.** 1.5 bar of pure NH3 is a strong greenhouse and put the surface at 346 K; ammonia boils near 250 K at that pressure, so the body had no ammonia hydrosphere to be named for. The 150-280 K band is right. Pressure to 0.2 bar -> **249.2 K -> `planet/ammonia-planet`**. - **earth like — DATA WRONG.** 1% CO2, twenty-five times Earth's, for +92 K and a 73 °C surface. The 250-315 K band is right — Earth's 287 K sits mid-band. CO2 to 400 ppm -> **289.6 K -> `planet/earth-like`**. - **superhabitable — DATA WRONG**, same cause: 371.4 K (98 °C) with 80% ocean that would be near boiling. CO2 to 400 ppm -> **316.4 K -> `planet/superhabitable`**. - **swamp — DATA WRONG, and the mirror of [[B15]].** The body is 1.00 Me, 1.00 Re, Earth's exact N2/O2 at 1.1 bar with a biosphere — it IS Earth, so earth-analogue beating it was correct. A swamp world should be WETTER than Earth: coverage 0.8 -> 0.90, inside swamp's 0.6-0.95 and outside earth-analogue's 0.5-0.85 cap. Now `planet/swamp`, and it no longer competes with the Earth twin. - **coreless — NEITHER.** Its 268 K dry surface is legitimately a desert AND its interior is legitimately coreless; both are `kind: base` and bases are mutually exclusive. Not a band error and not a data error, but a taxonomy question — a STRUCTURAL claim competing in the same pool as surface claims. Left alone deliberately. **A FIFTH case found, diagnosed and deliberately LEFT, because the diagnosis is the deliverable.** `forest` classifies as `planet/silicate` and has done since before this session: at 268.9 K its water is frozen, so `liquidCoverage` is 0 and the fingerprint is disqualified outright. **It is not fixable with the CO2 knob** — no CO2 gives 268.9 K, 100 ppm gives 295.1 K and 400 ppm gives 306.8 K, jumping clean over the 273-292 K window it needs. That is the **ice-albedo bistability**: bright ice and no vapour below freezing, dark water and vapour feedback above — the standing "a bright condensate makes the solve bistable" rule showing up in a fixture. It needs an ORBIT change, not an atmosphere nudge. Reverted to as authored. **Sol's derived output is unchanged, correctly** — Testion is a separate example file and no bundled starmap body is involved. **The wider finding, captured not fixed: the fixture is broken far past the four named.** At least eight more bodies do not classify as the class they are named for, nearly all pre-existing — `ocean` (416 K, boiled off), `earth analogue` (244.8 K, too cold), `barren`, `crater`, all three `eyeball` variants, `hot neptune`, `puffy` and `brown dwarf`. **A one-body-per-class fixture with no assertion per body will keep silently ceasing to cover things**; it wants a pass of its own that asserts each body classifies as its own name, which would have caught all of this the day it broke. | fixed v2.1.337-beta (4 of 5) — coreless is a taxonomy question, forest diagnosed and left |
| B15 | **`planet/swamp` out-scores `planet/earth-analogue` on Earth's own numbers.** Noticed while measuring [[B6]]. Earth's classification explanation now reads base `planet/swamp` at 2.10 with `planet/jungle` runner-up at 2.06 — a clean 1.0 on all four swamp bands (biosphere, surface 286.9 K in 278-300, water, liquidCoverage 0.71 in 0.6-0.95). It does not reach the screen, because Earth is hand-authored and keeps `planet/earth-analogue`; but a GENERATED Earth twin with a biosphere would come out a swamp world. The cause is weight, not bands: the biosphere types carry `weight: 1.5` and score `mean x (1 + 0.1n) x weight`, so four perfect bands at 1.5 beats five perfect bands at 1.0. Note the existing test 'classifies an Earth twin as the specific type' passes, so the synthetic twin in that fixture does NOT have a biosphere — which is why this has never shown up. Decide whether earth-analogue/earth-like should out-weigh the biome types (they are the more specific claim: a named Earth twin rather than a generic living world) or whether a living Earth genuinely IS a swamp world by this taxonomy. Cheap to change, so worth being deliberate about rather than nudging a weight. **FIXED v2.1.336-beta. NEITHER band is at fault — it is the WEIGHT, and that is the finding.** earth-analogue's five bands are the most demanding in the pack (0.8-1.3 Me, 0.9-1.15 Re, 10-35% O2, 255-300 K, 50-85% liquid water) and Earth matches all five perfectly. swamp's four are not generous either: Earth genuinely is a warm, waterlogged, living world. Score is `(mean fit) x (1 + 0.1 x bands) x weight`, and **the specificity bonus was working correctly** — earth-analogue 1.5 against swamp 1.4. The weight overturned it. **Against the pack's own scale the omission is obvious**: subsurface-ocean 1.8, forest/jungle/swamp and cold-eyeball 1.5, carbon/sulfur 1.4, the exotics 1.3, and the generic types pushed DOWN (silicate 0.95, barren 0.85, crater 0.8) — while **earth-analogue, earth-like and superhabitable were the only distinctive world types in the set carrying no weight at all**, i.e. level with `planet/terrestrial`. Earth scored swamp 2.10 against earth-analogue 1.50. **Weight 1.45, and the value encodes a rule that can be stated in one sentence: an Earth twin outranks a biome label only when it matches Earth EXACTLY; miss any of the five and the biome describes it better.** 1.5 was tried first and is WRONG — it let a PARTIAL earth-analogue beat a PERFECT jungle. Testion's jungle body sits at 314 K against the 255-300 band (fit 0.689) and scored 2.11 against jungle's 2.10, which is exactly the pathology the scoring header says it was redesigned to prevent. 1.45 puts a perfect match at 2.175 (beats 2.10) and that partial at 2.040 (loses to 2.10). **Only earth-analogue is weighted** — earth-like and superhabitable are GENERIC habitables that a biome type legitimately refines, so they stay at 1.0. **Four bodies move, all correct.** Earth in all three copies of Sol (best-fit swamp 2.10 -> earth-analogue 2.17; its DISPLAYED class was already earth-analogue, being hand-authored, so this was only ever visible in the "why this type" block). Testion's "swamp" body -> earth-analogue, which is RIGHT and belongs to [[B14]]: it is 1.00 Me, 1.00 Re, Earth's exact N2/O2 at 1.1 bar with a biosphere — Earth with more ocean, not a swamp world. **Swept the other bundled Earth-likes as asked:** Diamedes, Reach and Uggi's main world unaffected; Pandora stays `planet/earth-like`; TRAPPIST-1 e/f keep authored classes. Derived diff is ONE body, ONE field — Earth's `classification` block. Raised while measuring: [[B16]]. | fixed v2.1.336-beta |
| B16 | **A PARTIAL match of a more-specific type can still out-score a PERFECT match of a less-specific one.** Found while fixing [[B15]], and it is the reason that fix uses weight 1.45 rather than 1.5. `fingerprintScore` is `(mean fit) x (1 + 0.1 x bands) x weight`, and the header comment says it was deliberately reshaped so "partial fits now drag the score down instead of padding it up" and so "a band-rich catch-all whose extra bands are barely-true edge slivers can no longer out-score a perfect match on fewer bands". That holds only while weights are equal. At weight 1.5, `planet/earth-analogue` with ONE band at fit 0.689 (Testion's jungle body, 314 K against a 255-300 K band) scored 2.11 against a PERFECT `planet/jungle` at 2.10 — the exact pathology, reintroduced by a weight rather than by a band count. Worked around per-type for now by choosing a weight that cannot do it, which is fragile: every future weight change has to re-derive the same inequality by hand. **The candidate rule is that no fingerprint with any band below full fit may out-score one with every band at full fit** — a tier ordering rather than a scalar comparison. Cheap to state, needs a sweep of all 70 fingerprints to confirm it does not reshuffle anything else, so it wants its own pass. | captured |
| B24 | **A stability verdict can print a reason that contradicts it.** Found while doing [[B19]]. Several bodies read, in one string, "...but a locked mean-motion resonance keeps their conjunctions away from the crossing point, so it stays stable" followed by "Predicted outcome: flung out". Both halves are correct in isolation: the resonance note comes from the ORBIT-CROSSING test, which correctly spared the pair, while the fate came from a DIFFERENT driver on the same body (usually `assessHostBindingStability` — "Orbit exceeds host's stable Hill sphere"). `mergeAssessment` collects every reason and lets the most severe driver own the fate, so reasons and fate can end up describing different mechanisms with nothing marking which reason produced the verdict. Seen on Bergamen and Hades in the Uggi example. **The fix is presentational rather than physical** — carry the reason that OWNS the fate, or group reasons by driver so the verdict sits with its own cause. Do not resolve it by dropping the resonance note; that note is doing real work and is the more interesting half. | captured |
| B25 | **Fifteen gas-dominated bodies classify as eyeballs.** Found while sweeping [[B18]]: of the 59 bundled bodies with `makeup.gas > 0.5`, ten are `planet/cold-eyeball` and five are `planet/hot-eyeball`. The three eyeball fingerprints key on `starTidallyLocked` plus a `SurfaceTemp_K` band and nothing else, so they never test whether the body HAS a surface to have a permanent dayside on — a tidally locked gas giant matches as readily as a tidally locked rock. The eyeball notes are explicitly about ground ("molten/dry dayside", "icy except the substellar point", "temperate oasis"), which is what [[B3]] and [[B6]] established when they moved the family onto surface temperature, so a makeup gate is consistent with the reasoning already applied to them. Cheap to fix — a `makeup.gas` band on the three fingerprints, as rule-pack data — but it WILL move classifications, so it wants the usual before/after diff rather than a blind edit. | captured |
| B26 | **A ring carries a "surface radiation" dose.** Noticed while doing [[B17]], and it predates it: `Jupiter's Rings`, `Saturn's Rings`, `Uranus' Rings` and `Neptune's Rings` all carry a `surfaceRadiation` figure, and the belt term has now made them the loudest numbers in the system (Jupiter's Rings 352 Sv/day, above Io). The number is not WRONG — a probe in the ring plane at 1.8 R_J really would take that — but a ring is distributed debris with no surface to stand on, so "surface dose" is the same category question [[B18]] answered for gas giants: does this axis mean anything for this kind of body? Note the two differ in an important way, though, and that is the reason this is captured rather than fixed by analogy: a giant's envelope has no place to stand at ALL, whereas a ring is made of countless small bodies that each DO have a surface, so the honest answer may be that the figure is meaningful and only the LABEL is wrong. Decide which before changing anything. | captured |
| A8 | **The rest of the GM starmap's scaling text.** A4 fixed the system names, the depth annotations and the placement ghost. Still inside the world transform and still growing with zoom: the ROUTE labels (`.route-label` / `.route-name`, positioned along a line, so they need the same treatment applied to a transform rather than an x/y pair), the measure read-out, and the `+` indicator on a system with more than four stars — that last is arguably part of the marker glyph rather than a label and may be right as it is. Same one-line fix per label; `labelK` already exists in `Starmap.svelte`. Left out of A4 to keep that change to what was reported. **Done for the route distance, the route name and the measure read-out; the `+` is deliberately NOT done.** The route name needed no special handling in the end: its `rotate()` is about the midpoint, so scaling the y-offset keeps the name the same distance off the line at every zoom and the transform is left untouched — the worry in the entry above about "a transform rather than an x/y pair" did not materialise. **The `+` call, with the reason:** the star discs it annotates are plain `r={5}` circles at fixed world offsets with no counter-scale, so the whole marker glyph grows with the map. The `+` is typography belonging to that glyph, not an annotation of the map, and pinning it to a constant screen size would make it drift off its own marker at high zoom and swamp it at low zoom. It scales because the thing it belongs to scales. Verified by measuring on-screen bounding boxes across a 4.9x zoom range (scale 0.397 → 1.94 → 0.397): route label, route name and measure read-out are pixel-identical at every step while the route line goes 189 → 706 px and the measure line 144 → 706. | fixed v2.1.310-beta |
| A9 | **True scale cannot be both visible and in proportion, and the dial should probably say which it means.** Raised by F2/F3. At true scale Earth's rendered radius at whole-system framing is about 0.05 px — genuinely true is genuinely invisible, so some floor must exist, and any floor puts bodies out of proportion with their orbits. v2.1.288 keeps a floor at 2% of readable, which stops the belt burying the planets without making the planets honest. If a real "true proportions" reading is wanted, the options are (a) a SCREEN-space floor — a body renders at its true size whenever that exceeds about 1 px and never vanishes below it, costing a per-frame rescale, which is what the construct glyphs already do; or (b) floor the SYSTEM rather than each body, scaling everything by one common factor so relative sizes survive the magnification. Neither is a bug fix; both are a decision about what the dial promises. **Resolved by taking option (a), across v2.1.297-303.** Commit trail: a screen-space visibility floor so planets are drawn at all (297); framing a body at true scale no longer clips it through the near plane (299); "true scale means true" — real radii, pixel floors, camera unclamped (300); the focus ease now expires on arrival rather than after a fixed frame count (301); the planet sits ON its orbit line, rocks are planet-sized and moons are reachable (302); and the Spread and Body-size dials carry a green "actual" pip marking where the setting is physically honest (303). The pip is the answer to "the dial should say which it means" — rather than renaming the ends, it marks the one point on the travel that is true and lets the rest be readable. Verdicts here are from commit subjects and the live session, not a diff read. | fixed v2.1.297-303-beta |
| A10 | **The legacy Field Guide skins read a GM-local store.** From the A3 investigation. The old console skin renders `SystemVisualizer`, whose promotion to the feature-rich `PlanetDisc` overlay is gated on `get(trueColorMode)` — a store written only by the GM's own system view. It defaults to true so it usually looks right, but a GM who turns true colour off in their orrery silently strips surface features from every player view on that browser. Also from that dig: `DISC_OVERLAY_MIN_R = 11` px means nothing is promoted at whole-system zoom even in the GM view, so the surface-feature work is invisible in the most-used view — the threshold guards real cost (an SVG overlay per body), so do not lower it without measuring frame rate on a busy system. Lower priority than it reads: the Field Guide rail entry sits behind `PLAYER_VIEWS_ENABLED` as of v2.1.287, so on beta these paths are reachable only by URL. | captured |
| A11 | **The picker should float, on every screen, GM and player alike — the way the time control already does.** It currently sits as a fixed strip at the top of the view, which is why A6 calls it "sometimes ineffective": it takes permanent space and is not where the eye is. The model to copy is the collapsed time puck in `src/routes/catalogue/+page.svelte:1067` — a `.time-controls` element with an `expanded` flag and `use:clickOutside` to close, i.e. a puck that opens on demand and gets out of the way. **One component, six mount sites**, all of `BodyPicker.svelte`: GM starmap (`Starmap.svelte:1062`), GM system view (`SystemView.svelte:2193`), transit planner (`TransitPlannerPanel.svelte:942`), the main route twice (`routes/+page.svelte:1589` and `:1747`) and the player catalogue (`routes/catalogue/+page.svelte:1054`). Do the floating behaviour INSIDE `BodyPicker` behind an opt-in prop rather than at six call sites, or the next surface reintroduces the strip. Note the transit-planner mount is inside a panel, not over a map — it likely wants to stay inline, so the prop needs to default to the current behaviour. `SystemView.svelte:2722` already has `position: relative` to "anchor the BodyPicker + time overlays", and `:2738` documents the z-order (the eye sits above the picker at 60) — that stacking is the existing constraint to respect. **Built as an opt-in `floating` prop on `BodyPicker`, default OFF — and only THREE of the six mounts wanted it.** The other three are already `inline`: the transit planner is a form field, and `routes/+page.svelte:1589`/`:1747` are `inline startOpen` inside the dedicated "All bodies & constructs" and "Constructs" dialogs, where the picker IS the modal's content. None of those is a strip over a map, so the complaint does not apply to them and they are untouched (verified: the find-a-body dialog still renders the same 495 px inline strip, list open). The three that float are the GM starmap, the GM system view and the PLAYER catalogue. The puck keeps the strip's own anchor rather than inventing a position, so every host's existing placement CSS still works — the catalogue's `.holo-picker-left` override puts its puck at left 10, the two GM mounts stay top-centre at `top`. One trap worth recording: the collapsed container must be `width: auto`, or a 420 px transparent box goes on sitting over the map swallowing clicks. Dismissal is outside-click, Escape, a pick, or the host changing focus; the outside-click listener has to survive the dropdown being shut, since that listener is what puts the puck away. Verified live on all three: puck 42 px → 420 px strip with a working list, no overlap with the SystemView eye button (strip right 723, eye left 813), and the player mount exercised in a real live player view over the broadcast. **Does NOT share a root with A13**, despite both being "floating chrome on the starmap": A13 was an ancestor `overflow: hidden` throwing away growth the panel had already done, A11 is a shape change in a different component with no persisted state at all. They were done in one session but they are two unrelated fixes. **FOLLOW-UP v2.1.311 on review: "floating" was only half of it — the picker and the time transport had to become the SAME control, not two that resemble each other.** The behaviour now lives in `src/lib/ui/floatingControl.ts` with two shared chrome components (`FloatGrip`, `FloatPin`), and both the transport and the picker consume it: slim move handle left, lock right, collapse to a puck, put itself away on the first pointerdown anywhere else unless locked. The lock REPLACED the transport's minimise button and had to keep both meanings, so unlocking also puts the control away — otherwise the one button a GM already knew stopped doing what it did. Defaults chosen to change nothing unasked: the transport starts locked open (its previous always-on state), the picker starts as a puck. **Three things only the live pass found.** (1) `didDrag()` was STICKY — set on a grip drag and never cleared, so a later click that fired no pointerdown (keyboard activation, or the same flag read by a different grip) inherited the old verdict and refused to open the puck; it consumes the flag now. (2) The picker's drag offset sits on an INNER element, because the outer box is what each host anchors (the catalogue pins it left, the GM mounts centre it) — and a parent's `getBoundingClientRect()` is unaffected by a transformed child, so the on-screen clamp measured the wrong rectangle and did nothing. `float.root` had to move onto the shifted element. (3) Nothing clamped on LOAD, only during a drag: an offset dragged on a desktop opened a phone with no puck on screen to grab. Now clamped on mount and on resize — verified by storing dx 900/1100 and reloading at 375x812, both pulled fully inside. The grip hides while locked (nothing to drag, and 15 px of pill back on a narrow phone); a locked control is repositioned by unlocking, dragging the puck, and locking again. **SECOND follow-up v2.1.312, from the next look.** (a) The picker GREW SYMMETRICALLY: the host centres it with `translateX(-50%)`, so expanding from a 41 px puck to a 420 px strip moved the left edge 190 px left and the puck slid out from under the pointer into the middle of the bar. The time transport never had this because it is `inline-block` anchored bottom-left and simply grows right. Fixed with one rule — a floating picker anchors its LEFT edge (`transform: none`) — which also matches the catalogue's existing left-anchored override instead of fighting it. The lone puck now sits a puck's-half right of true centre, which reads as centred. (b) Nothing re-clamped when a control CHANGED SIZE, only on mount, resize and mid-drag — so opening near an edge ran straight off it. Now re-clamped on open, on lock and on drag release. **Two traps inside that:** the clamp was scheduled on `requestAnimationFrame`, which is SUSPENDED while the page is not painting (minimised, background tab) — so a control opened off-screen while hidden was still off-screen when you came back; it is a macrotask now, which also made it testable in the automated pane, where rAF is exactly as unreliable. And the in-drag clamp reads a rect one frame behind, which converges over a slow drag but lets a FLICK that ends at the edge stop just past it — hence the extra clamp on release. Validated at 375x812 by flicking both controls into the bottom-right corner and opening them: puck 330-371 opening to a 351 px strip at 20-371, clock 343-371 opening to a pill at 52-371, all wholly on screen. | fixed v2.1.308-beta, v2.1.311-312-beta |
| A12 | **When "Ignore depth when measuring distances" is on, the GM starmap should stop showing z-axis information.** If depth does not count, the depth annotations are noise on the map. The setting is a single campaign flag — `Starmap.ignoreZForDistances`, written by the checkbox at `SettingsModal.svelte:345`, and there is already ONE predicate for reading it: `zCounts()` at `src/lib/map/systemDistance.ts:22`, whose comment says depth counts unless the campaign opts out. Use that, do not read the flag directly. What to hide in `Starmap.svelte`: the `.depth-label` WS7 depth cue on each system marker (~1382-1397, the signed "above/below the plane" text) and the same cue on the placement ghost (~1406). **Judgement call to make explicitly, not silently:** the "Set Depth…" context-menu item (~1470) should probably STAY — the depth data still exists and the campaign can turn the setting back on, so hiding the editor would make the value uneditable rather than merely uncounted. Say which way it went in the commit. Check the 3D starmap too: this is stated as a GM-starmap item, and a genuine 3D view showing depth it is not counting is a different question — do not change it without asking. **Done as written: both cues gated on `zCounts(starmap)`, nothing reads the flag directly.** Verified live by round-tripping the checkbox on the Science Fiction map — 41 depth cues, 0, 41 — with the system names untouched at 42 throughout. **The judgement call, made explicitly: the depth EDITORS stay.** "Set Depth…" is still on the star context menu with the setting on (verified), and so is the elevation control in the "Add System near here…" placement dialogue, which is the same class of thing and was not in the original write-up. The reasoning is the one the entry suggested: the z value still exists and the campaign can turn counting back on, so hiding the editor would make depth uneditable rather than merely uncounted — and an editor is not an annotation, which is what the observation was about. The 3D starmap is untouched; see A14 for why that is a separate question rather than an oversight. | fixed v2.1.309-beta |
| A13 | **The starmap Description box shows about 20 words and its resize grabber does not genuinely resize.** `StarmapInfoPanel.svelte` — the desktop draggable panel holding Description + GM Notes. Editing in situ is right and should stay (no pencil-icon mode was wanted); the default panel size is right and should stay too. The ask is that the grabber at the bottom of the box actually makes the box bigger and shows more description. **Verified mechanism, three CSS rules fighting each other.** The textarea already has `resize: vertical; min-height: 56px` (~172-175) so the browser DOES draw the native grabber and the element does grow — but `.info-panel` is `max-height: 70%` with `overflow: hidden` (110-121) and `.info-body` is `overflow-y: auto` (152-158). So dragging the grabber makes the textarea taller INSIDE a fixed-height panel and the body simply becomes a scroll region: the visible window never changes, which is exactly "it doesn't resize". Compounding it, the panel is `width: min(320px, calc(100% - 24px))` and the description is `rows="3"` — 320 px x 3 rows is where the ~20 words comes from. **Fix direction:** let the PANEL follow its content instead of clipping it — grow with the resized textarea up to a viewport-relative ceiling — or give the panel its own real resize handle. Either way the size should persist the way the position already does (`pos.x/pos.y` are clamped into view and saved, ~26; size belongs in the same store, with the same clamp so a size dragged on a desktop cannot strand the panel on a phone). GM Notes stay always-editable and get the same treatment for free, being the same field pattern. **ROOT CAUSE, measured live: the panel is anchored by its TOP and defaults to the BOTTOM of the canvas, so the grabber grew it into CLIPPED space.** The triage above named `max-height: 70%` as the clip. That rule is real, but it is the SECOND clip and at the default position it never fires — so "the panel is a fixed height" was the wrong picture. Measured: canvas 956 px tall, panel top 648 (the default position is `parentH - panelHeight - 64`, deliberately leaving only the scale-bar clearance below it), and BOTH `.area.canvas` and `.app-shell` are `overflow: hidden` at 956. Dragging the description grabber out to 260 px put the panel bottom at 1093 — 137 px, including the whole GM Notes field, cut off — while `max-height: 70%` = 669 px was never reached. So the textarea WAS growing and the panel WAS following it; the result was thrown away by an ancestor. Fixed by giving the panel a real corner grip that sets a persisted `{w, h}` (`sse-starmap-info-size-v1`): the fields flex to fill it (Description grow 3, GM Notes 2, so the field that was too small takes most of the extra), the grip clamps the size to the canvas and pulls `pos` up/left so every pixel it adds is on screen, and size and position now share ONE clamp on mount — a 644 px panel dragged on a 956 px canvas reopens at 536 px, wholly inside, on a 560 px one. The per-textarea grabbers are gone (`resize: none`): two grabbers with different meanings was half the confusion, and the panel grip also widens, which the native one could not — 320 px was the other half of "about 20 words". Default panel is byte-for-byte unchanged; no size is stored until the grip is used. | fixed v2.1.307-beta |
| A18 | **Player view: a selected body's framing gradually retreated while time was running.** Reported 2026-07-31 — "the camera drifts away", "only if time is running", which is the whole diagnosis in five words: the body was moving and the camera was not. **Root cause, one line in `driveFocus` (`lib/holo/scene.ts`).** Once the focus ease expired, the FREE-HEADING (3D) branch did only `controls.target.lerp(desiredTarget, 0.08)` — it re-aimed at the body and left `camera.position` exactly where it stood, with a comment calling that "the natural hologram feel". Turning to track reads well for a second and then fails: a body orbiting AWAY from a fixed camera is further away every frame, so the shot silently retreated until the planet was a dot. Rotating in place cannot hold a distance; only travelling with the body can. Note the LOCKED-heading branch was always correct — it re-seats the camera at `target + heading * d` every frame — so this only ever affected rotatable 3D presets, which is every Holo Table. **Fix:** carry the target→camera offset with the body (`followOffset = camera.position - target`, copy the target onto the body, replace the camera at `target + followOffset`). Preserving the OFFSET is what keeps everything else working: dragging still orbits the body, the wheel still changes distance, the turntable still spins, and a re-click still re-frames through the ladder because that arms `focusDrive` and eases to the new distance above. The target is copied rather than lerped, matching the locked branch — a lerp lags a fast-moving moon off centre. **Verified live** on a broadcast player view (Altair IV focused, clock at 1 month/s): centre-frame ink held at 1131-1774 over ~11 simulated months with no downward trend, where the old behaviour collapsed it to ~12 as the body left frame. Confirmed visually by Alex on the same running view. | fixed v2.1.313-beta |
| A19 | **Player view at 1:1 scale AND 1:1 distances: small bodies far out jitter — the orbit line vibrates and a moon does not sit on it.** Reported 2026-07-31, using Pluto/Charon as the example, and explicitly flagged as edge-of-the-envelope: it needs BOTH dials at their honest end, which is the regime A9 opened up (v2.1.297-303). Not diagnosed yet. **Where to start.** The 2D orrery solved exactly this and the fix is documented — `SystemVisualizer` uses a **Floating Origin (relative camera)** pattern, doing its arithmetic in 64-bit JS relative to the current camera focus before handing coordinates to the renderer, precisely because large absolute distances lose precision in a 32-bit rendering matrix (see `docs/dev/ARCHITECTURE.md`). The holo scene has no equivalent: it compresses map/AU distances into scene units and then leaves three.js to build float32 matrices, so a body 40 AU out drawn at its true radius is a tiny object at a huge coordinate — the worst case for float32, and it would show up as exactly this, a vibrating orbit line and a satellite that will not stay glued to its primary. **Two candidates, and they are separable:** (a) float32 precision in the world/view matrices, which the floating-origin treatment fixes; (b) the true-scale screen-space floor (`updateTrueScaleFloor`, A9/v2.1.297+) resizing a sub-pixel body per frame, which could make the body and its orbit line disagree about where the centre is. Check (b) first — it is cheap to rule out by pausing the floor — before taking on (a), which is a real piece of work.  **CHEAP CANDIDATE RULED OUT, v2.1.316-beta — and by construction, not by sampling.** The A9 screen-space floor (`updateTrueScaleFloor`, `holo/scene.ts:1086`) writes `b.mesh.scale` and NOTHING else. Scaling a mesh about its own origin cannot translate its centre, so it cannot make a body and its orbit line disagree about where that centre is. Confirmed by checking every write to a body's position in the scene: they are all in `updatePositions` (:1866/:1868/:1871) and `updateSurfaceConstructs` (:1992), none of them in the floor. Nor can the floor cause the vibration: `k = minPx / pxR` sets the rendered radius to exactly `minPx`, so the pinned size is stable by construction rather than a feedback loop that could hunt. **So it is the expensive one.** The remaining candidate is float32 precision in the GPU's world/view matrices, which is exactly what a body 40 AU out drawn at its true radius provokes: a tiny object at a huge coordinate, where neighbouring vertices differ in the last bits a float32 has. That fits both symptoms at once — an orbit line whose vertices round differently frame to frame VIBRATES, and a moon placed by a separate per-frame path rounds differently again, so it will not sit on the line. It also fits the trigger: it needs BOTH dials at their honest end, because that is when scene coordinates get large while radii get tiny. **What the fix involves.** A Floating Origin, which the 2D orrery already does and documents — do the arithmetic in 64-bit JS RELATIVE TO THE CAMERA, and hand the renderer only small numbers (`ARCHITECTURE.md`, `SystemVisualizer`). In the holo that means rebasing the scene on the current focus: body positions, the static orbit-ring vertex buffers (which are built once in absolute scene units and would need rebuilding, or a per-object offset, when the origin moves), the belts, and anything else holding absolute coordinates. It is a real piece of work with a re-render cost, and it wants its own scoped session — NOT this batch.  **QUANTIFIED 2026-07-31 — the numbers say it outright.** At TRUE scale `compressScalar` is linear: `scene = GRID_RADIUS * r / rMax` = `12 * r / 39.5` for Sol, so Pluto sits at scene coordinate 12. float32 has a 24-bit mantissa, so the gap between representable values at magnitude 12 is `2^-20` = **9.54e-7 scene units**. Against that: Charon's orbit radius is 3.98e-5 scene units = **42 ULP** — a whole orbit quantised to about 42 steps per axis, which is why it will not sit smoothly on its line — and Pluto's own RADIUS is 2.41e-6 = **2.5 ULP**, i.e. the body is essentially inside the rounding error before the screen-space floor inflates it for display. Every camera nudge re-rounds, which is the vibration. It gets worse, not better, on a map whose outermost body is further out. **Rebasing fixes it by orders of magnitude:** with the origin on Pluto, Charon's coordinates are ~4e-5, where the ULP is 3.6e-12 — about **1e7 ULP of headroom** instead of 42. **Design notes for whoever takes it.** (1) There is ONE choke point: every absolute→scene conversion goes through `positionToScene` (`holo/scene.ts:725`, 8 call sites). (2) Rebase AFTER the compression, not before — `compressScalar` is a RADIAL, nonlinear map, so a translation does not commute with it; subtracting the focus's COMPRESSED scene position is a pure translation of the rendered space and composes cleanly. (3) Moon orbit rings are ALREADY safe: `buildMoonOrbitRing` stores vertices relative to the parent and the loop is positioned at the parent, so its numbers are small by construction. The absolute holders that would need re-emitting on a rebase are `buildOrbitRing` (heliocentric), the belts, and the grid. (4) The camera and `controls.target` rebase with everything else. **Verification it should be held to:** assert that Charon's RENDERED offset from Pluto matches the propagated offset at true scale — today it will not — and measure frame-to-frame stability of the orbit-line vertices across a slow camera move. Both are numeric, like C3's. **FIXED v2.1.319-beta — a floating origin, rebased on the camera's focus.** Root cause in a sentence: the heliocentric orbit rings and the grid are static float32 vertex buffers holding ABSOLUTE scene coordinates, and at true scale those are magnitude 12, where a float32 step is 9.5e-7 — so the line's SHAPE is quantised at that step, and the shader then adds that large vertex to a large, oppositely-signed modelView translation, a cancellation that re-rounds every time the camera moves. **A correction to the earlier diagnosis, and it changes what you should look at.** The MOON was never misplaced. three.js builds `modelViewMatrix` on the CPU in float64 and only then hands it to the GPU, so a body's centre arrives as a SMALL camera-space translation, accurate to ~1e-12; every body position in this scene is a float64 JS number all the way. So "a moon will not sit on its line" is the LINE being wrong, not the moon — which is also why the moon reads as steady while the line vibrates against it. The cheap candidate stays ruled out by construction: `updateTrueScaleFloor` writes `b.mesh.scale` and nothing else, re-confirmed in one pass over every write to a body's position. **Measured, against the prediction.** The float32 step at Pluto's scene coordinate 11.99 is 9.5367e-7 (2^-20), exactly as predicted. The Pluto-Charon SEPARATION is 3.98e-5 scene units = **41.7 ULP** — the predicted "42" is CONFIRMED, and it was quoting the pair separation; Charon's own orbit about the barycentre is 3.52e-5 = **36.9 ULP**. Pluto's radius is 2.41e-6 = **2.5 ULP**, also as predicted. Rebased, that same separation sits where the step is 3.64e-12: **1.09e7 ULP**, confirming "about 1e7". End to end: a vertex on the orbit line beside Pluto is drawn **3.8e-7 scene units** from where it belongs — 1.1% of Charon's whole orbit — and rebased it lands within **2.8e-7 OF** Charon's orbit. Over a slow 600-frame pan the absolute vertex holds only **148 distinct positions** and jumps a full ULP when it moves; that staircase IS the vibration. Rebased it holds 600, moving one camera-step per frame. **Where the notes left a choice — rebuild vs per-object offset — the answer is NEITHER.** An offset is the wrong instrument: it moves the object in float64 but leaves magnitude-12 numbers in the vertex buffer, so both the shape quantisation and the shader cancellation survive it untouched. A full rebuild re-propagates 1024 samples per ring, and for a belt would re-randomise the rubble field (`buildBeltBand` calls `Math.random`) — i.e. it would change how the scene LOOKS. The absolute holders instead keep a float64 MASTER copy of their vertices and re-emit from it: one array pass, the subtraction done in float64 and rounded once. The grid gets the same treatment — its outermost ring sits at rMax, which for Sol is Pluto's own distance, so it is as much a reference line as the orbits are — with its AU labels translated rather than re-made, since they are canvas textures. **Belts are the exception and stay ABSOLUTE.** Their rocks are baked host-relative and the group is re-anchored to the host's rendered position every frame, so they follow the rebase for free; putting them through the rebased choke point would count the origin twice. Moon rings, planetary rings and surface-locked constructs likewise need nothing — all three are placed from a parent's rendered position. **The policy.** Rebase once the camera's target has drifted more than 2000x the working distance from the origin. That multiplier holds the float32 error to 1.5e-4 of the view width (a fifth of a pixel at 1000 px), and it is also what keeps the whole thing invisible at readable scale: there the camera never comes closer than the controls' 0.05 floor, which puts the threshold at 100 scene units against an outermost body at 12 — so the origin never leaves zero and that end of both dials is bit-identical. Suppressed while a focus ease is in flight, so a fly-in re-emits once on arrival instead of on 48 consecutive frames. **What is NOT done.** The on-screen pass. The numbers live in `src/lib/holo/floatingOrigin.spec.ts` (15 assertions) and `npm run build` plus the full suite are green (only E1 fails), but the browser pane in the session that did the work could not composite frames, so nobody has yet LOOKED at Pluto/Charon at 1:1 scale and 1:1 distances, or panned slowly across the outer system. Read A22 before doing that — it explains a second reason the pair may not appear to sit on a line. | fixed v2.1.319-beta |
| A20 | **QUESTION, not a bug: the in-system ruler reports a PLAN-VIEW distance. Does that agree with what transit charges for the same trip?** The ruler has two modes and the rail button says so — `RailNav.svelte:129` reads "tap two bodies for the distance between them in AU" in the system view and "tap two stars or ships" on the starmap. A17 is the starmap half. This is the system half. **First written up as "the same shape as A17". It is NOT, and the correction matters.** A17 was a genuine amputation: the code intended a 3D answer, wired `zCounts()` correctly, and then lost z at the endpoint, so it returned a number nothing in the system agreed with. Here the planar answer is CONSISTENT with its surroundings. **Verified:** `SystemVisualizer.svelte:65-71` computes `rulerDistanceAU` as `Math.hypot(a.x - b.x, a.y - b.y)` from a `Map<string, { x, y }>` filled by `computeWorldPositions` — but that component references inclination NOWHERE AT ALL (zero occurrences), so the 2D orrery is a plan view uniformly and by design, not a 3D view that dropped a coordinate. `computeWorldPositions3D` (`physics/worldPositions.ts:111`, "inclination-aware… what the holo view uses") has exactly one consumer, `holo/scene.ts`, and the holo view has no ruler. Note also that orbital inclination has existed since day one, unlike starmap z which arrived with WS7 on 2026-07-30 — so a planar in-system ruler is far more likely to be a deliberate plan-view choice than an oversight. **The one thing that would make it a real bug is unresolved and needs checking:** whether the AU figure the ruler shows matches the distance the transit planner and journey durations actually charge for that pair. Neither transit nor journeys go through `computeWorldPositions` at all, so they derive distance somewhere else — find where, and compare on a genuinely inclined pair. If they disagree, the map contradicts itself the way A17 did and this becomes a fix; if they agree, this is correct as it stands and should be closed wont-fix with the evidence. **Do NOT wire this to `ignoreZForDistances`** — that is a STARMAP field described in interstellar terms in `systemDistance.ts`, and in-system inclination is not the same quantity as starmap depth. **Belongs with the in-system transit work, not the starmap batch.** | captured, needs one check |
| A21 | **Interstellar space has no depth: a ship stranded away from a system cannot record one.** Residual from A17, and a DATA-MODEL gap rather than a display one, which is why it was not fixed there. Two places store an interstellar position and both are 2D: a journey's POINT origin/destination (`ActiveJourney.fromX/fromY`, `toX/toY` — used when a course is replotted from where an adrift ship sits, or aimed at a stranded one) has no `fromZ/toZ`; and `AdriftConstruct` (`types.ts:644`) stores `x, y, vx, vy` and no z. So a ship stranded between the stars reads as being ON the reference plane, however deep the systems either side of it are, and a rescue course plotted to it measures to the plane. Everything downstream is already ready for the answer — `posZ`, `mapSeparation` and `ConstructPlacement` all handle z — so this is purely about recording it: add the fields, write them where the strand/replot happens, and carry them through serialisation. **Also open, and the reason a coasting drift has no `vz` today:** the fly-by slingshot deflects a coasting ship by rotating `vx/vy` through a planar angle (`flybyTurn`, `interstellar.ts`). Giving the drift an out-of-plane velocity means deciding what that deflection does in 3D, which is a transit-physics question. Until then a coasting ship holds the depth it had when it was stranded, which is at least continuous. | captured |
| A22 | **A barycentre member's orbit ring is drawn about the scene ORIGIN, not about its barycentre.** Noticed while reading `buildOrbitRing` for A19; deliberately NOT fixed there (out of scope). `propagateState3D` returns a PARENT-relative position and `buildOrbitRing` hands it straight to the projector — right for a planet, whose parent is the star at the origin, but Pluto and Charon are members of a root-level barycentre, so their rings come out as loops of radius 4e-6 and 3.5e-5 scene units AT THE SUN, inside the star's own globe, instead of around their barycentre out at 39.5 AU. At the eyepiece that means the only line near Pluto is the BARYCENTRE's ring, which the pair straddle by design — so part of "a moon will not sit on its line" is a ring that was never drawn where it belongs. The moon rings already solve exactly this: `buildMoonOrbitRing` stores vertices in the parent's local frame and `updateOrbitRings` positions the loop at the parent each frame via `trackParentId`. A barycentre member wants the same, tracked against its barycentre's rendered point. **FIXED v2.1.320-beta, and it was the answer to the reported symptom too.** Each member now gets a ring about its barycentre's rendered point, stored in that point's local frame and tracked to it every frame. The two local-frame builders (moon, member) are now ONE function taking a per-sample radial rule: a ring has to be laid down with exactly the transform the body's own placement uses or the body will not sit on it, and keeping the two rules side by side is what stops them drifting. Barycentres carry a ring but no mesh, so their scene points are now kept alongside the bodies in `updatePositions` — which also means they move with a floating-origin rebase like everything else. Nested pairs fall out for free (no partner radius, so no clearance, so pure physics). | fixed v2.1.320-beta |
| A23 | **An orbit ring is a 1024-gon, and past a certain zoom you are inside one of its facets.** Measured while closing A19/A22, and the reason a framed Pluto appeared to sit off its line even after the floating origin removed the jitter. `ORBIT_SAMPLES` is 1024 for every heliocentric ring whatever its radius, so at Pluto's 12 scene units a single chord sags **5.6e-5 scene units** off the true ellipse — **1.4x the entire Pluto-Charon separation**. It is a RESOLUTION limit, not a precision one: the vertices are exactly right, the straight line between them is not. A22 makes it moot for the pair (their own rings are 3e5 times smaller, where 1024 samples sag 1.6e-10), but it still applies to ANY body zoomed in far enough against its own heliocentric ring — Earth framed at Luna's orbit is the next case up. Options, none costed: raise the sample count globally (4096 brings Pluto's sag to 0.09x the pair separation, at 4x the buffer and 4x the propagation on every `setSystem`, which is already the prime suspect for the random slowdowns); scale the count per ring from its radius; or re-emit the ring non-uniformly around the floating origin on a rebase, which now has somewhere natural to live since the rings already keep a float64 master and are re-emitted there. **FIXED v2.1.321-beta by the THIRD option, and confirmed on screen first — the kink was visible in the outer line beside Pluto.** Diagnosis sharpened: the visible artefact is the 0.35 degree TURN at each vertex, not the sag, because the line spans the whole screen; that is why raising the count cannot win (the kink alone wants ~16k samples at that zoom, and deeper zoom wants more again). The same 1024 samples are now redistributed by a monotonic cubic warp of the orbit parameter, packed into the arc nearest the floating origin. The centre density is SOLVED from the tolerance — chord sag within camDist/2000, sub-pixel at any zoom — rather than picked, so it is correct at 1e-5 and at 1e-2 alike. Costs: the ring to refine is found by scanning the existing float64 master (no propagation), and only a ring passing within 8 camera-distances is refined, so in practice exactly ONE re-propagates per rebase. Re-emitted on rebase and on any zoom past 1.6x, and never called at all while the origin is at zero, so readable scale is untouched. The far side degrades to ~1 degree per vertex at the deepest zooms, which is only ever when it is also 24 scene units away and subtends nothing. **REOPENED AND ACTUALLY FIXED v2.1.322-beta — the v2.1.321 refinement was correct and unreachable.** Its gate asked whether the ring passed within 8 camera-distances of the focus and answered with the distance to the nearest VERTEX of the uniform master. On a 1024-gon at 12 scene units the vertices are 0.074 apart, so a ring passing exactly THROUGH the focus reports 0.037 — **46x over the threshold** — and every ring silently took the uniform path. It now measures the distance to the nearest SEGMENT (the two meeting at that vertex), which is what the question meant; the vertex scan stays, because it fixes the orbit parameter the refinement is centred on. A LESSON worth keeping: a proximity test against a sampled curve must be against its segments, never its samples, or the sampling pitch becomes the floor. Second fault found while proving the first: the dense arc was centred on the floating ORIGIN, which only re-latches after the target has drifted 2000 camera-distances (0.2 scene units at camDist 1e-4) against a dense arc 0.15 wide — so a followed body walked out of its own refinement. The refinement now follows the CAMERA and the rebase follows precision, and it re-emits once the focus crosses a quarter of the arc. | fixed v2.1.322-beta |
| A24 | **Constructs had no orbit lines on the player view.** Reported 2026-07-31 while checking A19/A22, with the ask that they be a lighter colour so they do not read as worlds. They were excluded from the ring branch outright. **FIXED v2.1.321-beta**: a construct now takes whichever branch fits it (heliocentric, parent-local, barycentre-local) in its own `icon_color` lightened 45 percent toward white — and neutral grey under the `white` body-colour selection, which exists so a screen filter sees one palette. Two exclusions remain, both deliberate: a construct carrying `scheduled_journeys` is placed by the transit sampler rather than by its orbit, so a ring from that orbit would be a line it is not on; and a surface-locked one has no orbit to draw, tested per frame rather than at build time because the lock is live. A construct also contributes no radius to a moon ring's clearance, matching its own placement (`radiusScene` is 0 for one). | fixed v2.1.321-beta |
| A25 | **A companion STAR is listed under a heading that says "Moons".** Found by the A6 sweep (v2.1.324); pre-existing, deliberately NOT fixed there to keep that change to what was reported. On a barycentre's page `guideDocument.ts` builds `moonRow = [...companions, ...moons]` and heads the whole thing "Moons" — so the Alpha Centauri AB Barycentre page reads "Moons: ★ Toliman (Alpha Centauri B)", a main-sequence K star filed as a moon, glyph and all. The same block also catches CIRCUMBINARY PLANETS, because `membersOf` matches `n.parentId === bary.id` as well as `memberIds` — confirmed on a synthetic P-type fixture, where both planets land under "Moons" too. **Note it is only wrong for STAR barycentres: for Pluto-Charon the companion genuinely is a moon and the heading is right**, which is why this wants a content-derived heading rather than a rename. Same shape as the fix A6 already applied to its own new block (heading chosen from what is in the list), so the pattern to copy is three lines above it. Related: [[A6]]. **FIXED v2.1.327-beta. DECIDED: two blocks with honest headings, not one heading covering both** — `Companion star` / `Companion stars`, then `Moons`. **The reason one shared heading was rejected is not tidiness, it is that no honest name exists for that list.** It can hold a main-sequence star, a genuine moon (Charon) and a circumbinary PLANET all at once, because `membersOf` matches `parentId === bary.id` as well as `memberIds`. Any single heading would be wrong for two of the three. **So the split is by WHAT EACH NODE IS, not by which query produced it:** a star goes to Companions, a moon joins the Moons list, and a planet is left out of both so the A6 orbiters block files it under Planets. That one change fixes the circumbinary half of this entry as a side effect rather than needing its own pass. **Verified on all three shapes:** the Alpha Centauri AB Barycentre now reads "Companion star: Toliman (Alpha Centauri B)"; Pluto-Charon still reads "Moons: Charon", which is the case the entry flagged as already correct and which a blanket rename would have broken; and on the synthetic P-type fixture both circumbinary planets move out of Moons and into Planets. Also re-checked live on the fixed Struve 2398 and Groombridge 34, where the barycentre pages now read "Companion star" instead of listing a planet as a member ([[D3]]). Related: [[A6]], [[A26]]. | fixed v2.1.327-beta |
| A26 | **A ringed planet's RINGS appear in no drill-in list.** Found by the A6 sweep (v2.1.324). A ring is `roleHint: 'ring'`, so `moonsOf` (which requires `roleHint === 'moon'`) excludes it, and the planet is not a star or a barycentre so A6's new orbiters block does not run for it either — Jupiter's page lists Io, Europa, Ganymede and Callisto and never mentions Jupiter's Rings, although the ring is a real node with its own facts page and IS reachable from the star's list, since `listBodiesOf` includes belts and rings. So the ring is orphaned only from its own parent's page. Low value on its own; it is really the same question as A25 — what a body's satellite list should CONTAIN and be CALLED — and the two should be scoped together. Related: [[A6]], [[A25]]. **FIXED v2.1.327-beta, alongside A25 as that note suggested. CORRECTION to this entry: the ring was NOT reachable from the star's list either.** That claim assumed `listBodiesOf` would pick it up, but `orbiters()` matches on the HOST, and all four Solar System ring nodes are parented to their PLANET, not to the Sun — checked in the shipped data. So a ring was reachable from nowhere in the document at all, which makes this worth more than the entry credited it with. **DECIDED: a ring ROW, in its own `Rings` block.** The entry's hesitation was fair — a ring is not somewhere you drill INTO the way a moon is — but it is a real node with its own facts page (radii, temperature, composition), and the star's list has always offered BELTS on exactly that basis. Treating a ring differently from a belt would be the inconsistency, not the row. **Implementation is a widening, not a new block:** A6's orbiters query now runs for ANY subject rather than only stars and barycentres, since `orbiters()` already excludes moons and stars and never returns constructs — so for a planet it yields precisely its rings. The heading is composed from content (`Planets`, `Belts`, `Rings`, or a joined pair), so a page never announces something it is not showing. **Verified:** Jupiter reads "Moons: Io, Europa, Ganymede, Callisto" then "Rings: Jupiter's Rings"; Mars, which has no rings, is unchanged. Related: [[A6]], [[A25]]. | fixed v2.1.327-beta |
| A27 | **QUESTION, raised by A2 (v2.1.325) and deliberately not answered: is `cargoDescription` player-safe?** It is a free-text manifest the GM writes — Blip-A's reads "Xenonite hull stock, tools for every conceivable emergency, a large supply of hot high-pressure ammonia, and one grieving, brilliant engineer" — and 24 of the campaign's 26 constructs carry one. **The reason it is a question and not an obvious yes:** every other narrative field has its own redaction lever and this one has none. `description` is player-visible but can be withheld per node via `description_playerhidden`; `gmNotes` is stripped outright. `cargoDescription` has neither, so including it would make a ship's manifest unconditionally public the moment the construct itself is visible — and "what is in that hold" is exactly the sort of thing a GM reveals on a roll rather than on a panel. The A2 block therefore prints the cargo TONNAGE (observable from outside, and already reaching players) and leaves the prose out. **Three ways it could go, and it is a design call rather than a bug:** (a) show it, treating a manifest as public; (b) give it a `cargoDescription_playerhidden` flag of its own and default to showing it; (c) treat it as GM-only and strip it in `computePlayerSnapshot` alongside `gmNotes` — worth noting the snapshot does NOT strip it today, so it is already crossing the wire even though nothing renders it. **DECIDED 2026-07-31: option (c), GM-ONLY — strip it.** Reasoning, and it is worth keeping because it generalises: a STAR CATALOGUE would not know a ship's cargo, fuel or supply reserves — only its CAPACITY. The catalogue is a reference work, not a live telemetry feed. Strip `cargoDescription` in `computePlayerSnapshot` alongside `gmNotes`, which also closes the existing wire leak. "We can always complicate later" — if a GM later wants to reveal one, that is a `cargoDescription_playerhidden` flag added then, not now. **See A29: the same principle indicts three rows that already shipped.** Related: [[A2]], [[A29]]. **DONE v2.1.328-beta, option (c) as decided.** `cargoDescription` is now deleted in `computePlayerSnapshot` (`system/utils.ts`, re-exported from `api.ts`) on the same line as `gmNotes`, so it never leaves the GM's machine. **Note what this actually closed:** the field was already crossing the wire — nothing rendered it, so it read as a display question, but the prose for all 24 constructs that carry one was in every broadcast. This is a leak fix, not only a policy decision. **No `cargoDescription_playerhidden` flag was added**, per the decision: a GM who later wants to reveal one gets a flag then. Related: [[A2]], [[A29]]. | fixed v2.1.328-beta |
| A29 | **The construct block prints CURRENT LEVELS, not just capacity — and by A27's own reasoning it should not.** Raised 2026-07-31 when deciding A27, and it applies to code that SHIPPED in v2.1.325, so it is a live player-visibility question rather than a design musing. A27 was settled with the principle "a star catalogue would not know cargo or fuel or supply reserves, only capacity". That indicts three rows of the new construct block, not just the cargo prose. **Verified in `catalogue/bodyFacts.ts`:** `Crew` prints "current of max" (`:106`), `Cargo` prints "current of capacity t" (`:121-123`), and `Fuel` prints "current volume of capacity m³" (`:134-137`). Each shows a live reading a reference work would not hold — how loaded a hold is, how much range a ship has left, how many hands are aboard. **The counter-argument to weigh before stripping:** a player looking at their OWN ship would reasonably know all three, so a blanket strip may be too blunt; the honest split might be that capacity is catalogue data and current level is telemetry, with telemetry shown only for constructs the viewing player owns or is aboard. That needs the ownership/visibility model checked before it can be built — find out what `computePlayerSnapshot` already knows about who is looking. **Do not guess: get the decision before changing shipped behaviour**, and note that whichever way it goes, `Total mass` (`:124`) is derived FROM the current fuel and cargo, so it leaks the same reading indirectly and has to move with them. **DIRECTION SET 2026-07-31: gate them behind a Player View preset toggle, not a strip.** Proposed name "Show temp info", rejected because "temp" reads as TEMPERATURE in an app full of temperatures. **Use "Live readings"**, with the sub-line "Fuel, cargo and crew as they are now, not just capacity" — a *reading* is taken now, a *specification* is permanent, and the name extends cleanly to anything transient added later (position, heading, damage). It belongs with the other presentation booleans in `player/presetTypes.ts` (`auroras`, `unlit`, `starmapRouteGlow` etc.), so a "star catalogue" preset has it off and a "ship's console" preset has it on — same data, different document. **KNOW WHAT THIS BUYS, because two different boundaries are in play.** The SNAPSHOT decides what LEAVES (`computePlayerSnapshot`, `src/lib/api.ts`) — it already deletes `gmNotes` (:75/:86/:99), honours `description_playerhidden` (:78) and `object_playerhidden` (:43), and strips `scheduled_journeys`, `draft_transit_plan`, `classification` and `aiContext` outright (:113-116), so the codebase already treats forward-looking tactical data as never-leaves. The PRESET decides only what is SHOWN, on data already sitting on the player's machine. A preset toggle is therefore COSMETIC, not secure: the figures still cross the wire and anyone reading the raw broadcast has them. That is acceptable here because the stated intent is "a star catalogue would not ASSERT this", not "a player must never obtain it" — but it should be a knowing choice, and if the answer ever changes to the latter, current fuel and cargo belong beside `draft_transit_plan` in the snapshot strip rather than behind a checkbox. **CONFIRMED 2026-07-31, and the trade-off was accepted knowingly: build it as a PRESENTATION toggle.** The figures continuing to cross the wire is a KNOWN and ACCEPTED consequence, not an oversight — "if they hack the data stream, well, that is live role playing, well done them". **So do not harden this into a snapshot strip without being asked.** A future session finding live readings in the broadcast will be tempted to treat it as the same fault A27 closed; it is not, and that would silently take a decision that has already been taken the other way. `Total mass` (`:124`) is derived from current fuel and cargo, so it moves with them either way. Related: [[A27]], [[A2]]. **BUILT v2.1.329-beta as specified — a `liveReadings` boolean beside `auroras` and `starmapRouteGlow` in `presetTypes.ts`, labelled "Live readings" with the sub-line "Fuel, cargo and crew as they are now, not just capacity."** **The LABEL changes with the setting, not just the value.** Off reads `Crew capacity` / `Cargo capacity` / `Fuel capacity`; on reads `Crew` / `Cargo` / `Fuel` as current-of-capacity. Printing a bare number under an unchanged label would have left the reader guessing which figure they were looking at — the same reasoning that produced `Power surplus` vs `Power output` in A2. **Two more rows had to follow than the direction named, and this is the one judgement call worth reviewing.** `Total mass` was already identified. But `Max acceleration` divides thrust by the CURRENT wet mass, and `Δv (vacuum)` is the log of the wet/dry ratio — i.e. literally how much fuel is left. Leaving them on would have meant the block withheld the fuel reading in one row and restated it in the next, which is the "quietly wrong" pattern A5 and A2 both turned on. Both are therefore gated too, and `Supplies remaining` with them. **The cost is real and is not hidden:** with the setting off there is no Δv figure at all, where a catalogue would reasonably want the RATED Δv at full tanks. That figure is not derived anywhere today and inventing it here would break the read-don't-recompute rule — see [[A31]]. **Defaults, chosen by what each preset IS:** ON for `datapad` ("a hand-held data terminal readout") and `console` ("a ship-console orbital plot") — the two that are instruments; OFF for `guide`, `crt`, `holo` and `projection`, and OFF as the schema default so a stored preset picks it up. This DOES change shipped behaviour: v2.1.325 showed live readings everywhere, and The Guide now shows capacity. That is the correction this item asked for. **The accepted trade-off was honoured, not quietly re-litigated: NOTHING was added to `computePlayerSnapshot`.** Current fuel, cargo and crew still cross the wire, by decision. (`cargoDescription` IS now stripped, but that is [[A27]] and a different question — prose with no hide flag, versus figures with an accepted one.) **Verified on both consumers, off and on:** `FilteredDocumentView` with it off shows Rocinante as Cargo capacity 200 t / Fuel capacity 3,500 m³, and `DocPanel` with it on shows Crew 4 of 6, Cargo 40 of 200 t, Total mass 1,259 t, Fuel 2,820 of 3,500 m³, Max acceleration 3.24 g, Δv 15,842.7 km/s. Blip-A checked the same way against the bundled map. Related: [[A27]], [[A2]], [[A31]]. | fixed v2.1.329-beta |
| A30 | **What SHOULD a construct look like in the info block?** Raised by A28 (v2.1.328), which removed the wrong picture — a ship was being illustrated with a planet's sphere — and deliberately did not choose a right one. So a construct now has a blank where every body has a graphic, which is honest but plain. **The material already exists, which is why this is a decision rather than an art job:** `bodyGlyph` gives a construct `◆`; every construct carries an authored `icon_type` and `icon_color` that the 2D starmap and the holo scene BOTH already draw it with; and the class string is structured enough to distinguish a station from a frigate from a derelict ("Station/Colony/Spin", "Ship/Frigate/Corvette"). **Options:** (a) leave it blank — a stat block needs no portrait; (b) draw the construct's own `icon_type`/`icon_color` at info-block size, which makes the panel agree with the marker the player is looking at on the map, and costs nothing new in data; (c) silhouettes by hull class, which is real artwork and a rule-pack asset question. (b) is the obvious middle and the only one that reuses what is already authored. **Constraint, and it has come back twice:** whatever is chosen belongs to the INFO BLOCK only and must never reach the system map. A GM-uploaded `image` must keep winning over any of it. Related: [[A28]], [[A2]]. **G3's design investigation (2026-08-01, `docs/dev/ship-appearance-design.md`) ANSWERS the long-term question but does NOT close this: the cheap interim should proceed NOW, independent of whether the 3D work ever happens.** Verdict from that doc: build option (b) — the `icon_type`/`icon_color` glyph at info-block size, the exact `getConstructIconTexture` vocabulary, so the panel agrees with the marker on the map — as a small self-contained block. It is not throwaway: it remains the permanent fallback tier under the eventual priority chain photo > 3D model > glyph > nothing. Option (c), silhouettes by hull class, is REJECTED in that doc with evidence (class strings are free-form — "Sensor Platform", "Warship w/Fighter Wing" — so the mapping is a fragile keyword heuristic feeding bespoke artwork). Related: [[G3]]. | captured — interim scoped, go |
| A31 | **A construct has no RATED performance figure — only its performance right now.** Raised by A29 (v2.1.329). `calculateFullConstructSpecs` computes `maxVacuumG` from the CURRENT wet mass and `totalVacuumDeltaV_ms` from the current wet/dry ratio, so both are readings of how loaded and how fuelled the ship is at this instant. With "Live readings" off there is therefore no acceleration or Δv row at all — which is honest, but a catalogue entry for a ship would certainly quote a Δv, and quoting the current one under a capacity heading would be exactly the fault A29 exists to fix. **What is missing is a FULL-TANKS figure:** Δv at `fuelCapacity`, and the acceleration band from full to empty. **Neither would be new physics** — `ConstructDerivedSpecs.svelte` (~:89-92) already derives an accel range across `fuelCapacity_tonnes` for the GM panel, so the arithmetic exists; it is not on `ConstructSpecs`, so the document layer cannot read it without recomputing, and A2's standing rule is that this layer READS derived values rather than deriving its own. **The fix is therefore in `construct-logic.ts`, not in the document:** add rated `deltaV`/`accel` fields to `ConstructSpecs` next to the current ones, then the capacity view can print a rated Δv and the GM panel can stop computing its range inline. Small, but it crosses out of the document layer, which is why A29 stopped short. Related: [[A29]], [[A2]]. | captured |
| A28 | **A construct is drawn as a planet's disc in the info block.** Noticed while verifying A2 (v2.1.325). The body-graphics setting ('3D sphere' / 'Simple disc' / 'Flat shape') is applied to whatever is selected, so Blip-A — a 110 m ship — gets a featureless orange sphere above its stat block, the same graphic a rocky world would get. `guideDocument` pushes a `bodyDisc` block for any subject when imagery is disc/sphere/flat, with no test for `kind === 'construct'`. Pre-existing and NOT touched by A2, which changed only the facts. **Note the constraint before fixing it:** the construct already has a glyph (`bodyGlyph` gives it `◆`) and an `icon_type`/`icon_color` pair the starmap and the holo scene both use to draw it, so the honest picture already exists in data — this is about the info block reaching for the wrong one, not about inventing artwork. A GM-uploaded `image` on the construct should still win, as it does now. Related: [[A2]]. **FIXED v2.1.328-beta. DECIDED: draw NOTHING for a construct, and do not invent a graphic for it here.** Root cause exactly as scoped — the `bodyDisc` branch tested only the imagery mode, never the kind, so a ship took a world's illustration. One added condition. **The reasoning for stopping at nothing rather than substituting a glyph:** the wrong picture is worse than no picture, so removing it is the whole of the reported fault; choosing what a construct SHOULD look like is a design question with real options (the `◆` glyph, the `icon_type`/`icon_color` pair the starmap and holo scene already use, a silhouette by hull class) and inventing one mid-fix is how a look ends up in code instead of in data. Raised as [[A30]]. **The GM-uploaded photo path is untouched and was checked, not assumed:** it is a separate branch above, gated on a loaded image, so a construct with a picture still shows it and only the procedural-body branch is refused. **The standing rule was respected:** this REMOVES a block from the info-block builder and adds nothing anywhere, so no construct graphic goes near the system map. **Verified on both consumers and both ways round:** Blip-A produces no `bodyDisc` block with imagery set to 'disc' while Jupiter still does, and live in `DocPanel` the Rocinante panel runs heading straight into facts with no sphere. Related: [[A2]], [[A30]]. | fixed v2.1.328-beta |
| A17 | **THE MEASURE TOOL HAS NEVER COUNTED DEPTH. It reports the planar distance whatever the campaign setting says.** Found by the A15 regression check, NOT caused by it — this predates A8 and A12 and has been wrong since WS7 introduced z on 2026-07-30. **Evidence:** Vega (depth +16 ly) to Zeta Reticuli (depth -35 ly) reads **34.17 ly**, identical with "ignore depth" on and off. The pair is 51 ly apart in depth ALONE, so no 3D separation between them can be below 51; the honest figure is about `sqrt(34.17² + 51²) ≈ 61 ly`. A GM measuring that pair is being told roughly HALF the distance the routes and journey durations for the same pair would use. **Root cause, and it is not where you would look.** The plumbing is right: `measureDist` (`Starmap.svelte:692`) already calls `systemSeparation(mA, mB, pixelsPerUnit, !zCounts(starmap))`, so the flag is wired correctly and the 3D branch does run. The endpoints are what is wrong — `measurePick(x, y, label, constructId)` (`:673`) takes only x and y, and the call site passes `s.position.x, s.position.y` (`:704`), dropping `s.position.z`. `posZ()` then reads both endpoints' absent z as 0, so the "3D" calculation returns the planar answer and the flag can change nothing. This is why the setting appears to work everywhere else: routes and journeys pass whole `position` objects. **Fix is small but not one line, and it needs its own verification:** add z to `MeasureEnd`, pass `s.position.z` at `:704`, and carry z through `resolveMeasure` (`:681`) for construct endpoints — which means `constructDisplayPlacement` has to supply a z for transit/adrift points too, or ship measurements stay planar while system measurements go 3D, which would be worse than the honest bug. Deliberately NOT fixed inside the A15 check: changing what a measurement REPORTS changes what the campaign counts, so it wants its own commit and its own before/after. **FIXED v2.1.314-beta. Four numbers, same pair, both zooms: depth counting 60.99 / 60.99 ly, depth ignored 34.17 / 34.17 ly.** The arithmetic checks out independently — `60.99² − 34.17² = 2552`, so Δz ≈ 50.5, which is what the two labels round to at ±1 (they print 16 and −35 via `Math.round`). **What was ruled out:** the flag, the predicate and the module were all innocent. `measureDist` already passed `!zCounts(starmap)`, `systemSeparation`'s 3D branch always ran, and `posZ` did exactly what it documents. The coordinate was amputated one level above all of them, at the pick. **The second caller, decided:** a CONSTRUCT endpoint needs no z at pick time, because `resolveMeasure` re-derives its whole position from the clock every frame — anything stored at the pick is overwritten immediately. So the ship's depth had to come from the placement instead, and `ConstructPlacement` now carries it wherever it is EXACTLY derivable: resting in a system it is that system's z; in transit it is the same lerp as x and y, because an interstellar hop is a straight line and `frac` was already on the placement. **Two honest gaps left, deliberately, and both are data-model not display:** a POINT origin/destination (`fromX/fromY`, `toX/toY`) has no `fromZ/toZ` in the journey schema, and `AdriftConstruct` stores only x/y — so a ship stranded in interstellar space has no recorded depth and reads as being on the plane. Also NOT invented: a `vz` for a coasting drift, because the slingshot deflection is modelled as a planar rotation of `vx/vy` and giving it an out-of-plane component is a transit-physics decision, not a display one. A coasting ship holds the depth it had when stranded. See A21. **Pinned by a test at the level the bug lived** (`Starmap.spec.ts`, "measure tool depth (A17)"): it drives the real path — ruler on, tap two stars, read the label — on a fixture 40 apart in plan and 9 in depth, so the answers are exactly 41 and 40. A unit test of `systemSeparation` would NOT have caught this, since that module was never wrong; confirmed by reverting the call site, which fails the test with `expected '40 ly' to be '41 ly'`. | fixed v2.1.314-beta |
| A14 | **What should the 3D starmap do when the campaign ignores depth?** Raised by A12, deliberately left unanswered there. A12 drops the depth cues from the 2D GM starmap on the grounds that annotating a number which affects no distance is noise. The same argument does not obviously carry to the 3D view, because in 3D the depth is not an annotation — it is the geometry, and the whole reason for the view. Three coherent answers: (a) leave it, and treat "ignore depth" as a MEASUREMENT setting only, which is exactly what `systemDistance.ts` says it is ("`ignoreZ` is the campaign's own choice… so an existing map keeps the distances it always had") — the 3D view then honestly shows a layout whose separations are not what the campaign counts; (b) flatten the 3D view to the plane when the flag is on, which makes the two views agree but throws away the placement work and arguably makes the 3D view pointless for that campaign; (c) keep the geometry but suppress the numeric read-outs, the same line A12 drew in 2D. Not a bug; a decision about what the setting means. Worth deciding before someone "fixes" the 3D view to match the 2D one. **DECIDED 2026-07-31: option (c).** Keep the geometry — bodies stay where they were placed, because that is the entire point of the 3D view — and suppress the numeric depth READ-OUTS, which is exactly the line A12 drew in 2D. The reasoning is then consistent across both views: the placement is real and stays visible, the number that affects no distance is not asserted. Option (b) is explicitly rejected: flattening throws away placement work. Implement via `zCounts()` (`src/lib/map/systemDistance.ts:22`), never by reading the flag directly. **BUILT 2026-07-31 at v2.1.312 — and the decision turned out to need NO behaviour change, which is a result rather than a dodge.** Both halves of option (c) were already true, and this is a proof rather than a sample. **Geometry is kept:** `Starmap3DView.svelte:53` passes `z` through unconditionally, and nothing in the 3D path can do otherwise — `src/lib/starmap/**` and the catalogue route never import `systemDistance`, never call `zCounts()` and never reference `ignoreZForDistances`; the single mention in `starmapScene.ts:201` is a comment saying distances live elsewhere. The flag cannot reach the scene, so option (b) was never a risk here. **There is no numeric depth read-out to suppress:** the scene draws exactly three kinds of text — system name (`starmapScene.ts:586`), route name (`:603`) and the grid distance rings (`:384`), and the rings come from `spanMap = max(maxX-minX, maxY-minY)` (`:532`), which is planar and never sees z. A search of the whole tree for a signed depth figure finds exactly two, both the 2D GM cues A12 already handles; `position.z` has only three consumers in `src/` (`Starmap.svelte`, `map/rebase.ts`, `map/systemDistance.ts`), so no document, panel or player surface prints a depth anywhere. **NO `zCounts()` call was added, deliberately:** a guard on a read-out that does not exist would be dead code asserting a rule it does not enforce. What WAS added is the durable part — a comment at the `z` pass-through recording the A14 decision and its reasoning, at the one line where a future session could break it by "making 3D match 2D". Note also that there is no GM 3D starmap at all: `Starmap3DView` is mounted only by the player catalogue and the preset-editor preview. | verified v2.1.312-beta |
| A16 | **LANDED — one shared handling for the floating chrome (picker + time controls).** Sanctioned follow-on from A11, not drift: A11 gave the body picker a floating puck, and the time control already had one, so the two were doing the same job twice. Being unified into shared components — `FloatGrip.svelte`, `FloatPin.svelte` and a new `src/lib/ui/` — with `TimeControls.svelte` losing the bulk of its own implementation and `BodyPicker.svelte` adopting the shared one. Recorded here because at the time of writing it is UNCOMMITTED and partly UNTRACKED, so it exists only in a working tree; a future session finding `FloatGrip` with no board entry would have no idea what it was for. **Note the blast radius before finishing it:** `TimeControls` is mounted on the GM starmap, the GM system view and the player catalogue, so this is a shared-component change reaching a player surface — the same trap A11 carried. Land it as its own commit with `npm run build` green, and verify the picker AND the time control on both a GM view and a player view before moving on. Related: [[A11]], [[A13]] — the third floating panel is `StarmapInfoPanel`, which has its own drag/persist implementation and is the obvious next thing this should absorb, if it does not already. **Shipped: the picker and the time transport are now ONE floating control (v2.1.311), and a control that would open off an edge pushes back on rather than overflowing (v2.1.312).** The entry is KEPT rather than deleted now that it is done, because it is the only place that says why `FloatGrip.svelte`, `FloatPin.svelte` and `src/lib/ui/` exist — a future session finding them with no board entry is exactly the confusion this was written to prevent. **CORRECTION to the blast radius above: `TimeControls.svelte` is NOT mounted on the player catalogue.** It has exactly TWO mounts, both GM — `Starmap.svelte:1449` and `SystemView.svelte:2278`. The player catalogue has its own separate, much smaller time puck (`.time-controls` at `catalogue/+page.svelte:1068`: a play/pause button, a rate slider and `use:clickOutside`), which this change did not touch and which still works. The player surface IS reached by this batch, but through `BodyPicker`, not `TimeControls` — so the A11 trap is real and the verification still had to happen, just on the picker. **Verification done on every surface (2026-07-31, at v2.1.312).** GM starmap: both controls, drag / lock / auto-close / open-rightwards. GM SYSTEM view (the mount that had not been exercised): unlock puts the pill away to its clock, dragging the clock moves it 180/-60 without counting as the tap that opens it, tapping reopens with the grip back and wholly on screen, and touching the orrery canvas closes it again. Player catalogue: puck at left 10, strip opens rightwards from exactly 10 to 430 with grip + pin + a working list, auto-closes to the puck on touching the scene, and the catalogue's own time puck still expands. **`StarmapInfoPanel` is deliberately NOT absorbed, and this is the reason.** It is not the same kind of object. The floating control models a puck that hides itself and remembers a POSITION; the info panel is a persistent notes panel that remembers a position AND A SIZE (A13), anchors by absolute `left/top` rather than an offset from a host anchor, is dragged by its whole header rather than a grip, and must NOT auto-close — auto-hiding notes you are typing into would be a bug. Absorbing it would mean either extending the shared control with a size model only one consumer wants, or leaving it half-converted with position shared and size bespoke. The second is worse than what is there now, and the first risks re-breaking A13, which was fixed hours earlier and is the panel that was actually complained about. Left alone on purpose; revisit only if a THIRD panel wants a persisted size, at which point the size model earns its place. | fixed v2.1.311-312-beta |
| A15 | **Regression check: does the MEASURE TOOL still work after A8 and A12?** Raised while deciding A14, not a reported fault — a check that has to be done before the batch is trusted. Both changes touched it from different directions. A8 (v2.1.310) made the measure read-out hold a constant screen size under zoom, so it now divides by `labelK` like the other labels — a read-out that renders at the wrong size, in the wrong place, or not at all would be that change. A12 (v2.1.309) hides depth cues when the campaign ignores depth, and the measure tool is precisely where depth is meant to STOP counting rather than stop being shown: `zCounts()` governs whether a measured distance is planar or 3D, and A12 must not have altered the arithmetic — only the annotation. **Verify both readings:** measure between two systems with the flag OFF (depth counts, the figure should be the 3D separation) and ON (planar), at more than one zoom level, and confirm the read-out is legible and correctly placed at each. If the figure moved when only the ANNOTATION should have, that is a real bug in A12 and it is more serious than anything A12 fixed. **DONE 2026-07-31 at v2.1.312. A8 and A12 are both CLEAN — and the check found a much older bug underneath, now A17.** Measured Vega (depth +16 ly) to Zeta Reticuli (depth -35 ly) on the Science Fiction map, four readings: flag OFF at zoom 0.397 → **34.17 ly**; flag OFF at zoom 1.234 → **34.17 ly**; flag ON at zoom 1.234 → **34.17 ly**; flag ON at zoom 0.397 → **34.17 ly**. **A8 verdict: clean.** Across a 3.1x zoom range the read-out is pixel-identical (17.33 x 56.21), stays centred on the line midpoint, and holds the same 5 px gap above it — legible and correctly placed at both ends. **A12 verdict: clean, and exonerated by the same numbers.** The figure did not move when the flag was toggled, which is what A12 promised. The depth ANNOTATIONS behaved: 41 cues → 0 → 41 across the toggle, with the measure read-out unaffected throughout. **But the figure not moving is itself the finding.** It should have moved, and 34.17 ly is impossible for that pair: the two systems are 51 ly apart in depth alone, so no 3D separation between them can be less than 51. The tool has never counted depth — see A17. Neither A8 nor A12 caused it: `measurePick` and its call site last changed on 2026-06-16 (`f626123`, `f5c6e72`), before WS7 added z at all, and neither of this batch's commits touches them. | verified v2.1.312-beta |
| A32 | **Changing the info-panel width does not take effect unless the player window is ALREADY OPEN when you save — and the top third of the slider does nothing at all.** Reported 2026-08-01: "it ONLY updates if I have the window open already and edit and save it — maybe it is scaling of old window rather than current size". The instinct is right, and there are THREE faults in five lines, only one of which was reported. **VERIFIED, all in `src/routes/catalogue/+page.svelte`.** **(1) THE CONVERSION IS ONE-SHOT.** `applyPlayerPreset(p)` (`:350`) converts the stored fraction to pixels at `:357` — `inspectorWidth = Math.max(200, Math.min(640, Math.round(p.inspectorWidthPct * window.innerWidth)))` — and that is the ONLY place it happens. It is never recomputed on resize, so the pixel width is frozen against whatever the viewport was when the preset was last applied. That is precisely why editing and saving with the window open works: it re-runs `applyPlayerPreset` against the current size. A window opened later, or resized after, keeps a stale figure. **(2) THE CLAMP KILLS THE TOP OF THE SLIDER, and this is [[F10]] RECURRING IN MIRROR IMAGE.** The slider's range is 0.15 to 0.50 (`PlayerPresetEditor.svelte:541`) but the live view clamps to `min(640, ...)`. On a 1920 px display 50% is 960 px, clamped to 640 px = 33%, so roughly the top third of the travel moves nothing. F10 fixed exactly this fault in the PREVIEW, which was drawing a raw pixel figure capped at 340 — nobody checked the LIVE view, which caps at 640. Whatever is decided, the editor's range and the live clamp must agree, or the slider is lying again. **(3) TWO SOURCES OF TRUTH FOR ONE NUMBER.** `inspectorWidth` is ALSO seeded from `localStorage['holo-insp-width']` (`:186-187`) and written by the drag handle (`:191-196`) as a PIXEL value, while the preset stores a FRACTION. Last writer wins and the two do not mean the same thing — a width dragged on one screen is a different proportion on another, which is the very problem F10 moved to fractions to solve. Decide which owns the value; the F10 reasoning says the FRACTION should, with the drag writing back a fraction rather than pixels. **The proper fix makes the pixel width a DERIVED, REACTIVE quantity** — a function of the stored fraction and the CURRENT viewport, recomputed on resize — rather than a variable assigned once at apply time. Related: [[F10]], [[A13]]. **FIXED v2.1.342-beta — all three, plus a fourth the verification turned up, and there was a FIFTH mechanism nobody had named.** Root cause exactly as scoped for (1)(2)(3), and the proper fix was taken: `inspectorWidth` is now a REACTIVE DERIVATION of a stored fraction and the live viewport (`$: inspectorWidth = clamp(pct × viewportW)`), with `<svelte:window bind:innerWidth>` feeding it, so it is right on a window opened later and tracks a resize. **THE FIFTH MECHANISM, and it is the one worth reading: making it reactive was not enough, because SVELTE ORDERS `$:` STATEMENTS BY THE ASSIGNMENTS IT CAN SEE.** `applyPlayerPreset` is called FROM a reactive block, so a write to `inspectorWidthPct` buried inside that function cannot order the derivation that reads it — the derivation had already run for that cycle and was never marked dirty again. Measured live: the panel sat at the local default (342 px = 0.18 × 1898) while the preset asked for 0.26, i.e. the preset's width reached nothing at all. The adoption therefore happens IN the reactive block, in plain sight, with a comment saying why it must stay there. This is A32's own fault — a one-way write nobody re-reads — in a second costume, and it would have shipped looking fixed. **(2) The clamp is gone and the ranges agree:** bounds are `INSP_MIN_PCT`/`INSP_MAX_PCT` = 0.15/0.50, character-for-character the editor's slider range, with a 200 px readability floor that is itself capped at half the viewport so it can never exceed the maximum on a narrow screen. Confirmed at the top of the travel: 50% on an 1898 px display gives **949 px**, where the old `min(640, ...)` gave 640 (34%). **(3) DECIDED: the FRACTION owns the value, and the drag writes a fraction.** New key `holo-insp-width-pct` (the old `holo-insp-width` pixel key is abandoned, not migrated — beta). **The tie-break needed one more distinction than the triage names, and this is the judgement call to review:** a stored drag wins on the FIRST application in a window, so a width the reader dragged survives a reload; every LATER application — the GM editing or swapping the preset live — wins over it, because that is what deploying a preset is for. Preset-always-wins would have meant a dragged width never surviving a reload; drag-always-wins would have meant a GM could never set it. **(4) FOUND WHILE VERIFYING, fixed in the same commit: the drag handle was `display: none` at every width.** `.insp-resize { display: none }` sits AFTER the `@media (min-width: 720px)` rule that un-hides it, at equal specificity, so the later rule won everywhere — the panel edge could not be dragged at all. Now `.inspector .insp-resize`. Without it fault (3) has no way to be exercised, which is presumably why it went unnoticed. **Verified live on a 1898 px display, GM + player window side by side, every case:** width changed with the player window SHUT then opened → 949 px (50%); resize → tracks (1500 → 390 px = 26%, 760 → 200 px floor); drag → stores `0.3462` (a proportion, not pixels) and reload restores 759 px = 40%; GM edits the preset to 20% with the window OPEN → 380 px, so the deploy still lands. Note the harness caveat for the next session: a CDP viewport override does NOT emit a `resize` event, so the resize case has to be driven by dispatching one. **RULED OUT:** nothing was changed in `PlayerPresetEditor` — its slider and preview were already right after F10, and the live view was the only side that disagreed. Related: [[F10]], [[A13]]. | fixed v2.1.342-beta |
| A33 | **Two differently-scaled radiation numbers with similar names sit near each other, and the small one reads as a bug.** Reported 2026-08-01 as "radiation is super high on Io but lowish on Mars". **Mars is not low — it is very nearly exact.** `surfaceRadiation` for Mars is **213.69 mSv/yr** against the ~210 mSv/yr Curiosity's RAD instrument measures on the martian surface, i.e. within about 2%. The figure that looks wrong is `irradiationDose: 0.574`, which is a DIFFERENT QUANTITY — `radiation.ts:8` describes it as the cumulative high-energy dose a surface has taken, RELATIVE, with Earth-unshielded-young about 1. It is a surface-ageing ratio, not an annual dose, and it has no business being read on the same scale as a mSv/yr figure. **The rest of the column is right too, which is why the pairing is the fault rather than the physics:** Earth 2.30 mSv/yr against ~2.4 background, Venus 2.0 under 92 bar, Mercury 3,500 unshielded and close in, Phobos 223.9 consistent with Mars beside it. **This is the same fault as [[A5]]** — two numbers describing related but different things, adjacent, with nothing in the presentation to say they are on different scales. A5 was a mean sitting outside its own range; this is a ratio sitting beside a dose. Fix it the same way: make the block say what each number IS. Options are to label the units explicitly, to move the relative figure away from the absolute one, or to drop `irradiationDose` from the reader-facing block entirely if its only real consumer is the ageing model. **Check what else in that block is a ratio rather than a measurement while you are there** — the A5 sweep found [[B8]] by doing exactly that. Related: [[A5]], [[B8]], [[B20]]. | captured |
| A3 | **Player view 2D loses every surface feature — Mars has no craters, Io no volcanism — while the GM view renders them correctly. DIAGNOSED, one line.** Both views mount the SAME `SystemVisualizer`. It draws plain canvas discs and only promotes a body to the feature-rich `PlanetDisc` SVG overlay (the one that consumes craters / lava / plumes / iceCracks) when `trueColorOn && sR >= DISC_OVERLAY_MIN_R` — `SystemVisualizer.svelte:1014`. `trueColorOn` reads the `trueColorMode` store, and that store is **only ever SET in `SystemView.svelte`**, the GM system view. The player route (`src/routes/catalogue/+page.svelte:1025`) mounts `SystemVisualizer` directly inside a `FilterFrame` and never goes through `SystemView` — so nothing sets the store, the promotion never fires, and every body is a flat disc. Nothing wrong with the tags or `deriveAppearance`. **The diagnosis above was for the LEGACY Field Guide skins, and it is not what was being looked at.** In a new Player View the 2D map is not `SystemVisualizer` at all — it is the holo scene locked overhead (`effectiveSystemTier` maps `diagram2d` to `holo`). The flat bodies were the body-graphics sprites of F1: Datapad stores `bodyGfx: 'photo'` and Console `'flat'`, and the sprite path drew a plain class-colour disc with no surface features. With F1 fixed, Mars in the Datapad player view renders fully cratered. No `trueColorMode` change was needed. **Still open, as a separate smaller thing (see A10):** the legacy console skin really does read a GM-local store, and `DISC_OVERLAY_MIN_R = 11` px still means nothing is promoted at whole-system zoom in the GM 2D orrery. | fixed v2.1.286-beta |
| D2 | **CORRECTED — narrower than first written. Craters and volcanism DO auto-spawn on the new worlds; only the air-and-water features cannot.** The first version of this entry claimed the 22 rebuilt systems "can't accumulate surface features because the inputs aren't there". That is wrong for the geological ones: `craterDensity` reads `ageDensity = surfaceAgeGyr / 4.5`, and `surfaceAgeGyr` is DERIVED by `geoActivity` from mass, makeup and system age — all of which a bare `massKg + radiusKm + makeup` body has. Volcanism/lava comes off the geology regime and tidal heat, likewise derived. So those bodies will crater and resurface correctly the moment A3 is fixed. What genuinely cannot appear without authored data: **clouds and haze** (need `atmosphere`), **oceans and ice caps** (need `hydrosphere`), **banding** (needs `rotation_period_hours` — `bandCount()` reads it directly). Original observation below, kept for the survey numbers. | corrected, captured |
| D2a | **Original text (survey still valid, conclusion superseded by D2 above).** Sol's bodies carry 35+ authored fields (`atmosphere`, `hydrosphere`, `biosphere`, `rotation_period_hours`, `axial_tilt_deg`, `magneticField`, `image`, `areas`…). Tau Ceti f/g/h carry FOUR: `massKg, radiusKm, makeup, tags`. Across the 22 systems added in the rebuild, 93% have `makeup` and 0% have atmosphere, hydrosphere or rotation period. So there is nothing for the appearance pipeline to work with — no atmosphere means no clouds/haze/greenhouse, no `rotation_period_hours` means `bandCount()` gives no banding, no hydrosphere means no oceans or caps. **Not a rendering bug**; the renderer is correctly drawing "almost nothing is known". And the data is honest — nobody has measured Tau Ceti f's air. **The question is what a GENERATOR should do about it.** The engine already infers a lot from nothing (makeup from density, classification, temperature, `tidallyLocked` from the despinning timescale). Rotation period is the obvious next inference — a locked body's period is known outright, an unlocked one can take a plausible primordial spin. Atmosphere is harder but volatile retention already gates what a world can hold at its mass and temperature. **Hard constraint if this is built: inferred must be distinguishable from measured** — a tag saying so — or the map starts quietly asserting invented facts about real planets, which is worse than looking plain. Alex noticed via Tau Ceti; Sol looks fine because Sol has been visited. | captured |

---

## Standing rules any worker session must follow

- **Physics and data drive tags; tags drive the image.** Do not add rendering code to make something look a
  particular way. If a look needs a new lever, the lever is rule-pack DATA.
- **WHEN YOU CHANGE A DERIVED QUANTITY, CHASE ITS TAGS.** Physics driving tags is the rule, but nothing
  enforces the follow-through, and it has now failed repeatedly. Before you close a physics item, grep for
  every tag bucketed off the quantity you changed — AND off any older proxy for it, which is the case that
  actually bites. B17 and B22 made Io's dose correct while `surface/irradiation` kept reporting "low",
  because that tag buckets `irradiationDose`, a space-weathering measure that never sees the belt (B28).
  Say in the entry which tags you checked and which you deliberately left.
- **A QUANTITY THAT IS CORRECT FOR ITS PURPOSE CAN STILL BE PUBLISHED AS A LIE.** Three items in one week
  came from this: a relative ratio printed beside an absolute dose (A33), a belt peak labelled "orbital"
  (B27), and an appearance driver published as a hazard reading (B28). Whenever you add a derived field or
  a tag, state WHAT it measures, WHERE, and IN WHAT UNITS — and check that its name and its neighbours in
  the block agree with that answer.
- `tests/fixtures/solar-system-input.json` and `tests/output/solar-system-derived.json` are GENERATED by
  `physics-baseline.test.ts` on every `vitest run`. Never hand-edit; expect churn; last runner wins.
- Commit as **FrunkQ <frunk@frunk.net>**, never ac@epsis.com.
- `npm run build` must be green before any push — svelte-check alone is not enough (runes-mode issues pass
  dev and fail the vite build).
- Bump the patch version every push. `beta` auto-pushes on a green build; production needs explicit approval.
- Stage explicit files, never `git add -A` — parallel sessions share this tree. **The changelog is tracked
  LOWERCASE as `changelog.md`.** On Windows `git add CHANGELOG.md` matches nothing, reports no error, and
  the commit goes out without it — four commits shipped that way on 2026-07-31 before it was noticed.
  Check `git status` after committing, not just the push result.
- UK English in UI, docs and new code. No emoji in docs. No personal names in shipped files.
